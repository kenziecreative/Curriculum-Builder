---
phase: 17-vocabulary-plain-language
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - .claude/plugins/curriculum/agents/curriculum-evaluator.md
  - .claude/plugins/curriculum/agents/knz-validator.md
  - .claude/plugins/curriculum/agents/curriculum-auditor.md
  - .claude/plugins/curriculum/agents/session-generator.md
autonomous: true
requirements: [VOCAB-01, VOCAB-02]

must_haves:
  truths:
    - "curriculum-evaluator.md has full three-layer enforcement: voice reference + Writing for Clarity + inline guardrail — its evaluation-report.md is readable by SMEs"
    - "knz-validator.md has vocabulary guardrails as defense in depth — even though its output is translated by the orchestrator, no jargon leaks through edge cases"
    - "Every agent that writes files a user opens has a post-write vocabulary verification scan referencing the canonical never-say table in curriculum-voice.md"
    - "curriculum-auditor.md column headers use plain language, not schema field names"
  artifacts:
    - path: ".claude/plugins/curriculum/agents/curriculum-evaluator.md"
      provides: "Full vocabulary enforcement for evaluation reports"
      contains: "Writing for Clarity"
    - path: ".claude/plugins/curriculum/agents/knz-validator.md"
      provides: "Vocabulary guardrails for validation output"
      contains: "curriculum-voice.md"
    - path: ".claude/plugins/curriculum/agents/curriculum-auditor.md"
      provides: "Plain-language column headers in audit results"
      contains: "curriculum-voice.md"
    - path: ".claude/plugins/curriculum/agents/session-generator.md"
      provides: "Post-write scan references canonical table"
      contains: "curriculum-voice.md"
  key_links:
    - from: ".claude/plugins/curriculum/agents/curriculum-evaluator.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "voice reference + post-write scan"
      pattern: "curriculum-voice.md"
    - from: ".claude/plugins/curriculum/agents/session-generator.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "post-write scan references canonical list"
      pattern: "curriculum-voice.md"
---

<objective>
Add vocabulary enforcement to all agents that produce user-facing output, and canonicalize post-write scans to reference curriculum-voice.md instead of hardcoding per-agent term lists.

Purpose: Agents are the last line of defense — they write the actual files users open. curriculum-evaluator.md writes evaluation-report.md (read directly by SMEs), session-generator.md writes facilitator guides, and curriculum-auditor.md writes audit-results.md. Each needs enforcement proportional to its user exposure. Post-write scans must reference the canonical list so there is one list to maintain, not four.

Output: 4 agent files updated with appropriate vocabulary enforcement and canonicalized post-write scans.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vocabulary-plain-language/17-CONTEXT.md
@.claude/reference/curriculum-voice.md
</context>

<interfaces>
<!-- Existing enforcement patterns to follow/extend -->

From session-generator.md (lines 396-415) — the post-write scan pattern to replicate:
```markdown
## Post-Write Verification — mandatory before completion signal

After writing all files, verify your own output before reporting complete. Read back each written file and check:

**1. Prohibited vocabulary scan** — the following terms must NEVER appear as visible text in any written file...
- `TMA` — not as a label...
- `bloom_level` — never visible
[...]

**3. If any violation is found:** Fix it in the written file before proceeding.
```

From curriculum-auditor.md (line 15) — existing inline guardrail:
```markdown
**Critical inline guardrail: Never expose schema field names, ID formats, Bloom's taxonomy labels, or instructional design terminology in `audit-results.md` or in the Completion Signal.**
```

From curriculum-evaluator.md — currently has NO voice reference, NO Writing for Clarity, NO inline guardrail, NO post-write scan.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add full vocabulary enforcement to curriculum-evaluator.md</name>
  <files>.claude/plugins/curriculum/agents/curriculum-evaluator.md</files>
  <action>
curriculum-evaluator.md writes evaluation-report.md which SMEs read directly. It currently has NO voice enforcement at all. Add the full three-layer treatment.

1. **Voice reference** — Add after the opening description paragraph (after the `---` that follows "Context Received"), before the "Evaluation Rules" section:

```
## Persona

Read .claude/reference/curriculum-voice.md before generating any user-facing content.

**Critical inline guardrail: Never use in evaluation-report.md or the completion signal: bloom_level, Bloom's, Kirkpatrick, outcome_id, TMA, DCR, WIPPEA, schema, enum, formative assessment, summative assessment, metaskill, DAG. Reference the full prohibited-term list in curriculum-voice.md. Translate all check references to plain language before writing.**
```

2. **Writing for Clarity** — Add immediately after the Persona section:

```
## Writing for Clarity

Write in kernel sentences — one idea per sentence, subject before verb, active voice. No warm-up openers ("In this section we will...", "Now that we have..."). Start every paragraph with the conclusion, then support it. Use precise language — numbers beat adjectives, specific beats general. This applies to every line in evaluation-report.md — strengths, failures, recommendations, and quality ratings.
```

3. **Post-write verification scan** — Add a new section before the completion signal section. This scan references the canonical list rather than hardcoding terms:

```
## Post-Write Verification — mandatory before completion signal

After writing evaluation-report.md, read the file back and verify:

**Prohibited vocabulary scan** — Read the "Terms That Never Appear in Output" table in .claude/reference/curriculum-voice.md. Scan evaluation-report.md for every term in that table. Also scan for: check IDs (T1-xx, T2-xx, T3-xx), tier labels ("Tier 1", "Tier 2", "Tier 3"), schema field names (any_snake_case_field), and enum values used as display labels.

**If any violation is found:** Replace with the plain-language equivalent from curriculum-voice.md, then re-read to confirm the fix. Do not report the completion signal with known violations.
```

Do NOT modify any evaluation logic, check definitions, or report structure. Only add the enforcement layers.
  </action>
  <verify>
    <automated>grep -q "Writing for Clarity" .claude/plugins/curriculum/agents/curriculum-evaluator.md && grep -q "curriculum-voice.md" .claude/plugins/curriculum/agents/curriculum-evaluator.md && grep -q "Post-Write Verification" .claude/plugins/curriculum/agents/curriculum-evaluator.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>curriculum-evaluator.md has voice reference, inline guardrail, Writing for Clarity, and post-write vocabulary scan — evaluation-report.md will be readable by SMEs with no ID or technical vocabulary</done>
</task>

<task type="auto">
  <name>Task 2: Add vocabulary guardrails to knz-validator.md and curriculum-auditor.md, and canonicalize session-generator.md post-write scan</name>
  <files>
    .claude/plugins/curriculum/agents/knz-validator.md
    .claude/plugins/curriculum/agents/curriculum-auditor.md
    .claude/plugins/curriculum/agents/session-generator.md
  </files>
  <action>
Three agents need updates — knz-validator gets lighter enforcement (its output is translated by the orchestrator), curriculum-auditor gets its existing guardrail tightened, and session-generator gets its hardcoded scan canonicalized.

**knz-validator.md:**

Add voice reference and a light guardrail after the opening description, before the "Context Received" section. knz-validator.md output is always translated by validate.md before the user sees it, so full treatment is not needed — this is defense in depth for edge cases.

```
## Vocabulary Guardrails

Read .claude/reference/curriculum-voice.md for the list of prohibited terms. Although your report files are translated by the orchestrator before the user sees them, avoid using prohibited terms in report prose where possible. Schema field names are acceptable in structured report columns (schema-report.md) but should not appear in plain-language failure descriptions or recommendation text.
```

**curriculum-auditor.md:**

1. The existing inline guardrail (line 15) is good but lists specific terms — extend it to reference the canonical list:

Replace the existing guardrail with:
```
**Critical inline guardrail: Never expose schema field names, ID formats, Bloom's taxonomy labels, or instructional design terminology in `audit-results.md` or in the Completion Signal. Reference the "Terms That Never Appear in Output" table in .claude/reference/curriculum-voice.md for the full prohibited list. Translate all field references to plain language before writing. The summary column must be readable by an SME with no ID or technical vocabulary.**
```

This preserves the existing prohibition specifics while adding the canonical reference.

2. Add a post-write vocabulary scan before the completion signal section, following the session-generator pattern:

```
## Post-Write Verification — mandatory before completion signal

After writing audit-results.md, read the file back and verify:

**Prohibited vocabulary scan** — Read the "Terms That Never Appear in Output" table in .claude/reference/curriculum-voice.md. Scan audit-results.md for every term in that table and for any snake_case field names used as visible labels. The summary column and any prose description must be readable by an SME.

**If any violation is found:** Replace with the plain-language equivalent from curriculum-voice.md, then re-read to confirm. Do not report the completion signal with known violations.
```

**session-generator.md:**

The existing post-write scan (lines 400-415) hardcodes a list of prohibited terms. Update it to reference the canonical list while keeping the specific examples (which are useful guidance for this agent):

1. Add this line at the top of the "Prohibited vocabulary scan" subsection (before the bulleted list):
```
Reference the "Terms That Never Appear in Output" table in .claude/reference/curriculum-voice.md for the complete list. The terms below are the most common violations in session output — check for all of them plus any term in the canonical table:
```

2. Keep the existing bulleted list of specific terms and examples intact — they provide useful agent-specific guidance. The canonical reference is additive.

Do NOT modify any behavioral logic, assessment rules, report structures, or generation instructions in these files.
  </action>
  <verify>
    <automated>grep -q "curriculum-voice.md" .claude/plugins/curriculum/agents/knz-validator.md && grep -q "curriculum-voice.md" .claude/plugins/curriculum/agents/curriculum-auditor.md && grep -q "Terms That Never Appear" .claude/plugins/curriculum/agents/session-generator.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 4 agents reference the canonical curriculum-voice.md table; curriculum-evaluator has full three-layer enforcement; knz-validator has defense-in-depth guardrails; curriculum-auditor and session-generator post-write scans reference the canonical list</done>
</task>

</tasks>

<verification>
- `grep -rn "curriculum-voice.md" .claude/plugins/curriculum/agents/` returns a hit for every agent file
- curriculum-evaluator.md has: voice reference, inline guardrail, Writing for Clarity, post-write scan
- knz-validator.md has: vocabulary guardrails section referencing canonical list
- curriculum-auditor.md has: updated inline guardrail referencing canonical list + post-write scan
- session-generator.md has: post-write scan referencing canonical list + keeping existing specific examples
</verification>

<success_criteria>
- Every agent that writes files a user opens has a post-write vocabulary verification scan
- Every post-write scan references curriculum-voice.md rather than hardcoding its own term list
- curriculum-evaluator.md has the same three-layer enforcement as approve.md (voice + guardrail + WfC)
- No agent behavioral logic, evaluation rules, or generation instructions were modified
</success_criteria>

<output>
After completion, create `.planning/phases/17-vocabulary-plain-language/17-03-SUMMARY.md`
</output>
