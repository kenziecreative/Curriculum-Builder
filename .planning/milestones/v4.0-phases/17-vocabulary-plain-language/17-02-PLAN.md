---
phase: 17-vocabulary-plain-language
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - .claude/plugins/curriculum/commands/approve.md
  - .claude/plugins/curriculum/commands/transfer.md
  - .claude/plugins/curriculum/commands/assessments.md
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/marketing.md
  - .claude/plugins/curriculum/agents/session-generator.md
autonomous: true
requirements: [VOCAB-01, VOCAB-04, VOCAB-05]

must_haves:
  truths:
    - "approve.md has three-layer vocabulary enforcement: curriculum-voice.md reference + inline guardrail + dedicated never-say list"
    - "No generation hint or template bracket in any command references Kirkpatrick, MO-2-3, FA-4, or any ID format"
    - "parent_module_id never appears as a visible label in session template — uses HTML comment instead"
    - "Primary metaskill reads as Core thinking skill in module output template"
    - "marketing.md generation instructions do not use prohibited vocabulary terms as visible labels — schema field names are referenced only where structurally required"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/approve.md"
      provides: "Three-layer vocabulary enforcement for highest-stakes output"
      contains: "Never-say"
    - path: ".claude/plugins/curriculum/commands/transfer.md"
      provides: "Kirkpatrick references replaced with plain-language measurement descriptions"
    - path: ".claude/plugins/curriculum/commands/assessments.md"
      provides: "ID codes removed from transparency note example"
    - path: ".claude/plugins/curriculum/commands/modules.md"
      provides: "Primary metaskill label replaced with Core thinking skill"
    - path: ".claude/plugins/curriculum/commands/marketing.md"
      provides: "Generation instructions sanitized of prohibited vocabulary"
    - path: ".claude/plugins/curriculum/agents/session-generator.md"
      provides: "session.md template with parent_module_id as HTML comment"
  key_links:
    - from: ".claude/plugins/curriculum/commands/approve.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "never-say list references canonical table"
      pattern: "curriculum-voice.md"
---

<objective>
Fix every known vocabulary leak in templates and generation hints, and give approve.md the full three-layer enforcement treatment.

Purpose: Templates and bracketed generation hints are where jargon most often leaks to users — if Claude renders a bracket or the SME reads a hint, they see the prohibited term. This plan rewrites every known instance. approve.md gets the strongest enforcement because it produces the final gate summary an SME reads before signing off.

Output: 6 files updated with plain-language templates, approve.md with never-say list and inline guardrail.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vocabulary-plain-language/17-CONTEXT.md
@.claude/reference/curriculum-voice.md
</context>

<interfaces>
<!-- Key locations of known vocabulary leaks from CONTEXT.md code_context -->

From approve.md line 96:
```
Measuring: [plain-language Kirkpatrick level] — [what's being measured]
```

From transfer.md line 154:
```
- [Plain-language Kirkpatrick level, e.g., "Behavior change"]: [what is being measured]
```

From transfer.md lines 159-161:
```
Plain-language Kirkpatrick translations for display:
- "Level 1 - Reaction" → "Participant satisfaction"
- "Level 2 - Learning" → "Knowledge and skill gain"
- "Level 3 - Behavior" → "Behavior change on the job"
```

From assessments.md line 161:
```
> I added [N] assessment(s) and elevated [N] assessment(s) to match their complexity levels. Example: "I added an assessment for MO-2-3 and elevated FA-4 to Evaluate to match its objective."
```

From modules.md line 302:
```
- **Primary metaskill:** [primary_metaskill]
```

From session-generator.md line 182:
```
**parent_module_id:** [M-N]
```

From marketing.md line 95:
```
- A `curriculum_traceability` object with `claim_text` (verbatim excerpt from `content`), `supporting_element` (specific stage reference), and `strength` (direct/inferred/adjacent)
```

From marketing.md line 101:
```
Keep the `curriculum_traceability` pointing to stage-01 audience field.
```

From marketing.md line 105:
```
Use behavioral language matching the Bloom's level of the cited objectives.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add three-layer enforcement to approve.md and fix its template jargon</name>
  <files>.claude/plugins/curriculum/commands/approve.md</files>
  <action>
Add the three-layer enforcement to approve.md. This is the highest-stakes user-facing output — it gets the strongest treatment.

**Layer 1** — curriculum-voice.md reference (already present from Plan 01).

**Layer 2** — Add an inline guardrail immediately after the Persona section (after the Writing for Clarity block added in Plan 01):

```
**Critical inline guardrail: Never use in approval output: bloom_level, Kirkpatrick, outcome_id, schema, formative assessment, summative assessment, TMA, DAG, metaskill, parent_module_id, self_direction_level. Reference the full prohibited-term list in curriculum-voice.md.**
```

**Layer 3** — Add a dedicated Never-say section immediately after the inline guardrail:

```
## Never-say List — Approval Output

These terms must never appear in the final gate summary, gate options, or any text the user reads during the approval flow. Use the plain-language replacement from curriculum-voice.md.

| Never say | Say instead |
|-----------|-------------|
| Kirkpatrick | success measurement, impact measurement |
| bloom_level, Bloom's | thinking level |
| formative assessment | check-in assessment |
| summative assessment | final assessment |
| metaskill | thinking skill, core skill |
| DAG | sequence, learning path |
| outcome_id, MO-X-X | (reference by name or number) |
| schema | (omit) |
| TMA | (omit) |
| parent_module_id | (omit) |
| self_direction_level | learner readiness |
```

**Template fix** — In the Final Validation gate summary template (line 96), replace:
```
Measuring: [plain-language Kirkpatrick level] — [what's being measured]
```
with:
```
Measuring: [measurement approach, e.g., "behavior change on the job"] — [what's being measured]
```

This removes "Kirkpatrick" from the generation hint entirely. Claude still knows how to generate the correct measurement description from the transfer data without the framework name.
  </action>
  <verify>
    <automated>grep -c "Kirkpatrick" .claude/plugins/curriculum/commands/approve.md | grep -q "0" && grep -q "Never-say" .claude/plugins/curriculum/commands/approve.md && grep -q "Critical inline guardrail" .claude/plugins/curriculum/commands/approve.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>approve.md has three-layer enforcement (voice reference + inline guardrail + never-say list), and no Kirkpatrick reference appears in any generation hint or template bracket</done>
</task>

<task type="auto">
  <name>Task 2: Fix template jargon in transfer.md, assessments.md, modules.md, and session-generator.md</name>
  <files>
    .claude/plugins/curriculum/commands/transfer.md
    .claude/plugins/curriculum/commands/assessments.md
    .claude/plugins/curriculum/commands/modules.md
    .claude/plugins/curriculum/agents/session-generator.md
  </files>
  <action>
Fix every known vocabulary leak in template brackets and generation hints across these four files.

**transfer.md:**

1. Line 154 — Replace:
   ```
   - [Plain-language Kirkpatrick level, e.g., "Behavior change"]: [what is being measured]
   ```
   with:
   ```
   - [Measurement approach, e.g., "Behavior change on the job"]: [what is being measured] — measured [when] by [by_whom]
   ```

2. Lines 159-161 — Replace the "Plain-language Kirkpatrick translations" block:
   ```
   Plain-language Kirkpatrick translations for display:
   - "Level 1 - Reaction" → "Participant satisfaction"
   - "Level 2 - Learning" → "Knowledge and skill gain"
   - "Level 3 - Behavior" → "Behavior change on the job"
   ```
   with:
   ```
   Measurement approach translations (use these plain descriptions, never the framework labels):
   - Reaction-level → "Participant satisfaction"
   - Learning-level → "Knowledge and skill gain"
   - Behavior-level → "Behavior change on the job"
   - Results-level → "Business impact"
   ```

3. Line 211 — Replace any remaining "No Kirkpatrick level labels" with "No measurement framework labels".

**assessments.md:**

1. Line 161 — Replace the transparency note example:
   ```
   Example: "I added an assessment for MO-2-3 and elevated FA-4 to Evaluate to match its objective."
   ```
   with:
   ```
   Example: "I added an assessment for your third module outcome about data analysis, and elevated the check-in activity for module 4 to match its thinking level."
   ```

**modules.md:**

1. Line 302 — Replace:
   ```
   - **Primary metaskill:** [primary_metaskill]
   ```
   with:
   ```
   - **Core thinking skill:** [primary_metaskill]
   ```

2. Scan for any other instances of "Primary metaskill" used as a visible label in templates or user-facing output. The internal field name `primary_metaskill` in code/schema references is fine — only visible labels need changing.

**session-generator.md:**

1. Line 182 — The template already has a post-write verification rule (line 406/413) that catches `parent_module_id` as a visible label and converts to HTML comment. Verify the template on line 182 matches the conversion rule. If line 182 still shows `**parent_module_id:** [M-N]` as a visible field, change it to:
   ```
   <!-- internal: parent_module_id=[M-N] -->
   ```
   This makes the session.md template itself consistent with the post-write verification rule that already enforces this.

Do NOT modify any behavioral logic, generation instructions, or schema references in these files. Only change user-visible template text and generation hints.
  </action>
  <verify>
    <automated>echo "--- assessments.md: MO-2-3 and FA-4 check ---" && grep -c "MO-2-3\|FA-4" .claude/plugins/curriculum/commands/assessments.md | xargs -I{} test {} -eq 0 && echo "PASS: no ID codes" || echo "FAIL: ID codes still present" && echo "--- modules.md: Core thinking skill check ---" && grep -q "Core thinking skill" .claude/plugins/curriculum/commands/modules.md && echo "PASS: label updated" || echo "FAIL: label not found" && echo "--- transfer.md: Kirkpatrick in generation hints check ---" && grep -n "Kirkpatrick" .claude/plugins/curriculum/commands/transfer.md | grep -v "guardrail\|Never\|never" | grep -v "^44:\|^50:" && echo "FAIL: Kirkpatrick in non-guardrail lines" || echo "PASS: Kirkpatrick only in enforcement lines"</automated>
  </verify>
  <done>transfer.md has zero Kirkpatrick in generation hints (only in the never-say guardrail line 44 and never-say vocabulary list line 50 which are enforcement instructions, not output); assessments.md uses plain descriptions not ID codes; modules.md shows "Core thinking skill" not "Primary metaskill"; session-generator.md template is consistent with its own post-write rule</done>
</task>

<task type="auto">
  <name>Task 3: Audit and fix vocabulary leaks in marketing.md generation instructions</name>
  <files>.claude/plugins/curriculum/commands/marketing.md</files>
  <action>
marketing.md is listed in VOCAB-01 as having generation instruction vocabulary leaks. Audit and fix.

**Known issues:**

1. Line 95 — The generation instruction references `curriculum_traceability` as a visible field name:
   ```
   A `curriculum_traceability` object with `claim_text` (verbatim excerpt from `content`), `supporting_element` (specific stage reference), and `strength` (direct/inferred/adjacent)
   ```
   This is a schema generation instruction (Claude reads it to know what to produce). The field name `curriculum_traceability` is a prohibited term per curriculum-voice.md. Rewrite the instruction to describe the purpose without using the prohibited term:
   ```
   A `source_link` object showing where each claim comes from: `claim_text` (verbatim excerpt from `content`), `supporting_element` (specific stage reference), and `strength` (direct/inferred/adjacent)
   ```
   Note: This changes the generation hint label only. If the schema file (stage-08-marketing.md) uses `curriculum_traceability` as the actual field name, Claude will still populate the correct field — the generation hint is guidance, not a field mapper.

2. Line 101 — Replace:
   ```
   Keep the `curriculum_traceability` pointing to stage-01 audience field.
   ```
   with:
   ```
   Keep the source link pointing to the stage-01 audience field.
   ```

3. Line 105 — Replace:
   ```
   Use behavioral language matching the Bloom's level of the cited objectives.
   ```
   with:
   ```
   Use behavioral language matching the thinking level of the cited objectives.
   ```

**Full scan:** After making the three fixes above, scan marketing.md for any other instances of terms from the curriculum-voice.md never-say table appearing outside of the existing guardrail/never-say enforcement sections (lines 42 and 48). The guardrail and never-say sections at lines 42 and 48 are enforcement instructions and correctly reference prohibited terms — do not change those.

Do NOT modify the copywriting-doctrine reference, the marketing ratio rule, the file write section, or any other behavioral logic.
  </action>
  <verify>
    <automated>echo "--- marketing.md: curriculum_traceability outside guardrails ---" && grep -n "curriculum_traceability" .claude/plugins/curriculum/commands/marketing.md | grep -v "^42:\|^48:" && echo "FAIL: prohibited term in non-guardrail lines" || echo "PASS: no leaks outside guardrails" && echo "--- marketing.md: Bloom's outside guardrails ---" && grep -n "Bloom's" .claude/plugins/curriculum/commands/marketing.md | grep -v "^42:\|^48:" && echo "FAIL: Bloom's in non-guardrail lines" || echo "PASS: no Bloom's leaks"</automated>
  </verify>
  <done>marketing.md generation instructions use plain-language terms (source link, thinking level) instead of prohibited vocabulary (curriculum_traceability, Bloom's); guardrail and never-say sections unchanged</done>
</task>

</tasks>

<verification>
- `grep -rn "Kirkpatrick" .claude/plugins/curriculum/commands/` returns hits only in guardrail/never-say enforcement lines, never in template brackets or generation hints
- `grep -rn "MO-[0-9]" .claude/plugins/curriculum/commands/` returns zero hits
- `grep -rn "FA-[0-9]" .claude/plugins/curriculum/commands/` returns zero hits
- `grep "Primary metaskill" .claude/plugins/curriculum/commands/modules.md` returns zero hits in template/display context
- approve.md contains "Never-say" section, "Critical inline guardrail", and curriculum-voice.md reference
- marketing.md has zero `curriculum_traceability` or `Bloom's` outside of guardrail enforcement lines
</verification>

<success_criteria>
- Zero Kirkpatrick references in any generation hint or template bracket across all commands
- Zero outcome/assessment ID codes (MO-X-X, FA-X) in user-facing example text
- approve.md has all three enforcement layers
- parent_module_id never appears as a visible field label in session-generator template
- "Core thinking skill" replaces "Primary metaskill" as the user-visible label
- marketing.md generation instructions use plain-language equivalents for all prohibited terms
</success_criteria>

<output>
After completion, create `.planning/phases/17-vocabulary-plain-language/17-02-SUMMARY.md`
</output>
