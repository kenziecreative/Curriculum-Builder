---
phase: 19-pipeline-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - .claude/plugins/curriculum/commands/sessions.md
  - .claude/plugins/curriculum/commands/validate.md
  - .claude/plugins/curriculum/commands/metaskills.md
  - .claude/plugins/curriculum/commands/transfer.md
  - .claude/plugins/curriculum/commands/marketing.md
  - templates/project-scaffold/STATE.md
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05

must_haves:
  truths:
    - "Each auto-chained stage starts with a fresh context window rather than inheriting a saturated context from previous stages"
    - "Registry + workspace STATE.md are the only handoff mechanism between stages -- no conversation state carries over"
    - "STATE.md tracks module-level progress within session generation"
    - "A context clear mid-session-generation does not lose completed module work"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/sessions.md"
      provides: "Context break after sessions + module-level STATE.md tracking"
      contains: "context break"
    - path: ".claude/plugins/curriculum/commands/validate.md"
      provides: "Context break after validation"
      contains: "context break"
    - path: ".claude/plugins/curriculum/commands/metaskills.md"
      provides: "Context break after metaskills"
      contains: "context break"
    - path: ".claude/plugins/curriculum/commands/transfer.md"
      provides: "Context break after transfer"
      contains: "context break"
    - path: "templates/project-scaffold/STATE.md"
      provides: "Module-level progress tracking format"
      contains: "Module Progress"
  key_links:
    - from: "sessions.md completion"
      to: "validate.md start"
      via: "context break -- user types /clear then runs next command"
      pattern: "Type `/clear` now"
    - from: "sessions.md parallel generation"
      to: "workspace STATE.md"
      via: "module-level progress write after each module completes"
      pattern: "Module Progress"
---

<objective>
Replace in-context auto-chaining with context-break handoffs so every stage gets a fresh window, and add module-level progress tracking to session generation so interruptions do not lose completed work.

Purpose: After sessions (Stage 5) fills the context window, all subsequent auto-chained stages generate in a degraded state. Context breaks at every transition give each stage peak quality. Module tracking means a /clear mid-session-generation preserves what is already done.
Output: Auto-chain replaced with context-break handoffs for stages 5-8. Module-level STATE.md tracking added to sessions.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-pipeline-infrastructure/19-CONTEXT.md
@.planning/phases/19-pipeline-infrastructure/19-01-SUMMARY.md
@templates/project-scaffold/STATE.md

<interfaces>
<!-- Current auto-chain pattern in sessions.md (lines 239-247): -->
## Auto-Trigger Validation
After writing all STATE.md updates, automatically invoke the validation command:
> Running validation on your completed curriculum...
Invoke `/curriculum:validate` as a Skill.

<!-- Current auto-chain in validate.md (lines 232-244): -->
## Auto-Trigger Metaskills
Invoke `/curriculum:metaskills` as a Skill.

<!-- Current auto-chain in metaskills.md (line 217): -->
Invoke `/curriculum:transfer` as a Skill.

<!-- Current auto-chain in transfer.md (line 258): -->
Invoke `/curriculum:marketing` as a Skill.

<!-- marketing.md already has the correct pattern (lines 262-264): -->
## Important: No Auto-Trigger
The user must explicitly run `/curriculum:approve`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace auto-chain Skill invocations with context-break handoffs</name>
  <files>
    .claude/plugins/curriculum/commands/sessions.md,
    .claude/plugins/curriculum/commands/validate.md,
    .claude/plugins/curriculum/commands/metaskills.md,
    .claude/plugins/curriculum/commands/transfer.md
  </files>
  <action>
Replace every in-context auto-chain (Skill invocation) with a context-break handoff pattern. The current chain is: sessions -> validate -> metaskills -> transfer -> marketing -> (already manual) approve. Each link currently uses "Invoke as a Skill" which keeps the same context window. Replace with explicit /clear instructions.

**sessions.md changes:**

Find the "Auto-Trigger Validation" section (around line 239). Replace the entire section with:

```markdown
## Next Stage Handoff

After writing all STATE.md updates and confirming all files verified:

> Sessions complete. Type `/clear` now, then run `/curriculum:validate` to check your curriculum.

Do not invoke validate automatically. Do not proceed in the current context. The user must clear context and run the next command manually. This ensures validation runs with a fresh context window, not a saturated one from session generation.
```

**validate.md changes:**

Find the "Auto-Trigger Metaskills" section (around line 232). Replace with:

```markdown
## Next Stage Handoff

This section runs ONLY when `tier_1_failures == 0`.

Do NOT trigger when `tier_1_failures > 0`. The chain stops at the failure message.

> Validation passed. Type `/clear` now, then run `/curriculum:metaskills` to map thinking skills.

Do not invoke metaskills automatically. The user must clear context first.
```

**metaskills.md changes:**

Find the line that says "Invoke `/curriculum:transfer` as a Skill" (around line 217) and its surrounding approval flow. Replace the auto-invoke with:

```markdown
> Your thinking skill map is written and saved. Type `/clear` now, then run `/curriculum:transfer` to design the follow-through system.
```

Remove any "No user prompt before triggering" instruction. The user MUST clear context before the next stage.

**transfer.md changes:**

Find the line "Invoke `/curriculum:marketing` as a Skill" (around line 258) and its surrounding flow. Replace with:

```markdown
> Your transfer ecosystem is written and saved. Type `/clear` now, then run `/curriculum:marketing` to generate enrollment materials.
```

Remove any "No user prompt before triggering" instruction.

**marketing.md:** No changes needed -- it already has the correct "No Auto-Trigger" pattern with a manual `/clear` then `/curriculum:approve` instruction.

**Key constraint:** Every handoff message must follow the exact pattern: `Type "/clear" now, then run /curriculum:{next}`. This is the established handoff pattern already used by intake, outcomes, assessments, and modules (confirmed by grep). The change makes stages 5-8 consistent with stages 1-4.
  </action>
  <verify>
    <automated>! grep -q "Invoke.*Skill" .claude/plugins/curriculum/commands/sessions.md && ! grep -q "Invoke.*Skill" .claude/plugins/curriculum/commands/validate.md && ! grep -q "Invoke.*Skill" .claude/plugins/curriculum/commands/metaskills.md && ! grep -q "Invoke.*Skill" .claude/plugins/curriculum/commands/transfer.md && grep -q 'Type `/clear` now' .claude/plugins/curriculum/commands/sessions.md && grep -q 'Type `/clear` now' .claude/plugins/curriculum/commands/validate.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>No stage command invokes another stage as a Skill. Every stage-to-stage transition tells the user to type /clear and run the next command. The auto-chain is replaced with context-break handoffs at every transition from sessions through marketing.</done>
</task>

<task type="auto">
  <name>Task 2: Add module-level progress tracking to session generation</name>
  <files>
    .claude/plugins/curriculum/commands/sessions.md,
    templates/project-scaffold/STATE.md
  </files>
  <action>
Add module-level granularity to STATE.md tracking within session generation, so a context clear mid-generation does not lose completed module work.

**1. Update workspace STATE.md template (`templates/project-scaffold/STATE.md`):**

Add a "Module Progress" section after the Stage Progress table. This section is only populated during Stage 5 (session generation) and stays empty for other stages:

```markdown
## Module Progress
<!-- Populated during session generation (Stage 5). Empty for other stages. -->
<!-- Status: not-started | in-progress | complete -->
```

The section is initially empty -- it gets populated when sessions.md begins generation.

**2. Modify sessions.md -- Module Reading section:**

After building the module list and before dispatching Tasks, add:

```markdown
Write a Module Progress table to workspace STATE.md with one row per module, all set to `not-started`:

| Module | Name | Sessions | Status |
|--------|------|----------|--------|
| M-1 | {name} | {count} | not-started |
| M-2 | {name} | {count} | not-started |
...

Do this silently.
```

**3. Modify sessions.md -- after each Task completion:**

In the progress reporting section (where it prints "[module_name] sessions written"), add:

```markdown
After printing the progress line, silently update the Module Progress table in workspace STATE.md: set this module's status to `complete`.
```

**4. Modify sessions.md -- resume logic (the `in-progress` status check):**

The existing `in-progress` handler (around line 119) already checks which modules have complete session files. Enhance it to also read the Module Progress table from STATE.md:

```markdown
- `in-progress` → Read the Module Progress table from workspace STATE.md. Identify modules with status `not-started` or `in-progress`. Cross-reference against the file system to confirm which session directories are actually complete. Re-dispatch Tasks only for modules that are not complete. Update Module Progress entries for any modules that have complete files on disk but were not marked complete in STATE.md (handles the case where files were written but STATE.md was not updated before context cleared).
```

**5. Modify sessions.md -- completion:**

After all modules are verified, update all Module Progress entries to `complete` and add a note:

```markdown
After file verification passes, silently update all Module Progress entries to `complete` in STATE.md.
```

**Important constraints:**
- Module Progress updates are SILENT -- same pattern as all STATE.md writes
- The Module Progress table uses the same inline-table markdown format as the Stage Progress table
- Only sessions.md writes to Module Progress -- no other command touches this section
- The `in-progress` resume path must handle the case where STATE.md says a module is in-progress but files exist on disk (trust the file system, update STATE.md)
  </action>
  <verify>
    <automated>grep -q "Module Progress" templates/project-scaffold/STATE.md && grep -q "Module Progress" .claude/plugins/curriculum/commands/sessions.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>STATE.md template has a Module Progress section. sessions.md writes module-level status (not-started/in-progress/complete) as each module completes. Resume logic reads Module Progress to identify incomplete modules. A context clear mid-generation preserves completed module work in STATE.md.</done>
</task>

</tasks>

<verification>
- No "Invoke as Skill" auto-chain patterns remain in sessions.md, validate.md, metaskills.md, or transfer.md
- Every stage handoff uses the "Type `/clear` now, then run" pattern
- Module Progress section exists in STATE.md template
- sessions.md writes module status after each Task completion
- sessions.md in-progress resume reads Module Progress table
</verification>

<success_criteria>
Every stage transition requires the user to clear context before running the next command -- no stage inherits a saturated context window from a predecessor. Session generation tracks module-level progress in STATE.md, so resuming after a context clear re-dispatches only incomplete modules.
</success_criteria>

<output>
After completion, create `.planning/phases/19-pipeline-infrastructure/19-03-SUMMARY.md`
</output>
