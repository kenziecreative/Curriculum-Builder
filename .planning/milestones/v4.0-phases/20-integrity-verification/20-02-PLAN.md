---
phase: 20-integrity-verification
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/sessions.md
  - .claude/plugins/curriculum/commands/metaskills.md
autonomous: true
requirements: [INTG-01, INTG-03]

must_haves:
  truths:
    - "Draft-audit checks at stages 4-6 catch outcome drift, generic content, broken cross-stage links, missing formative assessment, pre-work gaps, and doctrine violations — not just completeness and vocabulary"
    - "Session generation verifies each session actually achieves its module goal — substantive content with topic-specific nouns, not generic placeholders"
    - "Session generation verifies pedagogical flow — pre-work references, session sequencing, and build-on-previous are structurally real"
    - "Each stage runs only the subset of checks relevant to its output type"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/modules.md"
      provides: "Expanded draft-audit with stage-4-relevant integrity checks"
      contains: "Outcome Drift"
    - path: ".claude/plugins/curriculum/commands/sessions.md"
      provides: "Expanded draft-audit with stage-5 checks + goal-backward verification"
      contains: "Goal-Backward Verification"
    - path: ".claude/plugins/curriculum/commands/metaskills.md"
      provides: "Expanded draft-audit with stage-6-relevant integrity checks"
      contains: "Outcome Drift"
  key_links:
    - from: ".claude/plugins/curriculum/commands/sessions.md"
      to: "module-spec.md files"
      via: "goal-backward check reads module goals and verifies session content against them"
      pattern: "module.*goal|substantive|wired"
    - from: ".claude/plugins/curriculum/commands/modules.md"
      to: "curriculum-registry.json"
      via: "outcome drift check compares draft content against registry outcome wording"
      pattern: "outcome.*drift|registry"
---

<objective>
Expand the Phase 19 draft-then-audit mechanism from 4 checks to a comprehensive integrity system with 7 named failure modes, plus goal-backward session verification that ensures sessions actually achieve their module goals.

Purpose: The existing 4 checks (completeness, registry consistency, vocabulary, schema) catch structural problems but miss content-level failures — generic placeholder content, outcome wording that drifted from the original, missing formative assessments, and sessions that exist but don't teach anything. This plan adds the content-level checks that make the integrity system catch its own failures.

Output: Expanded draft-audit sections in modules.md, sessions.md, and metaskills.md with stage-appropriate integrity checks and goal-backward verification.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-integrity-verification/20-CONTEXT.md
@.planning/phases/19-pipeline-infrastructure/19-02-SUMMARY.md
@.planning/phases/20-integrity-verification/20-01-SUMMARY.md

<interfaces>
<!-- Existing draft-audit structure in each command file (from Phase 19) -->

Draft Audit pattern (identical in modules.md, sessions.md, metaskills.md):
- Check 1: File Completeness — verify files exist with non-zero content
- Check 2: Registry Consistency — verify IDs in drafts exist in curriculum-registry.json
- Check 3: Vocabulary Scan — scan for prohibited terms from curriculum-voice.md
- Check 4: Schema Compliance — verify required fields present per stage schema
- [Verification Integrity section added by Plan 01]
- Audit Result — promote if all pass, auto-fix vocab, stop if unfixable

New checks are ADDED after Check 4, before Verification Integrity. They become Check 5, 6, 7 etc.
Each stage gets only the checks relevant to its output type.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integrity checks to modules.md and metaskills.md draft-audit sections</name>
  <files>.claude/plugins/curriculum/commands/modules.md, .claude/plugins/curriculum/commands/metaskills.md</files>
  <action>
Read modules.md and metaskills.md. Add new integrity checks after Check 4 (Schema Compliance) and before the Verification Integrity section (added by Plan 01) in each file's Draft Audit section.

**For modules.md (Stage 4) — add these checks:**

**Check 5: Outcome Drift**
Read `workspace/{project-name}/curriculum-registry.json` field `outcome_wording.program_outcomes` and `outcome_wording.module_outcomes`. For each outcome ID referenced in the draft module specs, compare the outcome statement text in the draft against the registry's canonical wording. If the wording has changed beyond minor formatting (e.g., capitalization, punctuation), flag it as outcome drift. Report the module file, the outcome ID, the registry wording, and the draft wording.

This is a blocking failure — outcome wording must match the registry. Auto-fix by replacing draft wording with registry wording.

**Check 6: Generic Content Detection**
For each module spec's `content_chunks`, `social_learning.group_processing_prompt`, `metaskill_activation_activity`, and `belief_challenging_encounter`: verify the content contains topic-specific nouns from the module's subject domain. Flag any field where the content could apply to ANY module without modification. Examples of generic content that should fail: "Discuss the key concepts", "Reflect on what you learned", "Apply the framework to a scenario". Examples that pass: "Analyze this customer complaint using the HEARD framework", "Compare waterfall and agile sprint planning for a 3-person team".

This is a blocking failure — generic content cannot be auto-fixed (requires regeneration).

**Check 7: Doctrine Compliance**
Verify each module spec has all structurally enforced pedagogy fields populated with substantive content (not empty or placeholder):
- `social_learning` has all 4 sub-fields with content-specific text
- `belief_challenging_encounter` names a specific belief and describes a mechanism
- `modality_switches` has at least 1 entry with named activities

This is a blocking failure — missing pedagogy fields cannot be auto-fixed.

**For metaskills.md (Stage 6) — add these checks:**

**Check 5: Outcome Drift**
Same pattern as modules.md — verify outcome IDs in the metaskill map reference real outcomes from the registry with matching wording.

This is a blocking failure — auto-fix by replacing with registry wording.

**Check 6: Generic Content Detection**
For each metaskill entry's `activation_activity` and `thinking_routine`: verify the content names specific skills, scenarios, or domain concepts from the curriculum. A thinking routine that could apply to any program without modification fails.

This is a blocking failure — cannot be auto-fixed.

Update the auto-fix logic in the Audit Result section of each file: the existing auto-fix for vocabulary violations and missing defaults still applies. Add: "Outcome drift: auto-fix by replacing draft wording with registry canonical wording." Generic content and doctrine compliance failures cannot be auto-fixed — they require regeneration.

Update the check count references: "these {N} checks" should reflect the new total (7 for modules.md, 6 for metaskills.md).
  </action>
  <verify>
    <automated>grep -c "Outcome Drift\|Generic Content\|Doctrine Compliance" .claude/plugins/curriculum/commands/modules.md | xargs test 3 -le && grep -c "Outcome Drift\|Generic Content" .claude/plugins/curriculum/commands/metaskills.md | xargs test 2 -le && echo "PASS"</automated>
  </verify>
  <done>modules.md draft-audit has 7 checks (original 4 + outcome drift + generic content + doctrine compliance). metaskills.md draft-audit has 6 checks (original 4 + outcome drift + generic content). Each check has blocking/auto-fix classification. Check count references updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add integrity checks and goal-backward verification to sessions.md draft-audit</name>
  <files>.claude/plugins/curriculum/commands/sessions.md</files>
  <action>
Read sessions.md. Add new integrity checks after Check 4 (Schema Compliance) and before the Verification Integrity section (added by Plan 01) in the Draft Audit section.

**Check 5: Outcome Drift**
Same pattern as modules.md — for each outcome ID or module ID referenced in draft session files, compare wording against `curriculum-registry.json`. Flag mismatches. Blocking failure, auto-fixable by replacing with registry wording.

**Check 6: Missing Formative Assessment**
Read the module specs from `workspace/{project-name}/03-modules/` (or 04-modules/ for new scheme). For each module, verify that the sessions for that module include at least one check-in assessment activity (a point where the facilitator can gauge whether learners are keeping up). A module with only lecture/discussion and a final deliverable but no mid-module check-in fails.

Look for evidence of formative assessment in session files: practice exercises with feedback loops, knowledge checks, peer review activities, guided practice with instructor observation. A session that only has "discuss" and "present final project" without intermediate checkpoints fails.

This is a blocking failure — cannot be auto-fixed (requires adding assessment activities to sessions).

**Check 7: Pre-work Gaps**
For each session that references pre-work (reading, video, preparation activity): verify the pre-work content is specified — not just "complete the pre-work" but naming what the learner should read/watch/do. For sessions after the first in a module: verify the session references or builds on what came before (the session should not be self-contained in a way that makes sequencing irrelevant).

This is a blocking failure — cannot be auto-fixed.

**Check 8: Goal-Backward Verification**
This is the exists/substantive/wired check from INTG-03. For EACH module, run these three sub-checks against its sessions:

**8a — Exists:** All expected session files were created (this overlaps with Check 1 but is stated explicitly for the goal-backward framework). If any session directory is missing any of the 4 required files (session.md, facilitator-guide.md, participant-materials.md, slide-outline.md), this sub-check fails.

**8b — Substantive:** Read each module's goal/description from the module spec (`03-modules/M-N/module-spec.md`). Then read the session files for that module. Verify that session activities, examples, and discussion prompts contain topic-specific nouns from the module's content domain. A session where you could swap in any other module's name and the content would still make sense is NOT substantive. Specifically:
- Extract 3-5 key domain terms from the module spec (e.g., for a module on "Customer Service Recovery": "HEARD framework", "service failure", "customer complaint", "recovery protocol")
- Verify at least 2 of these domain terms appear in each session's activities or examples
- Flag sessions where activities are generic: "Discuss the key concepts" fails; "Role-play a customer complaint using the HEARD framework's five steps" passes

**8c — Wired:** Verify pedagogical flow within each module's sessions:
- Session 1 sets up foundational concepts referenced by later sessions
- Sessions after the first reference or build on specific content from previous sessions (not just "building on what we learned")
- If pre-work is assigned, at least one session activity explicitly uses it (not just "having done the pre-work")
- A module where sessions could be rearranged in any order without loss of coherence fails the wired check

Report goal-backward results per module: "Module M-1 'Customer Service Recovery': Exists YES, Substantive YES, Wired YES" or "Module M-2 'Team Communication': Exists YES, Substantive NO (session M-2-S-2 activities are generic — no domain terms found), Wired NO (sessions are independent — no cross-session references)".

Goal-backward failures on Substantive or Wired are blocking — they cannot be auto-fixed because they require content regeneration.

Update the auto-fix logic in the Audit Result section: outcome drift is auto-fixable. Missing formative assessment, pre-work gaps, and goal-backward failures on substantive/wired require regeneration — report the specific module and session, and instruct the user to run `/curriculum:sessions` again.

Update the check count reference to reflect 8 checks total.
  </action>
  <verify>
    <automated>grep -c "Outcome Drift\|Missing Formative\|Pre-work Gaps\|Goal-Backward Verification" .claude/plugins/curriculum/commands/sessions.md | xargs test 4 -le && echo "PASS"</automated>
  </verify>
  <done>sessions.md draft-audit has 8 checks (original 4 + outcome drift + missing formative assessment + pre-work gaps + goal-backward verification with exists/substantive/wired sub-checks). Goal-backward reports per-module results. Blocking failures on content-level problems require regeneration, not auto-fix. Check count references updated.</done>
</task>

</tasks>

<verification>
modules.md has 7 named checks in its Draft Audit section. sessions.md has 8 named checks including Goal-Backward Verification with 3 sub-checks. metaskills.md has 6 named checks. Each new check has an explicit blocking/auto-fix classification. No check uses language from the prohibited-qualifier list when defining its pass/fail criteria.
</verification>

<success_criteria>
The draft-then-audit mechanism catches content-level failures (outcome drift, generic content, missing pedagogy, pre-work gaps) at every content generation stage. Session generation specifically verifies each session achieves its module goal through exists/substantive/wired checks. Failure modes are named specifically in reports, not summarized vaguely.
</success_criteria>

<output>
After completion, create `.planning/phases/20-integrity-verification/20-02-SUMMARY.md`
</output>
