---
phase: 23-pipeline-recovery-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/commands/validate.md
  - .claude/plugins/curriculum/commands/resume.md
  - templates/project-scaffold/STATE.md
  - dashboard/src/test/fixtures/STATE.md
autonomous: true
requirements: [PIPE-05, MC-02, MC-03, MC-05]
gap_closure: true

must_haves:
  truths:
    - "validate.md writes 'Run /curriculum:metaskills' as the next action after validation passes"
    - "resume.md routing table shows real command names for all stages 2-8 with no placeholder text"
    - "Template STATE.md references /curriculum:intake and /curriculum:resume — no dead /knz-* names"
    - "Dashboard fixture STATE.md references /curriculum:modules and /curriculum:resume — no dead /knz-* names"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/validate.md"
      provides: "Correct next-action routing after validation"
      contains: "Run /curriculum:metaskills"
    - path: ".claude/plugins/curriculum/commands/resume.md"
      provides: "Complete routing table for all pipeline stages"
      contains: "/curriculum:outcomes"
    - path: "templates/project-scaffold/STATE.md"
      provides: "Correct initial command references for new projects"
      contains: "/curriculum:intake"
    - path: "dashboard/src/test/fixtures/STATE.md"
      provides: "Correct command references in test fixture"
      contains: "/curriculum:resume"
  key_links:
    - from: ".claude/plugins/curriculum/commands/validate.md"
      to: ".claude/plugins/curriculum/commands/metaskills.md"
      via: "STATE.md next-action write directing user to metaskills after validation"
      pattern: "curriculum:metaskills"
    - from: ".claude/plugins/curriculum/commands/resume.md"
      to: "all stage commands"
      via: "routing table mapping pipeline state to command names"
      pattern: "curriculum:(outcomes|assessments|modules|sessions|metaskills|transfer|marketing)"
---

<objective>
Fix three pipeline recovery bugs: wrong next-action after validation, placeholder text in resume routing, and dead command names in template files.

Purpose: Users who clear context mid-pipeline and resume get routed to the correct next command instead of a dead end or skipped stage.
Output: Four files with corrected text — no logic changes, no new features.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix validate.md next-action and resume.md routing table</name>
  <files>.claude/plugins/curriculum/commands/validate.md, .claude/plugins/curriculum/commands/resume.md</files>
  <action>
In validate.md, find the State Update section (around line 225). Change:

```
Session Continuity -> Next Action: `Run /curriculum:approve to review human checklist and approve final package`
```

To:

```
Session Continuity -> Next Action: `Run /curriculum:metaskills to map thinking skills`
```

This is the "If file verification passed AND tier_1_failures == 0" branch only. The other branches (tier_1_failures > 0) should remain unchanged. Do NOT change the user-facing handoff around line 156 — it already correctly says /curriculum:metaskills.

In resume.md, find the routing table (around lines 99-113). Replace the placeholder rows and fallback template with real commands:

Replace:
```
| Stage 2 not-started or in-progress | Stage 2 (Outcome Design) — command available soon |
| Stage 3 not-started or in-progress | Stage 3 (Assessment Design) — command available soon |
```

With:
```
| Stage 2 not-started or in-progress | Run `/curriculum:outcomes` to continue |
| Stage 3 not-started or in-progress | Run `/curriculum:assessments` to continue |
```

Replace:
```
| Stages 4-8 not-started or in-progress | Stage {N} ({Name}) — command available in a future update |
```

With individual rows:
```
| Stage 4 not-started or in-progress | Run `/curriculum:modules` to continue |
| Stage 5 not-started or in-progress | Run `/curriculum:sessions` to continue |
| Stage 6 not-started or in-progress | Run `/curriculum:metaskills` to continue |
| Stage 7 not-started or in-progress | Run `/curriculum:transfer` to continue |
| Stage 8 not-started or in-progress | Run `/curriculum:marketing` to continue |
```

Remove the fallback template entirely:
```
For stages without commands yet, show:
> Stage {N} ({Name}) — command available in a future update. Run `/curriculum:resume` to check back.
```

Delete those two lines completely — all stages now have commands.
  </action>
  <verify>
    <automated>grep -n "curriculum:approve" .claude/plugins/curriculum/commands/validate.md | grep -i "next action" && echo "FAIL: validate.md still references approve as next action" && exit 1; grep -c "available soon\|future update" .claude/plugins/curriculum/commands/resume.md | grep -q "^0$" && echo "PASS: no placeholder text in resume.md" || (echo "FAIL: placeholder text remains in resume.md" && exit 1); grep -c "curriculum:outcomes\|curriculum:assessments\|curriculum:modules\|curriculum:sessions\|curriculum:metaskills\|curriculum:transfer\|curriculum:marketing" .claude/plugins/curriculum/commands/resume.md | grep -qv "^0$" && echo "PASS: real command names present" || (echo "FAIL: missing command names" && exit 1)</automated>
  </verify>
  <done>validate.md writes "Run /curriculum:metaskills" as next action after passing validation. resume.md routing table has real command names for all stages 2-8 with zero placeholder text remaining.</done>
</task>

<task type="auto">
  <name>Task 2: Fix dead command names in template and fixture STATE.md files</name>
  <files>templates/project-scaffold/STATE.md, dashboard/src/test/fixtures/STATE.md</files>
  <action>
In templates/project-scaffold/STATE.md (around lines 48-49), replace:

```
- **Next Action:** Run /knz-intake to begin structured intake
- **Resume Command:** /knz-resume
```

With:

```
- **Next Action:** Run /curriculum:intake to begin structured intake
- **Resume Command:** /curriculum:resume
```

In dashboard/src/test/fixtures/STATE.md (around lines 43-44), replace:

```
- **Next Action:** Run /knz-modules to build module structure
- **Resume Command:** /knz-resume
```

With:

```
- **Next Action:** Run /curriculum:modules to build module structure
- **Resume Command:** /curriculum:resume
```
  </action>
  <verify>
    <automated>grep -rn "knz-" templates/project-scaffold/STATE.md dashboard/src/test/fixtures/STATE.md && echo "FAIL: dead /knz-* command names remain" && exit 1 || echo "PASS: no dead command names"</automated>
  </verify>
  <done>Both STATE.md files reference /curriculum:* commands exclusively. Zero /knz-* dead command names remain in either file.</done>
</task>

</tasks>

<verification>
All three bugs verified fixed:
1. `grep "curriculum:metaskills" .claude/plugins/curriculum/commands/validate.md` — appears in State Update next-action line
2. `grep -c "available soon\|future update" .claude/plugins/curriculum/commands/resume.md` — returns 0
3. `grep -rn "knz-" templates/project-scaffold/STATE.md dashboard/src/test/fixtures/STATE.md` — returns nothing
</verification>

<success_criteria>
- validate.md State Update writes "Run /curriculum:metaskills" not "Run /curriculum:approve" after validation passes
- resume.md routing table has 7 real stage command entries (stages 2-8) and zero placeholder lines
- templates/project-scaffold/STATE.md uses /curriculum:intake and /curriculum:resume
- dashboard/src/test/fixtures/STATE.md uses /curriculum:modules and /curriculum:resume
</success_criteria>

<output>
After completion, create `.planning/phases/23-pipeline-recovery-fixes/23-01-SUMMARY.md`
</output>
