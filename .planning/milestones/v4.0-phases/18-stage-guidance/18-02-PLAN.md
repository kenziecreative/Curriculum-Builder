---
phase: 18-stage-guidance
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/commands/validate.md
  - .claude/plugins/curriculum/commands/verify.md
autonomous: true
requirements: [GUIDE-02]

must_haves:
  truths:
    - "Every constraint check translation includes both what was checked AND why it matters in one sentence"
    - "Passing checks are shown as a count, not individually listed"
    - "Failing checks show the what+why sentence so the SME understands the significance"
    - "verify.md check descriptions explain why each issue matters, not just what was found"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/validate.md"
      provides: "Extended translation table with why-it-matters clauses"
      contains: "why it matters"
    - path: ".claude/plugins/curriculum/commands/verify.md"
      provides: "Plain-language check descriptions with significance"
      contains: "why it matters"
  key_links:
    - from: "validate.md translation table"
      to: "validate.md Conversation Output"
      via: "translation table used when presenting failure descriptions"
      pattern: "Translate check IDs to plain language"
    - from: "verify.md check descriptions"
      to: "verify.md Step 3 results"
      via: "descriptions shown inline with each issue"
      pattern: "description"
---

<objective>
Extend constraint enforcement results with "what + why" explanations so SMEs understand not just what failed but why it matters — without needing instructional design training.

Purpose: Current translation table in validate.md says what was checked ("the objective is missing its assessment link") but not why it matters. An SME reading this doesn't know if it's a minor paperwork issue or a fundamental quality problem. Adding "why" turns each check from a cryptic finding into an actionable insight.

Output: Updated validate.md with "why it matters" clauses in the translation table; updated verify.md with significance explanations for each check type.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-stage-guidance/18-CONTEXT.md

@.claude/plugins/curriculum/commands/validate.md
@.claude/plugins/curriculum/commands/verify.md
@.claude/reference/curriculum-voice.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend validate.md translation table with "why it matters" clauses</name>
  <files>.claude/plugins/curriculum/commands/validate.md</files>
  <action>
Two changes to validate.md:

**Change 1 — Extend the translation table (lines 152-171) with a "Why it matters" column:**

The current table has columns: Check ID | Plain language. Add a third column: Why it matters. Each "why" should be one sentence explaining the practical consequence for the SME in plain language. The "what + why" pattern from CONTEXT.md: "Every learning objective has at least one way to prove the learner can actually do it — otherwise you're teaching things you can't measure."

Rewrite the table to this structure:

| Check ID | What was checked | Why it matters |
|----------|-----------------|----------------|
| T1-01 | the thinking level field is empty or invalid | without this, the tool can't verify your assessments match the difficulty of what you're teaching |
| T1-02 | an objective is missing its assessment link | you're teaching something with no way to prove the learner can actually do it |
| T1-03 | the real-world application context is missing | learners won't see how this connects to their actual work |
| T1-04 | the prerequisite knowledge field is missing | facilitators won't know what learners need before starting |
| T1-05 | the program doesn't cover enough thinking levels | all your objectives ask learners to do the same kind of thinking — the program needs variety from understanding to application |
| T1-06 | an assessment is missing its objective link | you have an assessment that doesn't prove any specific learning objective |
| T1-07 | the assessment is easier than the objective it covers | you're testing at a lower difficulty than what you're teaching — learners could pass without actually meeting the objective |
| T1-08 | a module is missing a check-in assessment | learners won't find out they're lost until the final assessment — too late to course-correct |
| T1-09 | the program is missing a final assessment | there's no way to prove learners achieved the program's goals |
| T1-10 | an open-skill program needs at least one performance-based assessment | you can't assess a hands-on skill with a written test alone |
| T1-11 | a module is missing its group activity structure | learners practice solo but never with others — collaborative skills need collaborative practice |
| T1-12 | the group activity type is not a recognized value | the group activity format doesn't match known effective structures |
| T1-13 | the group interdependence description is missing or too generic | the group activity doesn't require real collaboration — participants could complete it alone |
| T1-14 | a module is missing its belief-challenging encounter | learners never have their assumptions tested — they leave with the same mental models they arrived with |
| T1-15 | the module sequence has a circular dependency | modules reference each other in a loop — the sequence can't be taught in order |
| T1-16 | a session is missing one or more required content sections | the session plan is incomplete — a facilitator couldn't deliver it as written |
| T1-17 | the reflection question is too generic | the reflection prompt could apply to any session — it doesn't ask about anything specific from this one |
| T1-18 | the transfer activity doesn't connect to the real-work context | the practice activity is abstract — learners won't see how to use this on the job |

Claude: Write the exact "why it matters" phrases using your judgment — the examples above are directional, not verbatim mandates. Each "why" must be one sentence, plain language, no ID jargon, and pass curriculum-voice.md vocabulary rules.

**Change 2 — Update the failure presentation instruction (around line 145-148):**

Currently failures are shown as:
```
- {plain-language description of what's wrong}. Location: {stage file path}
```

Update the instruction to combine what + why:
```
- {what was checked} — {why it matters}. Location: {stage file path}
```

The "what" and "why" come from the updated translation table. Each failure line is one sentence combining both columns with an em dash separator.

Also update the instruction around line 150 ("Translate check IDs to plain language") to reference both columns: "Translate check IDs using both columns — combine the 'what' and 'why' with an em dash."

Passing checks remain as a count per existing behavior — do not change that.
  </action>
  <verify>
    <automated>grep -c "Why it matters" .claude/plugins/curriculum/commands/validate.md | grep -q "^[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>validate.md translation table has "Why it matters" column for all 18 checks, failure presentation combines what+why in one line, passing checks still shown as count</done>
</task>

<task type="auto">
  <name>Task 2: Add significance explanations to verify.md check descriptions</name>
  <files>.claude/plugins/curriculum/commands/verify.md</files>
  <action>
Update verify.md check descriptions to include "why it matters" for each check type, matching the "what + why" pattern.

**Change 1 — Check A (Missing required stage files):**
The current "Missing description" column just says "Not yet generated" for all entries. Add a "Why it matters" note to the issue description instruction. When a missing file is reported, the description should include significance:

Update the instruction text (around the Check A table) to say: When adding an issue for a missing file, use this description format: "{stage name} content not yet generated — {why it matters}".

For each stage, the "why" is:
- 01-outcomes: "learners won't have defined learning goals"
- 02-assessments: "there's no way to prove learners achieved the objectives"
- 03-modules: "the program has no structure connecting objectives to sessions"
- 04-sessions: "facilitators have nothing to deliver"
- 06-transfer: "learners have no support for applying skills on the job"
- 07-marketing: "there's no way to communicate what the program offers"

**Change 2 — Check B descriptions:**
Update the three scan descriptions (B1, B2, B3) to include significance:

- B1 (NEEDS: markers): Change from "has unresolved content markers" to "has unfinished content — sections marked for completion weren't filled in, so a facilitator couldn't deliver this session as written"
- B2 (TMA phase labels): Change from "contains formatting labels that should not appear in facilitator guides" to "contains internal structural labels that would confuse a facilitator — these are tool scaffolding, not delivery content"
- B3 (HTML comments): Change from "contains internal notes that should not appear in output" to "contains hidden notes meant for the tool, not the facilitator — these need to be removed before delivery"

**Change 3 — Results presentation (Step 3):**
The current format shows: `{relative file path} — {description}`. Keep this format. The updated descriptions from Changes 1-2 now carry the "why it matters" naturally.

All new text must pass curriculum-voice.md vocabulary rules.
  </action>
  <verify>
    <automated>grep -c "facilitator\|learner" .claude/plugins/curriculum/commands/verify.md | grep -q "^[3-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>verify.md check descriptions include "why it matters" for all check types — missing files explain the practical consequence, content issues explain why they'd cause problems for facilitators or learners</done>
</task>

</tasks>

<verification>
- validate.md translation table has 3 columns (Check ID, What was checked, Why it matters) with all 18 rows populated
- validate.md failure presentation instruction references both what+why columns
- verify.md Check A descriptions include significance explanations
- verify.md Check B descriptions include significance explanations
- No prohibited vocabulary terms from curriculum-voice.md in any new text
</verification>

<success_criteria>
- Every constraint check has both "what was checked" and "why it matters" in one actionable sentence
- Passing checks shown as count (unchanged)
- Failures shown with combined what+why so SME understands significance without ID training
- All new text passes curriculum-voice.md vocabulary rules
</success_criteria>

<output>
After completion, create `.planning/phases/18-stage-guidance/18-02-SUMMARY.md`
</output>
