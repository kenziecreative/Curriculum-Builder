---
phase: 18-stage-guidance
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/commands/intake.md
autonomous: true
requirements: [GUIDE-01]
gap_closure: true

must_haves:
  truths:
    - "The intake review gate shows 3-4 plain-language self-check questions the SME asks themselves before approving the program brief"
    - "Questions help the SME verify the captured program brief is accurate — a 'did we get this right?' register, not a learning design evaluation"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/intake.md"
      provides: "Self-check questions at the post-intake review gate"
      contains: "Self-check"
  key_links:
    - from: "intake.md self-check block"
      to: "AskUserQuestion review gate"
      via: "Self-check questions appear after 'Your Program at a Glance' summary, before AskUserQuestion call"
      pattern: "Self-check.*before you decide"
---

<objective>
Close the one remaining GUIDE-01 gap: add self-check questions to the intake review gate in intake.md.

Purpose: The post-intake gate currently fires AskUserQuestion immediately after the "Your Program at a Glance" summary with no evaluation prompts. The ROADMAP success criterion 1 names the intake gate explicitly. The other three gates (approve.md post-assessment, approve.md final validation, assessments.md PIPE-05) already have self-check blocks — this is the missing fourth.

Output: intake.md with a self-check block matching the established pattern from approve.md and assessments.md, but in intake-appropriate register ("did we capture your program correctly?" not "is the learning design sound?").
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-stage-guidance/18-CONTEXT.md
@.planning/phases/18-stage-guidance/18-VERIFICATION.md

<interfaces>
<!-- Established self-check pattern from approve.md (lines 111-117, 178-184) and assessments.md (lines 202-209). -->
<!-- The intake gate must follow this same structural pattern but with intake-appropriate questions. -->

From approve.md (post-assessment gate):
```markdown
### Self-check — before you decide

Ask yourself:
- Does the final assessment match what you'd expect someone to actually do on the job?
- If a learner passed every assessment, would you trust they can do this work?
- Are there any skills you care about that don't have a way to prove the learner can do them?
- Do the check-in assessments catch problems early enough, or would a learner reach the end before finding out they're lost?
```

From assessments.md (PIPE-05 gate):
```markdown
> ### Before you decide
>
> - Could a new hire follow these instructions on day one?
> - If someone passed all these assessments, would you trust they can do this work unsupervised?
> - Are there any critical skills missing — things you'd want proof of before letting someone loose?
> - Do the check-in assessments catch problems early enough, or would a learner reach the end before finding out they're lost?
```

Intake gate insertion point (intake.md lines 246-265):
```markdown
**Your Program at a Glance**

- **Topic:** {program_topic}
- **Learners:** {target_audience.description}
- **Current skill level:** {prior_knowledge in plain language}
- **Program format:** {contact_hours} hours across {session_count} sessions — {modality}, {delivery_mode}
- **Where they'll use it:** {transfer_context}
- **Success looks like:** {success_criteria}

---

### AskUserQuestion — review gate

Use `AskUserQuestion` with these three options:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add self-check questions to intake review gate</name>
  <files>.claude/plugins/curriculum/commands/intake.md</files>
  <action>
Insert a self-check block between the "Your Program at a Glance" summary section (after the `---` at line 255) and the `### AskUserQuestion — review gate` heading (line 257) in intake.md.

Use the `### Self-check — before you decide` heading with `Ask yourself:` lead-in to match the approve.md pattern (not the blockquote format from assessments.md — approve.md is the closer parallel since this is a standalone gate, not an inline gate within a larger generation step).

Write 3-4 questions in the intake register — these ask "did we capture your program correctly?" not "is the learning design sound?" The questions should help the SME verify:
1. Whether the audience description matches who they actually have in mind
2. Whether the time/format constraints are realistic for what they want to teach
3. Whether the success criteria capture what they actually care about
4. Whether anything important was missed in the conversation

Follow the sharp-colleague tone from 18-CONTEXT.md: questions an experienced peer would ask before signing off on a brief, not a compliance checklist. Each question targets one quality signal.

Do NOT use any ID vocabulary — check against the never-say table in curriculum-voice.md. The Writing for Clarity block already exists in intake.md and covers new text automatically.
  </action>
  <verify>
    <automated>grep -c "Self-check" .claude/plugins/curriculum/commands/intake.md | grep -q "1" && grep -A 6 "Self-check" .claude/plugins/curriculum/commands/intake.md | grep -c "^-" | grep -qE "[3-4]" && echo "PASS: Self-check block with 3-4 questions found" || echo "FAIL: Missing or malformed self-check block"</automated>
  </verify>
  <done>intake.md has a self-check block with 3-4 questions between the "Your Program at a Glance" summary and the AskUserQuestion call. Questions are in intake register (program brief accuracy, not learning design evaluation). No ID vocabulary.</done>
</task>

</tasks>

<verification>
1. `grep -n "Self-check" .claude/plugins/curriculum/commands/intake.md` shows exactly one self-check heading
2. The self-check block appears AFTER line 255 (the summary section divider) and BEFORE the AskUserQuestion heading
3. Questions use intake register — none reference assessments, learning design, or curriculum structure
4. No never-say vocabulary violations in the added text
</verification>

<success_criteria>
All four review gates (intake, assessment PIPE-05, approve post-assessment, approve final validation) now have self-check questions, completing GUIDE-01 coverage. The intake gate questions are in the correct register for a program brief review.
</success_criteria>

<output>
After completion, create `.planning/phases/18-stage-guidance/18-04-SUMMARY.md`
</output>
