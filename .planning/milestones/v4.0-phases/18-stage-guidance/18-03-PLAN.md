---
phase: 18-stage-guidance
plan: "03"
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - .claude/plugins/curriculum/commands/init.md
  - templates/project-scaffold/.gitkeep-dirs
  - templates/project-scaffold/STATE.md
  - .claude/plugins/curriculum/commands/approve.md
  - .claude/plugins/curriculum/commands/assemble.md
  - .claude/plugins/curriculum/commands/assessments.md
  - .claude/plugins/curriculum/commands/evaluation-mode.md
  - .claude/plugins/curriculum/commands/intake.md
  - .claude/plugins/curriculum/commands/marketing.md
  - .claude/plugins/curriculum/commands/metaskills.md
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/outcomes.md
  - .claude/plugins/curriculum/commands/resume.md
  - .claude/plugins/curriculum/commands/sessions.md
  - .claude/plugins/curriculum/commands/transfer.md
  - .claude/plugins/curriculum/commands/validate.md
  - .claude/plugins/curriculum/commands/verify.md
autonomous: true
requirements: [DEVL-03]

must_haves:
  truths:
    - "New projects created by init.md get directories numbered 01-09 (not 00-08)"
    - "Existing projects with 00-08 directories continue to work without modification"
    - "Every command auto-detects which numbering scheme exists and uses the correct paths"
    - "Stage numbers visible in STATE.md tables match the directory numbering for new projects"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/init.md"
      provides: "Creates 01-09 directories for new projects"
      contains: "01-project-brief"
    - path: "templates/project-scaffold/.gitkeep-dirs"
      provides: "Updated scaffold reference with 01-09 numbering"
      contains: "01-project-brief"
    - path: "templates/project-scaffold/STATE.md"
      provides: "STATE.md template unchanged — stage numbers already 1-9"
  key_links:
    - from: "init.md directory creation"
      to: "all command directory references"
      via: "auto-detection checks for 00- vs 01- prefix to determine scheme"
      pattern: "auto-detect|00-project-brief.*01-project-brief"
    - from: "each command's directory path resolution"
      to: "workspace stage directories"
      via: "commands check which numbering scheme exists before building paths"
      pattern: "00-project-brief|01-project-brief"
---

<objective>
Normalize stage numbering so new projects use 01-09 directories (matching stage numbers 1-9) while existing projects with 00-08 directories continue working. Every command auto-detects which scheme is present.

Purpose: Current directories are numbered 00-08 but stages are numbered 1-9 in STATE.md and all user-visible output. An SME reading "Stage 1: Intake" but seeing directory "00-project-brief/" faces an unexplained mismatch. New projects fix this; existing projects are unaffected.

Output: Updated init.md and scaffold template for 01-09 numbering; all 15 commands updated with auto-detection logic; STATE.md template unchanged (already uses 1-9).
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-stage-guidance/18-CONTEXT.md

@.claude/plugins/curriculum/commands/init.md
@templates/project-scaffold/.gitkeep-dirs
@templates/project-scaffold/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update init.md and scaffold template for 01-09 directory numbering</name>
  <files>.claude/plugins/curriculum/commands/init.md, templates/project-scaffold/.gitkeep-dirs</files>
  <action>
**Change 1 — Update init.md directory creation (Section 3, around line 43-57):**

Replace the directory list:
```
  00-project-brief/  ->  01-project-brief/
  01-outcomes/        ->  02-outcomes/
  02-assessments/     ->  03-assessments/
  03-modules/         ->  04-modules/
  04-sessions/        ->  05-sessions/
  05-metaskills/      ->  06-metaskills/
  06-transfer/        ->  07-transfer/
  07-marketing/       ->  08-marketing/
  08-validation/      ->  09-validation/
  source-material/    ->  source-material/  (unchanged)
  delivery/           ->  delivery/          (unchanged)
```

Keep the same comment about no placeholder files.

**Change 2 — Update templates/project-scaffold/.gitkeep-dirs:**

Update the directory listing and the Directory Purpose table to use 01-09 numbering. Keep all other content (descriptions, notes) unchanged. Update the notes section to clarify:
- "New projects use 01-09 directory numbering matching stage numbers 1-9"
- "Existing projects may use legacy 00-08 numbering — all commands auto-detect which scheme is present"

**Change 3 — Confirm STATE.md template needs no changes:**

Read templates/project-scaffold/STATE.md. The stage table already uses Stage 1-9 numbering. No change needed. The directory numbering now matches the stage numbering for new projects.
  </action>
  <verify>
    <automated>grep -c "01-project-brief" .claude/plugins/curriculum/commands/init.md templates/project-scaffold/.gitkeep-dirs | grep -v ":0$" | wc -l | grep -q "^2$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>init.md creates 01-09 directories for new projects, scaffold template documents 01-09 numbering, STATE.md template confirmed unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add auto-detection to all commands for 00-08 vs 01-09 directory paths</name>
  <files>.claude/plugins/curriculum/commands/approve.md, .claude/plugins/curriculum/commands/assemble.md, .claude/plugins/curriculum/commands/assessments.md, .claude/plugins/curriculum/commands/evaluation-mode.md, .claude/plugins/curriculum/commands/intake.md, .claude/plugins/curriculum/commands/marketing.md, .claude/plugins/curriculum/commands/metaskills.md, .claude/plugins/curriculum/commands/modules.md, .claude/plugins/curriculum/commands/outcomes.md, .claude/plugins/curriculum/commands/resume.md, .claude/plugins/curriculum/commands/sessions.md, .claude/plugins/curriculum/commands/transfer.md, .claude/plugins/curriculum/commands/validate.md, .claude/plugins/curriculum/commands/verify.md</files>
  <action>
Add a directory auto-detection instruction to every command file that references stage directories. This is a mechanical update — the same pattern applied 14 times (init.md was handled in Task 1).

**The auto-detection instruction block:**

Add this block after the "Find active project" section in each command (the section where `{project}` is determined). If a command doesn't have an explicit "Find active project" section, add it after the Prerequisites section or at the top of the Behavior section — wherever the command first resolves `{project}`.

```markdown
### Directory scheme detection

After identifying `{project}`, detect which directory numbering scheme this workspace uses:
- If `workspace/{project}/00-project-brief/` exists → use legacy scheme (00-08)
- If `workspace/{project}/01-project-brief/` exists → use new scheme (01-09)

Use this mapping for all subsequent directory references in this command:

| Stage | Legacy (00-08) | New (01-09) |
|-------|---------------|-------------|
| 1 - Intake | 00-project-brief | 01-project-brief |
| 2 - Outcomes | 01-outcomes | 02-outcomes |
| 3 - Assessments | 02-assessments | 03-assessments |
| 4 - Modules | 03-modules | 04-modules |
| 5 - Sessions | 04-sessions | 05-sessions |
| 6 - Metaskills | 05-metaskills | 06-metaskills |
| 7 - Transfer | 06-transfer | 07-transfer |
| 8 - Marketing | 07-marketing | 08-marketing |
| 9 - Validation | 08-validation | 09-validation |
```

**IMPORTANT:** Do NOT rewrite every directory reference in the file body. The detection block tells Claude which scheme to use. The existing directory references (e.g., `02-assessments/`) serve as the default (legacy) pattern. When Claude detects the new scheme, it maps `02-assessments/` to `03-assessments/` using the table above.

**Per-command changes:**

For each of the 14 command files, add the auto-detection block in the appropriate location:

1. **approve.md** — after "### 1. Find active project" (line 45-52)
2. **assemble.md** — after its workspace detection section
3. **assessments.md** — after "### 1. Check workspace exists" (line 12-17)
4. **evaluation-mode.md** — after its workspace detection section
5. **intake.md** — after its workspace detection section
6. **marketing.md** — after its workspace/prerequisites section
7. **metaskills.md** — after its workspace/prerequisites section
8. **modules.md** — after its workspace detection section
9. **outcomes.md** — after its workspace detection section
10. **resume.md** — after its workspace detection section
11. **sessions.md** — after its workspace detection section
12. **transfer.md** — after its workspace/prerequisites section
13. **validate.md** — after "### 1. Check workspace exists" (line 23-29)
14. **verify.md** — after "## Step 1 — Find active project" (line 27-34)

Read each file, find the correct insertion point, and add the detection block. The block content is identical across all 14 files.

Do NOT modify any other content in these files. Only add the detection block.
  </action>
  <verify>
    <automated>count=0; for f in .claude/plugins/curriculum/commands/approve.md .claude/plugins/curriculum/commands/assemble.md .claude/plugins/curriculum/commands/assessments.md .claude/plugins/curriculum/commands/evaluation-mode.md .claude/plugins/curriculum/commands/intake.md .claude/plugins/curriculum/commands/marketing.md .claude/plugins/curriculum/commands/metaskills.md .claude/plugins/curriculum/commands/modules.md .claude/plugins/curriculum/commands/outcomes.md .claude/plugins/curriculum/commands/resume.md .claude/plugins/curriculum/commands/sessions.md .claude/plugins/curriculum/commands/transfer.md .claude/plugins/curriculum/commands/validate.md .claude/plugins/curriculum/commands/verify.md; do grep -q "Directory scheme detection" "$f" && count=$((count+1)); done; [ $count -eq 14 ] && echo "PASS: all 14 commands have detection block" || echo "FAIL: only $count/14 commands have detection block"</automated>
  </verify>
  <done>All 14 command files have the directory auto-detection block, enabling them to work with both legacy (00-08) and new (01-09) workspace numbering schemes without requiring changes to existing workspace directories</done>
</task>

</tasks>

<verification>
- init.md creates 01-09 directories (grep for "01-project-brief")
- templates/project-scaffold/.gitkeep-dirs documents 01-09 numbering
- All 14 command files contain "Directory scheme detection" section
- templates/project-scaffold/STATE.md unchanged (already uses 1-9 stage numbers)
- No command has lost its existing directory references (auto-detection is additive, not replacement)
</verification>

<success_criteria>
- New projects get 01-09 directories matching stage 1-9 numbering
- Existing 00-08 workspaces continue working with all commands
- Every command auto-detects which scheme is present
- No user-visible numbering mismatch for new projects
</success_criteria>

<output>
After completion, create `.planning/phases/18-stage-guidance/18-03-SUMMARY.md`
</output>
