---
phase: 21-deviation-validation-coverage
plan: "03"
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - .claude/plugins/curriculum/agents/knz-validator.md
autonomous: true
requirements: [DEVL-02]

must_haves:
  truths:
    - "The validator runs T1-19 through T1-33 against actual stage output instead of writing 'Not applicable'"
    - "A curriculum that passes validation has been checked end-to-end through all 8 stages"
    - "Stages 6-8 checks produce specific named failure messages when they fail, not generic summaries"
  artifacts:
    - path: ".claude/plugins/curriculum/agents/knz-validator.md"
      provides: "Full T1-01 through T1-33 validation coverage"
      contains: "T1-19"
  key_links:
    - from: ".claude/plugins/curriculum/agents/knz-validator.md"
      to: ".claude/reference/schemas/stage-09-validation.md"
      via: "check definitions for T1-19 through T1-33"
      pattern: "stage-09-validation"
    - from: ".claude/plugins/curriculum/agents/knz-validator.md"
      to: "workspace stages 06-08"
      via: "stage reading for metaskills, transfer, marketing files"
      pattern: "metaskill-map|transfer-ecosystem|marketing-package"
---

<objective>
Expand the validator agent to run T1-19 through T1-33 against actual stage output, completing end-to-end validation coverage through all 8 content stages.

Purpose: Currently the validator stubs T1-19 through T1-33 as "Not applicable — Stage N not yet generated." With stages 7-8 now having draft-audit pipelines (Plan 02), the validator can run all checks, making validation truly end-to-end.

Output: Updated knz-validator.md with full T1-01 through T1-33 coverage.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deviation-validation-coverage/21-CONTEXT.md
@.planning/phases/21-deviation-validation-coverage/21-02-SUMMARY.md

<interfaces>
<!-- From knz-validator.md Rule 2 (line ~57-63) — the stub to replace -->
```
### Rule 2 — Tier 1 Scope

Run T1-01 through T1-18 (Stages 2-5). For T1-19 through T1-33, write the following in the schema-report.md table for each check:

| T1-XX | [field_name] | stage-0N | — | Not applicable — Stage N not yet generated |
```

<!-- From stage-09-validation.md — the check definitions -->
Stage 06 checks (T1-19 to T1-24): metaskill activation, six-metaskill coverage, developability hierarchy, 30% cap, evidence gap
Stage 07 checks (T1-25 to T1-30): three transfer layers, implementation intentions, error management, spaced retrieval, evaluation level, community continuation
Stage 08 checks (T1-31 to T1-33): source citation, supporting element existence, marketing ratio

<!-- From knz-validator.md Rule 4 (line ~70-72) — Tier 3 scope stub to update -->
```
### Rule 4 — Tier 3 Scope (Phase 6)
Run T3-06, T3-07, and T3-08 only. Skip T3-01 through T3-05 (transfer ecosystem not yet generated) and T3-09 (marketing not yet generated).
```

<!-- Stage reading section (line ~119-128) — needs stages 5-7 added -->
Currently reads stages 1-4 only. Needs:
5. metaskills dir: metaskill-map.md
6. transfer dir: transfer-ecosystem.md
7. marketing dir: marketing-package.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand validator to run T1-19 through T1-33 and read stages 6-8</name>
  <files>.claude/plugins/curriculum/agents/knz-validator.md</files>
  <action>
Three changes to knz-validator.md:

**Change 1: Update Rule 2 — Tier 1 Scope**

Replace the current Rule 2 text with:

```
### Rule 2 — Tier 1 Scope

Run T1-01 through T1-33 (all stages). For any stage whose directory is missing or empty, write "Not applicable — Stage N not yet generated" for that stage's checks. Do not skip checks for stages that exist — run every applicable check.
```

**Change 2: Add stages 5-7 to Stage Reading section**

After the existing stage 4 reading (line ~126), add three more stage reads:

```
5. `{workspace_path}{metaskills-dir}/` — Read `metaskill-map.md`. Parse the YAML content to extract metaskill records with `metaskill_name`, `activation_activity`, `sequence_position`, and `evidence_gap_acknowledgment`. If directory missing or file missing → mark T1-19 through T1-24 as "Not applicable — Stage 6 not yet generated".

6. `{workspace_path}{transfer-dir}/` — Read `transfer-ecosystem.md`. This is a prose document, not YAML — parse sections by heading (Before the Program, During the Program, After the Program, How We'll Know It Worked). If directory missing or file missing → mark T1-25 through T1-30 as "Not applicable — Stage 7 not yet generated".

7. `{workspace_path}{marketing-dir}/` — Read `marketing-package.md`. Parse prose sections and the Source Traceability table. If directory missing or file missing → mark T1-31 through T1-33 as "Not applicable — Stage 8 not yet generated".
```

Use the same directory scheme detection already present in the file (legacy 00-08 vs new 01-09). The metaskills dir variable `{metaskills-dir}` should use the same detection: `05-metaskills` for legacy, `06-metaskills` for new. Similarly for transfer (`06-transfer`/`07-transfer`) and marketing (`07-marketing`/`08-marketing`).

**Change 3: Add T1-19 through T1-33 check implementations**

After the existing T1-18 implementation, add the check implementations. Follow the exact same structure as existing checks (read field, evaluate, write PASS/FAIL row with specific failure message).

**Stage 06 checks:**

T1-19: Read each metaskill record's `activation_activity`. Verify it names a specific thinking routine (not generic descriptors like "discussion", "reflection", "group activity", "brainstorm", "debrief"). FAIL message: `GENERIC ACTIVITY — '{activation_activity}' is not a named thinking routine`

T1-20: Collect all unique `metaskill_name` values across records. Verify all six metaskills appear (Exploring, Creating, Feeling, Imagining, Innovating, Adapting). For programs with `contact_hours` < 2: relax to minimum 3 of 6. FAIL message: `MISSING METASKILLS — {missing_names} not activated`

T1-21: Find the first record where `metaskill_name` is "Innovating". Verify its `sequence_position` is higher than at least one "Exploring" record AND at least one "Creating" record. FAIL message: `HIERARCHY VIOLATION — Innovating at position {N} but Exploring at {N} and Creating at {N}`

T1-22: Same check for "Adapting" — must come after at least one record from each of the other five metaskills (or at minimum after Exploring and Creating). FAIL message: `HIERARCHY VIOLATION — Adapting at position {N} before foundational skills`

T1-23: Count activations per metaskill. Verify no single metaskill exceeds 30% of total. Skip for programs with `contact_hours` < 2. FAIL message: `OVER-REPRESENTED — {metaskill_name} has {N}% of activations (max 30%)`

T1-24: For any record where `metaskill_name` is "Imagining", verify `evidence_gap_acknowledgment` is true. FAIL message: `MISSING EVIDENCE GAP — Imagining record at position {N} lacks evidence gap acknowledgment`

**Stage 07 checks:**

T1-25: Verify the transfer document contains all three sections: Before the Program, During the Program, After the Program. Each must have substantive content (not just headings). FAIL message: `MISSING TRANSFER LAYER — {section_name} section is missing or empty`

T1-26: Read the module list from stage 4 output. Verify the During the Program section references specific follow-through commitments for each module. FAIL message: `MISSING IMPLEMENTATION INTENTION — Module {M-N} has no follow-through reference in transfer design`

T1-27: Read `skill_type` from registry. If "open": verify During section mentions error correction practice. If "closed": auto-pass. FAIL message: `MISSING ERROR MANAGEMENT — open-skill program requires error correction practice`

T1-28: Read `contact_hours` from registry. Determine required intervals (< 2h: 1 week only; 2-16h: 1 week + 1 month; > 16h: all three). Verify After section describes the correct number of follow-up touchpoints. FAIL message: `SPACED RETRIEVAL MISMATCH — expected {N} intervals for {contact_hours}h program, found {M}`

T1-29: Determine minimum evaluation level from program type. Verify How We'll Know It Worked section describes measurement at or above minimum. FAIL message: `EVALUATION BELOW MINIMUM — program requires Level {N} minimum, found Level {M}`

T1-30: Verify After section includes community continuation content that is not placeholder. Scan for "TBD", "to be determined", "placeholder", or sections with only a heading and no content. FAIL message: `PLACEHOLDER COMMUNITY — community continuation is empty or marked TBD`

**Stage 08 checks:**

T1-31: Parse the Source Traceability table. Verify every row has a non-empty source reference. Also verify that every major claim in the prose sections (The Promise, Program Description, What Changes, Learning Promises) has a corresponding traceability entry. FAIL message: `UNSOURCED CLAIM — "{claim_excerpt}" has no source citation`

T1-32: For each source reference in the traceability table, verify the referenced stage output exists. Check that stage-02 references match actual objectives, stage-07 references match actual transfer content, etc. FAIL message: `BROKEN SOURCE — source "{reference}" does not exist in stage output`

T1-33: Count marketing prose word count (excluding traceability table). Count curriculum word count (stages 01-07). Verify marketing < 25%. FAIL message: `MARKETING RATIO EXCEEDED — marketing is {N}% of curriculum (max 25%)`

**Change 4: Update Rule 4 — Tier 3 Scope**

Replace the current Rule 4 text to enable Tier 3 checks for stages 7-8:

```
### Rule 4 — Tier 3 Scope

Run T3-01 through T3-09. For any stage whose directory is missing or empty, note "Not applicable — Stage N not yet generated" for that stage's Tier 3 items.
```

Use the same "Not applicable" pattern — if stage output exists, check it; if not, mark as not applicable.
  </action>
  <verify>
    <automated>grep -c "T1-19\|T1-25\|T1-31\|T1-33" .claude/plugins/curriculum/agents/knz-validator.md | grep -q -v "^0$" && grep "T1-01 through T1-33" .claude/plugins/curriculum/agents/knz-validator.md | grep -q "all stages" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>knz-validator.md runs T1-01 through T1-33 against all 8 stages with specific failure messages, reads stages 6-8 files (metaskill-map.md, transfer-ecosystem.md, marketing-package.md), and uses "Not applicable" only for stages that genuinely have not been generated yet</done>
</task>

</tasks>

<verification>
After task completes:
1. Rule 2 says "T1-01 through T1-33 (all stages)" not "T1-01 through T1-18"
2. Stage Reading section reads 7 stages (stages 1-4 existing + stages 5-7 new)
3. Each of T1-19 through T1-33 has a specific check implementation with named failure message
4. Rule 4 enables Tier 3 for all stages
5. Missing stages still get "Not applicable" (graceful degradation for partial pipelines)
6. Failure messages follow the established format (MISSING, GENERIC, VIOLATION, etc.)
</verification>

<success_criteria>
- Validator covers all 33 Tier 1 checks end-to-end
- Stages 6-8 are read and checked when they exist
- Missing stages degrade gracefully with "Not applicable" messages
- Tier 3 scope expanded to all stages
- A curriculum that passes validation has been checked through all 8 content stages
</success_criteria>

<output>
After completion, create `.planning/phases/21-deviation-validation-coverage/21-03-SUMMARY.md`
</output>
