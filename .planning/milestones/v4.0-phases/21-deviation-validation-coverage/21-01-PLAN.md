---
phase: 21-deviation-validation-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/sessions.md
  - .claude/plugins/curriculum/commands/metaskills.md
autonomous: true
requirements: [DEVL-01]

must_haves:
  truths:
    - "When a draft audit check fails, the generator retries up to 3 times with cumulative constraint injection before stopping"
    - "After 3 failed attempts, the stage stops and shows the user all failure reports with problem + location + suggestion format"
    - "Auto-fix is limited to vocabulary substitution, registry default fills, and outcome drift correction — no content-level auto-fixes"
    - "Escalation messages show both what was auto-fixed and what still needs attention"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/modules.md"
      provides: "3-attempt retry with escalation in draft audit"
      contains: "Attempt 1"
    - path: ".claude/plugins/curriculum/commands/sessions.md"
      provides: "3-attempt retry with escalation in draft audit"
      contains: "Attempt 1"
    - path: ".claude/plugins/curriculum/commands/metaskills.md"
      provides: "3-attempt retry with escalation in draft audit"
      contains: "Attempt 1"
  key_links:
    - from: ".claude/plugins/curriculum/commands/modules.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "vocabulary auto-fix references canonical substitution table"
      pattern: "curriculum-voice.md"
    - from: ".claude/plugins/curriculum/commands/sessions.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "vocabulary auto-fix references canonical substitution table"
      pattern: "curriculum-voice.md"
---

<objective>
Add structured deviation handling (3-attempt retry with cumulative constraint injection and escalation) to the existing draft-audit sections in modules.md, sessions.md, and metaskills.md.

Purpose: Currently these commands stop on first non-auto-fixable failure and tell the user to re-run the command manually. This plan adds automatic retry with progressively tighter constraints before escalating — the generator learns from each failure what specifically to avoid.

Output: Updated draft-audit sections in 3 command files with retry logic, cumulative constraints, and plain-language escalation presentation.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deviation-validation-coverage/21-CONTEXT.md
@.planning/phases/19-pipeline-infrastructure/19-02-SUMMARY.md
@.planning/phases/20-integrity-verification/20-02-SUMMARY.md

<interfaces>
<!-- Current draft-audit pattern in modules.md (lines ~349-420) — sessions.md and metaskills.md follow the same structure -->

From modules.md "Audit Result" section (the section being replaced/extended):
```
If any check fails:
1. Attempt auto-fix for simple failures:
   - Vocabulary violations: substitute with the plain-language replacement from curriculum-voice.md
   - Missing fields that have obvious default values: fill in from registry data
   - Outcome drift: replace draft outcome wording with registry canonical wording
2. Re-run the failing check(s) after auto-fix.
3. If still failing after auto-fix: stop and report the specific failures. Do not promote. Do not mark the stage complete.

   > Draft audit found {N} issue(s) that could not be auto-fixed:
   > - {file}: {specific problem}
   >
   > Fix these issues and run `/curriculum:modules` again.
```

modules.md has 7 checks: completeness, registry consistency, vocabulary, schema, outcome drift, generic content, doctrine compliance
sessions.md has 8 checks: completeness, registry consistency, vocabulary, schema, outcome drift, formative assessment, pre-work gaps, goal-backward
metaskills.md has 6 checks: completeness, registry consistency, vocabulary, schema, outcome drift, generic content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3-attempt retry with cumulative constraints to modules.md and metaskills.md</name>
  <files>.claude/plugins/curriculum/commands/modules.md, .claude/plugins/curriculum/commands/metaskills.md</files>
  <action>
Replace the current "Audit Result" section (after all checks, before "If all N checks pass: promote") in both files with the structured deviation handling pattern below. The promotion logic stays identical — only the failure path changes.

**New "Audit Result" section structure for both files:**

```
**Audit Result:**

If all {N} checks pass: [existing promotion logic unchanged]

If any check fails:

### Auto-Fix Pass

1. Attempt auto-fix for deterministic failures only:
   - Vocabulary violations: substitute with the plain-language replacement from curriculum-voice.md
   - Missing fields with obvious defaults: fill from registry data
   - Outcome drift: replace draft wording with registry canonical wording
2. Re-run the failing check(s) after auto-fix.
3. Track what was fixed: "{N} vocabulary issues fixed, {N} outcome wording corrected, {N} registry defaults filled."

**Auto-fix boundary — these three categories only.** Anything involving content judgment (generic content, doctrine compliance, missing pedagogy) is NOT auto-fixable. Do not attempt to patch content problems — they require regeneration.

### Retry with Cumulative Constraints (content failures only)

If blocking failures remain after auto-fix AND this is not yet attempt 3:

**Attempt tracking:** This is attempt {current} of 3 for {file name}.

1. Collect all remaining blocking failure reasons into a constraint list.
2. Regenerate ONLY the failing file(s) — not the entire stage output. Inject the constraint list into the generation prompt:

   > The previous draft of {file} failed these checks:
   > - {failure 1 reason}
   > - {failure 2 reason from attempt 1, if attempt 2+}
   >
   > Regenerate this file. The new version MUST avoid these specific problems.

3. Write the regenerated file to `_drafts/`, replacing the failing draft.
4. Re-run ALL checks (not just the previously failing ones) on the regenerated file.
5. If all checks pass: proceed to promotion.
6. If checks still fail: increment attempt counter. If under 3, loop back to step 1 with the cumulative constraint list (attempt 2 carries failure reasons from attempt 1; attempt 3 carries reasons from attempts 1 and 2).

### Escalation (after 3 failed attempts)

If a file has failed 3 attempts:

1. Stop the stage. Do not promote any files. Do not mark the stage complete.
2. Present the escalation report:

   > Draft audit tried 3 times to fix {file} and could not resolve all issues.
   >
   > **What was auto-fixed:** {brief summary — e.g., "Fixed 2 vocabulary issues, corrected outcome wording in 1 place"}
   >
   > **What still needs attention:**
   > - **{Check name}** in {file path}, {section/field}: {plain-language description of the problem} — {concrete suggestion for what to change}
   > - **{Check name}** in {file path}, {section/field}: {plain-language description} — {suggestion}
   >
   > The draft files are in `_drafts/` if you want to edit them directly, or run `/curriculum:{command}` to start fresh.

3. The escalation message must follow curriculum-voice.md — no ID jargon. Problem descriptions use the same plain language as the check failure messages established in Phase 18.
```

For **modules.md**: N=7. The auto-fixable checks are: vocabulary (Check 3), outcome drift (Check 5), and missing registry defaults. The content failures that trigger retry are: generic content (Check 6) and doctrine compliance (Check 7). Checks 1 (completeness), 2 (registry consistency), and 4 (schema) are structural and should stop immediately if they fail after auto-fix (no retry — these indicate a broken generation, not a content quality issue).

For **metaskills.md**: N=6. Same auto-fix categories. Content failure that triggers retry: generic content (Check 6). Same structural-stops-immediately rule for checks 1, 2, 4.

Add a note after the retry section: "Structural failures (file completeness, registry consistency, schema compliance) do not enter the retry loop — they indicate a generation error, not a content quality issue. Report them immediately."
  </action>
  <verify>
    <automated>grep -c "Attempt tracking" .claude/plugins/curriculum/commands/modules.md .claude/plugins/curriculum/commands/metaskills.md | grep -v ":0$" | wc -l | grep -q "2" && grep -c "Escalation" .claude/plugins/curriculum/commands/modules.md .claude/plugins/curriculum/commands/metaskills.md | grep -v ":0$" | wc -l | grep -q "2" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both modules.md and metaskills.md have 3-attempt retry logic with cumulative constraint injection, auto-fix boundary documentation, structural-failure short-circuit, and plain-language escalation presentation replacing the old "stop and report" pattern</done>
</task>

<task type="auto">
  <name>Task 2: Add 3-attempt retry with cumulative constraints to sessions.md</name>
  <files>.claude/plugins/curriculum/commands/sessions.md</files>
  <action>
Apply the same structured deviation handling pattern from Task 1 to sessions.md, with these sessions-specific adjustments:

Sessions.md has 8 checks. The content failures that trigger retry are:
- Check 6: Missing formative assessment (requires adding assessment activities)
- Check 7: Pre-work gaps (requires naming pre-work and building session connections)
- Check 8b/8c: Goal-backward Substantive and Wired failures (requires content regeneration)

Check 8a (Exists) is structural — missing files stop immediately, no retry.

The retry constraint injection for sessions is per-module (since sessions generate per-module). When a module's sessions fail:

```
> The previous draft of sessions for module {M-N} failed these checks:
> - {failure reason from Check 6/7/8b/8c}
>
> Regenerate sessions for this module. The new version MUST avoid these specific problems.
```

The per-file budget from CONTEXT.md applies: a module's sessions get 3 total attempts, not 3 per check type.

The escalation report for sessions should reference the specific module and session:

```
> **What still needs attention:**
> - **Missing mid-module check-in** in Module M-1 "Name": Sessions S-1 through S-3 have no formative assessment point — add a brief skill check or practice moment in at least one session
> - **Pre-work not named** in Module M-2 "Name", Session S-2: Pre-work is referenced but not described — name the specific reading, exercise, or preparation task
```

Replace the existing "Audit Result" / failure handling section with the full retry + escalation pattern. Keep the promotion logic and registry write logic identical.
  </action>
  <verify>
    <automated>grep -c "Attempt tracking" .claude/plugins/curriculum/commands/sessions.md | grep -q "1" && grep -c "Escalation" .claude/plugins/curriculum/commands/sessions.md | grep -q "1" && grep -c "cumulative" .claude/plugins/curriculum/commands/sessions.md | grep -q -v "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>sessions.md has 3-attempt retry logic with per-module constraint injection, auto-fix boundary, structural-failure short-circuit for Check 8a, and plain-language escalation with module/session-specific problem descriptions</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All three files (modules.md, sessions.md, metaskills.md) contain "Attempt tracking" and "Escalation" sections
2. Auto-fix boundary is documented in all three files (vocabulary, registry defaults, outcome drift only)
3. Structural failures (completeness, registry consistency, schema) do not enter retry loop in any file
4. Escalation messages follow plain-language format (problem + location + suggestion)
5. No ID jargon in any escalation message template
</verification>

<success_criteria>
- All 3 draft-audit command files have 3-attempt retry with cumulative constraint injection
- Auto-fix boundaries are explicitly documented (3 categories only)
- Content failures trigger retry; structural failures stop immediately
- Escalation shows both auto-fixes applied and remaining failures
- Escalation messages use plain language per curriculum-voice.md
</success_criteria>

<output>
After completion, create `.planning/phases/21-deviation-validation-coverage/21-01-SUMMARY.md`
</output>
