---
phase: 30-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/curriculum/intake/SKILL.md
  - .claude/reference/alignment-check-reference.md
  - .claude/commands/curriculum/outcomes/SKILL.md
  - .claude/commands/curriculum/assessments/SKILL.md
  - .claude/commands/curriculum/modules/SKILL.md
  - .claude/commands/curriculum/sessions/SKILL.md
  - .claude/commands/curriculum/metaskills/SKILL.md
  - .claude/commands/curriculum/transfer/SKILL.md
  - .claude/commands/curriculum/marketing/SKILL.md
autonomous: true
requirements: [AUDIT-05, RSRCH-05, ALIGN-01]

must_haves:
  truths:
    - "Intake Build Summary initialization includes Alignment checks and Consistency checks counter fields"
    - "Every skip guard and source material reference for domain-research-findings.md uses the fully anchored workspace path"
    - "alignment-check-reference.md lists the grounding document with its full workspace path"
  artifacts:
    - path: ".claude/commands/curriculum/intake/SKILL.md"
      provides: "Build Summary init with all 8 counter fields"
      contains: "Alignment checks"
    - path: ".claude/reference/alignment-check-reference.md"
      provides: "Anchored path for grounding document"
      contains: "workspace/{project}/source-material/domain-research-findings.md"
  key_links:
    - from: ".claude/commands/curriculum/intake/SKILL.md"
      to: ".claude/reference/audit-trail-format.md"
      via: "Build Summary field list matches template"
      pattern: "Alignment checks.*passed.*issues resolved"
    - from: ".claude/reference/alignment-check-reference.md"
      to: ".claude/commands/curriculum/research/SKILL.md"
      via: "Grounding document path matches output location"
      pattern: "workspace/\\{project\\}/source-material/domain-research-findings\\.md"
---

<objective>
Close two low-severity integration gaps identified by the v5.0 milestone audit: (1) intake Build Summary missing Alignment checks and Consistency checks counter fields, (2) domain-research-findings.md referenced with bare filename instead of anchored workspace path.

Purpose: Structurally complete the audit trail initialization and eliminate ambiguous file references in skip guards that could cause alignment checks to skip incorrectly.
Output: Updated intake SKILL.md, alignment-check-reference.md, and 7 stage SKILL.md files with anchored paths.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/reference/audit-trail-format.md
@.claude/reference/alignment-check-reference.md
@.claude/commands/curriculum/intake/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing Build Summary counter fields to intake trail initialization</name>
  <files>.claude/commands/curriculum/intake/SKILL.md</files>
  <action>
In `.claude/commands/curriculum/intake/SKILL.md`, there are two Build Summary initialization blocks — one for clean intake (around line 373) and one for audit-mode intake (around line 911). Both are missing two counter lines that exist in the canonical template at `.claude/reference/audit-trail-format.md`.

For BOTH blocks, insert two new lines between the `**Grounding:**` line and the `**SME checkpoints:**` line:

```
- **Alignment checks:** 0 passed, 0 issues resolved
- **Consistency checks:** 0 passed, 0 issues resolved
```

The clean intake block (around line 373) should become:
```
## Build Summary
- **Stages completed:** Stage 1: Intake
- **Source materials:** 0 files loaded
- **Grounding:** 0% of content sections grounded in source material
- **Alignment checks:** 0 passed, 0 issues resolved
- **Consistency checks:** 0 passed, 0 issues resolved
- **SME checkpoints:** 1 confirmation recorded
- **Modifications:** {0, or count of edits made at the review gate}
- **Revisions:** 0 post-delivery revisions
```

The audit-mode block (around line 911) should become:
```
## Build Summary
- **Stages completed:** Stage 1: Intake
- **Source materials:** {count of files read in Step 1} files loaded
- **Grounding:** {rough percentage — grounded intake decisions / total intake decisions x 100}% of content sections grounded in source material
- **Alignment checks:** 0 passed, 0 issues resolved
- **Consistency checks:** 0 passed, 0 issues resolved
- **SME checkpoints:** 1 confirmation recorded
- **Modifications:** {0, or count of edits made at the Step 7 review gate}
- **Revisions:** 0 post-delivery revisions
```

Both start at 0 because no generation stages have run yet at intake time. Downstream stages increment these counters as alignment and consistency checks pass.
  </action>
  <verify>
    <automated>grep -c "Alignment checks.*0 passed" .claude/commands/curriculum/intake/SKILL.md | grep "2" && grep -c "Consistency checks.*0 passed" .claude/commands/curriculum/intake/SKILL.md | grep "2"</automated>
  </verify>
  <done>Both Build Summary blocks in intake SKILL.md contain all 8 counter fields matching the audit-trail-format.md template. Each block has Alignment checks and Consistency checks lines initialized to 0.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor domain-research-findings.md paths in alignment reference and all stage skip guards</name>
  <files>
    .claude/reference/alignment-check-reference.md,
    .claude/commands/curriculum/outcomes/SKILL.md,
    .claude/commands/curriculum/assessments/SKILL.md,
    .claude/commands/curriculum/modules/SKILL.md,
    .claude/commands/curriculum/sessions/SKILL.md,
    .claude/commands/curriculum/metaskills/SKILL.md,
    .claude/commands/curriculum/transfer/SKILL.md,
    .claude/commands/curriculum/marketing/SKILL.md
  </files>
  <action>
The grounding document is created at `workspace/{project}/source-material/domain-research-findings.md` by the research command. But the alignment-check-reference.md and 7 stage SKILL.md files reference it with the bare filename `domain-research-findings.md`, which is imprecise and could cause skip guards to look in the wrong location.

**File 1: `.claude/reference/alignment-check-reference.md`**

Line 15 currently reads:
```
- `domain-research-findings.md` (the grounding document produced by Stage 1.5 domain research)
```
Change to:
```
- `workspace/{project}/source-material/domain-research-findings.md` (the grounding document produced by Stage 1.5 domain research)
```

**Files 2-8: All 7 stage SKILL.md files** (outcomes, assessments, modules, sessions, metaskills, transfer, marketing)

Each has a skip guard line that reads:
```
If no files exist in `workspace/source-material/` AND no `domain-research-findings.md` exists, skip ...
```

In EACH file, change the skip guard to use the fully anchored path:
```
If no files exist in `workspace/{project}/source-material/` AND no `workspace/{project}/source-material/domain-research-findings.md` exists, skip ...
```

Note: the `workspace/source-material/` in the first half of the guard is also missing the `{project}/` segment. Fix both halves of the AND condition.

Additionally, each stage has a "Read source material" step that says:
```
1. Read source material files from `workspace/source-material/` and `domain-research-findings.md` if present.
```
Change to:
```
1. Read source material files from `workspace/{project}/source-material/` (including `domain-research-findings.md` if present).
```

This makes clear that domain-research-findings.md IS a file inside source-material/, not a separate location.
  </action>
  <verify>
    <automated>grep -rn "domain-research-findings.md" .claude/reference/alignment-check-reference.md .claude/commands/curriculum/outcomes/SKILL.md .claude/commands/curriculum/assessments/SKILL.md .claude/commands/curriculum/modules/SKILL.md .claude/commands/curriculum/sessions/SKILL.md .claude/commands/curriculum/metaskills/SKILL.md .claude/commands/curriculum/transfer/SKILL.md .claude/commands/curriculum/marketing/SKILL.md | grep -v "workspace/{project}/source-material/domain-research-findings.md" | grep -v "research/SKILL.md" | wc -l | tr -d ' ' | grep "^0$"</automated>
  </verify>
  <done>Every reference to domain-research-findings.md across alignment-check-reference.md and all 7 stage SKILL.md files uses the fully anchored path `workspace/{project}/source-material/domain-research-findings.md`. No bare filename references remain. Skip guards check both halves with the `{project}/` segment included.</done>
</task>

</tasks>

<verification>
1. Intake Build Summary matches audit-trail-format.md template (all 8 fields present in both clean and audit-mode blocks)
2. No bare `domain-research-findings.md` references remain in alignment-check-reference.md or any stage SKILL.md
3. All skip guards use `workspace/{project}/source-material/` (not `workspace/source-material/`)
</verification>

<success_criteria>
- Intake audit trail initialization produces Build Summary blocks with Alignment checks and Consistency checks counters at 0
- Every alignment skip guard across 7 stages uses fully anchored workspace paths
- alignment-check-reference.md Section 1 lists grounding document with full path
- No regression in existing skip guard logic (same conditions, just anchored paths)
</success_criteria>

<output>
After completion, create `.planning/phases/30-tech-debt-cleanup/30-01-SUMMARY.md`
</output>
