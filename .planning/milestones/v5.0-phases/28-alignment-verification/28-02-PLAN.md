---
phase: 28-alignment-verification
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - .claude/commands/curriculum/outcomes/SKILL.md
  - .claude/commands/curriculum/assessments/SKILL.md
  - .claude/commands/curriculum/modules/SKILL.md
  - .claude/commands/curriculum/sessions/SKILL.md
  - .claude/commands/curriculum/metaskills/SKILL.md
  - .claude/commands/curriculum/transfer/SKILL.md
  - .claude/commands/curriculum/marketing/SKILL.md
autonomous: true
requirements: [ALIGN-01, ALIGN-02, ALIGN-03, ALIGN-04, ALIGN-05, ALIGN-06]

must_haves:
  truths:
    - "After each generation stage completes its draft audit, an alignment check verifies output against source material"
    - "Alignment issues block draft promotion — same hard-gate as other quality checks"
    - "When source material says 'in some contexts' but output says 'always', the qualifier stripping is detected"
    - "When source gives a range but output narrows it, the range narrowing is detected"
    - "Content generated without source backing is flagged as assumed in the alignment report"
    - "Marketing claims trace to outcome IDs but are not checked for verbatim alignment"
    - "Alignment check results are recorded in the stage's audit trail section"
  artifacts:
    - path: ".claude/commands/curriculum/outcomes/SKILL.md"
      provides: "Stage 2 alignment check in draft audit"
      contains: "alignment-check-reference"
    - path: ".claude/commands/curriculum/assessments/SKILL.md"
      provides: "Stage 3 alignment check in draft audit"
      contains: "alignment-check-reference"
    - path: ".claude/commands/curriculum/modules/SKILL.md"
      provides: "Stage 4 alignment check in draft audit"
      contains: "alignment-check-reference"
    - path: ".claude/commands/curriculum/sessions/SKILL.md"
      provides: "Stage 5 alignment check in draft audit"
      contains: "alignment-check-reference"
    - path: ".claude/commands/curriculum/metaskills/SKILL.md"
      provides: "Stage 6 alignment check in draft audit"
      contains: "alignment-check-reference"
    - path: ".claude/commands/curriculum/transfer/SKILL.md"
      provides: "Stage 7 alignment check in draft audit"
      contains: "alignment-check-reference"
    - path: ".claude/commands/curriculum/marketing/SKILL.md"
      provides: "Stage 8 alignment check (traceability variant) in draft audit"
      contains: "alignment-check-reference"
  key_links:
    - from: "all 7 SKILL.md files"
      to: ".claude/reference/alignment-check-reference.md"
      via: "load directive before alignment check runs"
      pattern: "alignment-check-reference\\.md"
    - from: "all 7 SKILL.md files"
      to: "audit-trail.md"
      via: "alignment results written to trail after check passes"
      pattern: "Alignment Check"
---

<objective>
Add alignment checks to all 7 generation stage commands so that every stage verifies its output against source material before promotion.

Purpose: This is the enforcement layer. Plan 01 defined what alignment checks do; this plan wires them into every stage's draft audit pipeline so distortion, qualifier stripping, and range narrowing are caught automatically before content advances.

Output: All 7 generation stage SKILL.md files updated with alignment check as a new draft audit check, alignment results written to audit trail, and marketing's traceability variant.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-alignment-verification/28-CONTEXT.md
@.planning/phases/28-alignment-verification/28-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the alignment-check-reference.md will exist when this runs -->
<!-- Executor must load that file to understand the full check logic -->
<!-- Key integration pattern from CONTEXT.md: -->

Alignment check integration pattern (from 28-CONTEXT.md):
- Alignment check is a NEW check added to each stage's existing draft audit section
- Runs ALONGSIDE other checks (vocabulary, registry, schema, etc.) before promotion
- Same 3-attempt retry pattern with cumulative constraints
- Alignment issues are never auto-fixable — stage must re-generate
- The reference doc alignment-check-reference.md is loaded before the check runs (same pattern as audit-trail-format.md)

Existing draft audit patterns by stage:
- outcomes/SKILL.md: No draft audit (outcomes uses inline review gate, not _drafts/ pipeline). Alignment check added as a post-generation verification step before the review gate display.
- assessments/SKILL.md: No _drafts/ pipeline (assessments uses inline approval). Alignment check added as verification before approval display.
- modules/SKILL.md: Has _drafts/ with 7 checks. Alignment check becomes Check 8.
- sessions/SKILL.md: Has _drafts/ with 8 checks. Alignment check becomes Check 9.
- metaskills/SKILL.md: Has _drafts/ with checks. Alignment check added as next check number.
- transfer/SKILL.md: Has _drafts/ with checks. Alignment check added as next check number.
- marketing/SKILL.md: Has _drafts/ with 7 checks. Alignment check (traceability variant) becomes Check 8.

Audit trail write pattern (from Phase 25):
- Each stage already has a trail write step after promotion
- Alignment Check subsection is added to the trail write — after "Read but Not Referenced", before "SME Confirmation"
- Build Summary alignment counter is incremented
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alignment checks to stages 2-5 (outcomes, assessments, modules, sessions)</name>
  <files>
    .claude/commands/curriculum/outcomes/SKILL.md
    .claude/commands/curriculum/assessments/SKILL.md
    .claude/commands/curriculum/modules/SKILL.md
    .claude/commands/curriculum/sessions/SKILL.md
  </files>
  <action>
Read all 4 SKILL.md files in full. For each, add the alignment check following the stage-specific integration pattern below.

**For ALL 4 stages, add this load directive** in the source material / reference loading section (near where audit-trail-format.md is loaded):

```
Load `.claude/reference/alignment-check-reference.md` for the alignment check logic. This must be available before the alignment check step.
```

**For ALL 4 stages, add the alignment check skip guard** at the start of the alignment check:

```
If no files exist in workspace/source-material/ AND no grounding document exists, skip the alignment check — there is nothing to align against. Record in the trail: "Alignment Check: Skipped — no source material available."
```

**Stage-specific integration:**

**outcomes/SKILL.md (Stage 2):**
Outcomes does NOT use the _drafts/ pipeline — it generates, runs inline constraint enforcement, then displays for review. Add the alignment check AFTER the existing constraint enforcement steps and BEFORE the review gate display. The alignment check:
1. Reads source material files from workspace/source-material/
2. Compares generated outcomes against source material using the rules in alignment-check-reference.md
3. Checks grounding-required areas: program outcomes, module outcomes, session outcomes, enduring understandings, essential questions
4. If alignment issues found: re-generate outcomes with alignment constraint injection (same cumulative retry pattern — 3 attempts max)
5. If alignment check passes: record results, then proceed to review gate display
6. On escalation: present alignment issues in the same plain-language format as other check failures, keep outcomes in draft state

Also update the existing audit trail write step to include the Alignment Check subsection after "Read but Not Referenced". Include result (PASS), issues found (0 or resolved count), distortions detected, assumed content areas, and attempt count.

Update the Build Summary increment to include alignment checks counter.

**assessments/SKILL.md (Stage 3):**
Assessments also does NOT use _drafts/ — it generates and presents for inline approval. Add the alignment check AFTER generation constraint enforcement and BEFORE the approval display, following the same pattern as outcomes. Grounding-required areas: assessment criteria, performance indicators, rubric standards.

Same trail write and Build Summary updates as outcomes.

**modules/SKILL.md (Stage 4):**
Modules DOES use the _drafts/ pipeline with 7 existing checks. Add alignment check as **Check 8: Source Material Alignment**. Place it after Check 7 (Doctrine Compliance). The check:
1. Reads source material files
2. Compares grounding-required areas: module descriptions, key concepts, prerequisite rationale
3. Reports using the format from alignment-check-reference.md

Update the check count: "All eight checks must pass before promotion" (was seven).

Alignment failures are BLOCKING and NOT auto-fixable — they follow the same retry path as Check 6 (Generic Content) and Check 7 (Doctrine Compliance). Update the retry section to include Check 8 in the list of checks that trigger re-generation.

Update the auto-fix boundary note: "these three categories only" still applies — alignment is in the "everything else" category that requires re-generation.

Update the escalation section to include alignment issues.

Update the trail write after promotion to include the Alignment Check subsection.

**sessions/SKILL.md (Stage 5):**
Sessions uses _drafts/ with 8 existing checks. Add alignment check as **Check 9: Source Material Alignment**. Place it after Check 8 (Goal-Backward Verification). Grounding-required areas: key concept explanations, pre-work content rationale. NOT checked: session activities, discussion prompts, time blocks, facilitator guide logistics (these are expected agent-generated per CONTEXT.md).

Update check count: "All nine checks must pass before promotion" (was eight).

Alignment failures are BLOCKING and NOT auto-fixable. Update retry section to include Check 9. Note: sessions retries are per-module — alignment issues for a specific module's sessions trigger re-generation of that module only.

Update escalation and trail write sections.
  </action>
  <verify>
    <automated>for f in outcomes assessments modules sessions; do grep -l "alignment-check-reference" .claude/commands/curriculum/$f/SKILL.md || echo "MISSING: $f"; done | grep -c "MISSING" | xargs test 0 -eq</automated>
  </verify>
  <done>All 4 stage commands (outcomes, assessments, modules, sessions) load alignment-check-reference.md, run the alignment check as part of their draft audit, block promotion on alignment failures, include alignment results in their audit trail write, and update the Build Summary alignment counter</done>
</task>

<task type="auto">
  <name>Task 2: Add alignment checks to stages 6-8 (metaskills, transfer, marketing traceability variant)</name>
  <files>
    .claude/commands/curriculum/metaskills/SKILL.md
    .claude/commands/curriculum/transfer/SKILL.md
    .claude/commands/curriculum/marketing/SKILL.md
  </files>
  <action>
Read all 3 SKILL.md files in full. For each, add the alignment check with the same load directive and skip guard as Task 1.

**metaskills/SKILL.md (Stage 6):**
Read the file to determine the current draft audit check structure. Add alignment check as the next numbered check after the existing last check. Grounding-required areas: thinking skill activation rationale, domain-specific examples. NOT checked: activation activity structure, facilitation logistics.

Follow the same integration pattern as modules/sessions: blocking, not auto-fixable, included in retry with cumulative constraints, included in escalation. Update check count, retry section, escalation section, and trail write.

**transfer/SKILL.md (Stage 7):**
Read the file to determine current draft audit structure. Add alignment check as next numbered check. Grounding-required areas: real-work application scenarios, success indicators. NOT checked: transfer activity structure, timeline logistics, community continuation format.

Same integration pattern as metaskills.

**marketing/SKILL.md (Stage 8) — TRACEABILITY VARIANT:**
Marketing gets a DIFFERENT alignment check per CONTEXT.md decisions. Read the file — it has 7 existing checks. Add as **Check 8: Source Material Alignment (Marketing Traceability)**.

Marketing alignment check does NOT verify verbatim alignment — marketing's job is transformation. Instead it verifies:

1. **Traceability:** Every learning promise in the marketing copy links to an outcome ID that exists in curriculum-registry.json outcome_wording. Check the `<!-- internal: outcome_id=... -->` comments — each referenced outcome ID must exist in the registry.

2. **Distortion (still applies):** Marketing cannot claim outcomes the evidence does not support. If source material says "research suggests peer feedback tends to improve retention" and marketing says "Peer feedback guarantees retention improvement" — that is qualifier stripping and is flagged. Apply the same qualifier stripping and range narrowing detection from alignment-check-reference.md to factual claims in the marketing copy.

3. **Over-claiming grounding (still applies):** If the audit trail's "Grounded In" section for marketing claims a source backs a claim, verify the source actually says that.

4. **NOT checked:** VOC language adaptation, PAS/DOS framework choice, headline formula application, emotional language derivation from outcomes — these are expected marketing transformations.

Update check count to 8. Alignment failures are BLOCKING for marketing too. Update retry section, escalation section, and trail write. The trail write's Alignment Check subsection for marketing should note "Traceability variant" and list: outcome IDs verified, distortions checked, over-claiming checked.
  </action>
  <verify>
    <automated>for f in metaskills transfer marketing; do grep -l "alignment-check-reference" .claude/commands/curriculum/$f/SKILL.md || echo "MISSING: $f"; done | grep -c "MISSING" | xargs test 0 -eq</automated>
  </verify>
  <done>All 3 remaining stage commands load alignment-check-reference.md, run the alignment check (marketing uses traceability variant), block promotion on alignment failures, include alignment results in their audit trail write, and update the Build Summary alignment counter. All 7 generation stages now have alignment verification.</done>
</task>

</tasks>

<verification>
1. All 7 generation stage SKILL.md files contain a load directive for alignment-check-reference.md
2. All 7 stages include the alignment check skip guard (no source material = skip)
3. Stages 2-7 check verbatim alignment with distortion detection (qualifier stripping, range narrowing, over-claiming)
4. Stage 8 (marketing) checks traceability + distortion but NOT verbatim alignment
5. All 7 stages block promotion on alignment failures
6. All 7 stages include alignment results in their audit trail write section
7. All 7 stages update the Build Summary alignment counter
8. Retry sections in all draft-audit stages include alignment check failures in the cumulative constraint injection
9. Escalation sections in all stages include alignment issues in the plain-language report
</verification>

<success_criteria>
- Running any generation stage with source material present triggers an alignment check before promotion
- Qualifier stripping (hedging removed) is detected and blocks promotion
- Range narrowing (range becomes single value) is detected and blocks promotion
- Marketing claims are verified for outcome ID traceability
- Alignment results appear in audit-trail.md after each stage completes
- The alignment check is skipped gracefully when no source material exists
</success_criteria>

<output>
After completion, create `.planning/phases/28-alignment-verification/28-02-SUMMARY.md`
</output>
