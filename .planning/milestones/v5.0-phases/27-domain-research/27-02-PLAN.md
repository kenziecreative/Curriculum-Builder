---
phase: 27-domain-research
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - .claude/commands/curriculum/intake/SKILL.md
  - .claude/commands/curriculum/resume/SKILL.md
autonomous: true
requirements: [RSRCH-06]

must_haves:
  truths:
    - "When from-scratch intake completes, the closing message directs the user to /curriculum:research instead of /curriculum:outcomes"
    - "When audit-mode intake completes (source materials exist), the closing message still directs to /curriculum:outcomes — research is skipped"
    - "The resume command knows about the domain research stage and routes correctly"
  artifacts:
    - path: ".claude/commands/curriculum/intake/SKILL.md"
      provides: "Updated chaining — from-scratch builds go to /curriculum:research, audit-mode builds go to /curriculum:outcomes"
    - path: ".claude/commands/curriculum/resume/SKILL.md"
      provides: "Updated routing table with domain research stage"
  key_links:
    - from: ".claude/commands/curriculum/intake/SKILL.md"
      to: ".claude/commands/curriculum/research/SKILL.md"
      via: "closing message and STATE.md Next Action for from-scratch builds"
      pattern: "curriculum:research"
    - from: ".claude/commands/curriculum/resume/SKILL.md"
      to: ".claude/commands/curriculum/research/SKILL.md"
      via: "stage routing table includes domain research"
      pattern: "research"
---

<objective>
Wire domain research into the existing pipeline — from-scratch intake chains to research instead of outcomes, and the resume command knows about the new stage.

Purpose: The research command exists (Plan 01) but nothing in the pipeline routes to it. This plan connects the dots so from-scratch builds naturally flow through research before outcomes, while audit-mode builds (with existing source materials) continue to skip directly to outcomes.

Output: Modified intake/SKILL.md and resume/SKILL.md with correct chaining and routing.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-domain-research/27-CONTEXT.md
@.planning/phases/27-domain-research/27-01-SUMMARY.md
@.claude/commands/curriculum/intake/SKILL.md
@.claude/commands/curriculum/resume/SKILL.md

<interfaces>
<!-- Current chaining points in intake/SKILL.md that need modification -->

From intake/SKILL.md — clean intake path (around lines 358-410):
- Line 361: `Session Continuity` → **Next Action:** Run /curriculum:outcomes to begin outcome design
- Line 410: > Your program brief is written and saved. Type `/clear` now, then run `/curriculum:outcomes` to design the learning outcomes.

From intake/SKILL.md — audit mode path (around lines 884-958):
- Line 887: `Session Continuity` → **Next Action:** Run /curriculum:outcomes to begin outcome design
- Line 844: **Next step:** Run `/curriculum:outcomes` to continue
- Line 958: > Your program brief, gap report, and pre-populated stage drafts are written and saved. Type `/clear` now, then run `/curriculum:outcomes` to review and build out the learning outcomes.

From resume/SKILL.md — stage routing:
- Needs a new routing entry for domain research stage between Stage 1 and Stage 2
- Must handle: not-started (route to /curriculum:research), in-progress (route to /curriculum:research), complete (skip to next)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update intake chaining for from-scratch vs audit-mode builds</name>
  <files>.claude/commands/curriculum/intake/SKILL.md</files>
  <action>
Read `.claude/commands/curriculum/intake/SKILL.md` fully. Make these targeted modifications:

**Clean intake path (from-scratch, no source materials):**

1. Find the STATE.md update step in the clean intake approval branch (around line 361). Change:
   - FROM: `Session Continuity` → **Next Action:** Run /curriculum:outcomes to begin outcome design
   - TO: `Session Continuity` → **Next Action:** Run /curriculum:research to ground the curriculum in evidence

2. Find the closing message in the clean intake approval branch (around line 410). Change:
   - FROM: > Your program brief is written and saved. Type `/clear` now, then run `/curriculum:outcomes` to design the learning outcomes.
   - TO: > Your program brief is written and saved. Type `/clear` now, then run `/curriculum:research` to ground the curriculum in evidence before designing outcomes.

**Audit mode path (existing source materials — RSRCH-06 skip):**

3. The audit mode path already chains to `/curriculum:outcomes` — this is CORRECT and should NOT change. Source materials exist, so domain research is skipped. Verify these lines remain unchanged:
   - Line ~844: **Next step:** Run `/curriculum:outcomes` to continue
   - Line ~887: `Session Continuity` → **Next Action:** Run /curriculum:outcomes to begin outcome design
   - Line ~958: closing message referencing /curriculum:outcomes

**Do NOT modify anything else in intake/SKILL.md.** This is a surgical edit — only the two chaining points in the clean intake path change.
  </action>
  <verify>
    <automated>grep -c "curriculum:research" .claude/commands/curriculum/intake/SKILL.md | awk '{if ($1 >= 2) print "PASS: found "$1" research references"; else print "FAIL: only "$1" research references"}'</automated>
  </verify>
  <done>
    - Clean intake path chains to /curriculum:research (STATE.md Next Action and closing message)
    - Audit mode path still chains to /curriculum:outcomes (unchanged)
    - No other modifications to intake/SKILL.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Update resume command with domain research routing</name>
  <files>.claude/commands/curriculum/resume/SKILL.md</files>
  <action>
Read `.claude/commands/curriculum/resume/SKILL.md` fully. Add domain research stage awareness:

1. **Stage routing table**: Find the section where resume routes to the next command based on stage status. Add a domain research entry between Stage 1 (intake) and Stage 2 (outcomes):

   When Stage 1 is `complete` and Domain Research is `not-started`:
   - Route to `/curriculum:research`
   - Display: "Domain research is next — this will ground your curriculum in evidence before outcome design."

   When Domain Research is `in-progress`:
   - Route to `/curriculum:research`
   - Display: "Domain research is in progress. Pick up where you left off."

   When Domain Research is `complete` (or when source materials existed and research was skipped):
   - Continue to normal Stage 2 routing

2. **Skip detection**: If `workspace/{project}/source-material/` already had files at intake time (audit mode), the Domain Research row in STATE.md will either be absent or marked `skipped`. In this case, resume should skip directly to Stage 2 outcomes routing — do not suggest running /curriculum:research.

3. **Status display**: If the resume status block shows stage progress, include domain research in the list if the row exists in STATE.md.

**Do NOT modify the existing routing for any other stages.** Only add the domain research routing between Stage 1 and Stage 2.
  </action>
  <verify>
    <automated>grep -c "curriculum:research\|domain.research\|Domain Research" .claude/commands/curriculum/resume/SKILL.md | awk '{if ($1 >= 2) print "PASS: found "$1" research references"; else print "FAIL: only "$1" research references"}'</automated>
  </verify>
  <done>
    - Resume command routes to /curriculum:research between Stage 1 and Stage 2
    - Resume handles not-started, in-progress, and complete states for domain research
    - Resume skips domain research routing when source materials exist (audit mode)
    - No other stages affected
  </done>
</task>

</tasks>

<verification>
- From-scratch intake → closing message says /curriculum:research (not /curriculum:outcomes)
- Audit mode intake → closing message still says /curriculum:outcomes (RSRCH-06 honored)
- Resume command recognizes domain research stage and routes correctly
- grep "curriculum:research" across intake and resume SKILL.md files returns expected matches
</verification>

<success_criteria>
- A from-scratch build flows: intake → research → outcomes (research inserted in pipeline)
- An audit-mode build flows: intake → outcomes (research skipped, RSRCH-06 satisfied)
- Resume command correctly handles all domain research states
- No existing pipeline stages broken
</success_criteria>

<output>
After completion, create `.planning/phases/27-domain-research/27-02-SUMMARY.md`
</output>
