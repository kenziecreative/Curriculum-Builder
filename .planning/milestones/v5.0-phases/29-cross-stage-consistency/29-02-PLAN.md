---
phase: 29-cross-stage-consistency
plan: "02"
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - .claude/commands/curriculum/sessions/SKILL.md
  - .claude/commands/curriculum/marketing/SKILL.md
  - .claude/commands/curriculum/approve/SKILL.md
autonomous: true
requirements: [XSTAGE-01, XSTAGE-02, XSTAGE-03, XSTAGE-04]

must_haves:
  truths:
    - "After sessions are generated, the user sees a consistency check confirming session-module alignment"
    - "After marketing is generated, the user sees a check confirming every claim traces to a measured outcome"
    - "Consistency checks run automatically as part of the draft audit — the user does not have to remember"
    - "When a contradiction is found, the user sees both conflicting values side by side with file references"
    - "The approve gate loads consistency-check-reference.md instead of inline integration check logic"
  artifacts:
    - path: ".claude/commands/curriculum/sessions/SKILL.md"
      provides: "Consistency check (time math, prerequisite ordering, outcome coverage) in draft audit"
      contains: "Consistency"
    - path: ".claude/commands/curriculum/marketing/SKILL.md"
      provides: "Consistency check (claim-to-assessment chain) in draft audit"
      contains: "Consistency"
    - path: ".claude/commands/curriculum/approve/SKILL.md"
      provides: "Final gate sweep via consistency-check-reference.md instead of inline logic"
      contains: "consistency-check-reference"
  key_links:
    - from: ".claude/commands/curriculum/sessions/SKILL.md"
      to: ".claude/reference/consistency-check-reference.md"
      via: "load directive in reference loading section"
      pattern: "consistency-check-reference"
    - from: ".claude/commands/curriculum/marketing/SKILL.md"
      to: ".claude/reference/consistency-check-reference.md"
      via: "load directive in reference loading section"
      pattern: "consistency-check-reference"
    - from: ".claude/commands/curriculum/approve/SKILL.md"
      to: ".claude/reference/consistency-check-reference.md"
      via: "final gate sweep loads reference instead of inline logic"
      pattern: "consistency-check-reference"
---

<objective>
Wire consistency checks into sessions and marketing stage commands, and refactor the approve gate to use consistency-check-reference.md instead of inline integration check logic.

Purpose: Make cross-stage consistency checking automatic — it runs as part of the draft audit for sessions and marketing, and as part of the final gate sweep, so nothing slips through the cracks.

Output: Updated sessions/SKILL.md, marketing/SKILL.md, approve/SKILL.md
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-cross-stage-consistency/29-CONTEXT.md
@.planning/phases/29-cross-stage-consistency/29-01-SUMMARY.md

@.claude/reference/consistency-check-reference.md
@.claude/reference/audit-trail-format.md
@.claude/reference/alignment-check-reference.md
@.claude/commands/curriculum/sessions/SKILL.md
@.claude/commands/curriculum/marketing/SKILL.md
@.claude/commands/curriculum/approve/SKILL.md

<interfaces>
<!-- Plan 01 creates consistency-check-reference.md with these sections that Plan 02 references: -->
<!-- Section 3: Session-to-Module Consistency (time math, prerequisite ordering, outcome coverage) -->
<!-- Section 4: Marketing Claim Tracing (claim -> outcome ID -> assessment) -->
<!-- Section 5: Final Gate Sweep (comprehensive sweep for approve gate) -->
<!-- Section 6: Contradiction Report Format (table with | Source | Says | File |) -->
<!-- Section 7: Retry Constraint Format -->
<!-- Section 8: Integration Pattern (runs after alignment, same retry behavior) -->

From .claude/commands/curriculum/sessions/SKILL.md:
- Check 9 is currently the last check (Source Material Alignment)
- Consistency check becomes Check 10
- Check count needs updating (currently 9)
- Retry with Cumulative Constraints section lists checks that trigger retry — add Check 10
- Trail write section needs Consistency Check subsection after Alignment Check

From .claude/commands/curriculum/marketing/SKILL.md:
- Check 8 is currently the last check (Source Material Alignment — Marketing Traceability)
- Consistency check becomes Check 9
- Check count needs updating (currently 8)
- Trail write section needs Consistency Check subsection after Alignment Check

From .claude/commands/curriculum/approve/SKILL.md:
- Lines 123-159: Cross-Stage Integration Check — this inline logic gets replaced with a reference to consistency-check-reference.md Section 5
- The approve gate already classifies results as blocking/warning/pending — keep this classification
- The approve gate already formats results for user display — update to use consistency-check-reference.md report format
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add consistency checks to sessions and marketing stage commands</name>
  <files>.claude/commands/curriculum/sessions/SKILL.md, .claude/commands/curriculum/marketing/SKILL.md</files>
  <action>
**Sessions (SKILL.md):**

1. Add load directive in the reference loading section (near where alignment-check-reference.md is loaded):
   `Load .claude/reference/consistency-check-reference.md for cross-stage consistency checks. This must be available before the consistency check step in the draft audit.`

2. Add Check 10 after Check 9 (Source Material Alignment). Title: "Check 10: Cross-Stage Consistency"

   Content for Check 10:
   - Skip condition: If module spec files do not exist (Stage 4 not yet generated), skip this check. Record in trail: "Consistency Check: Skipped — module specs not yet generated."
   - Run the three session-to-module checks from consistency-check-reference.md Section 3:
     a. Time math: For each module, sum session durations and compare against module spec allocated hours from `curriculum-registry.json` `time_allocations.modules`. On mismatch, report using the table format from Section 6 and show both fix paths.
     b. Prerequisite ordering: Check if any session references outcomes from a module that comes later in the prerequisite chain. Report violations per Section 6.
     c. Outcome ID coverage: For each module, verify every outcome ID in the module spec appears in at least one session. Report gaps per Section 6.
   - All three are blocking failures — consistency issues are never auto-fixable.
   - Report format: Use consistency-check-reference.md Section 6 (table format grouped by contradiction type).

3. Update the check count: Change "9 checks" to "10 checks" in the draft audit preamble/overview text.

4. Update Retry with Cumulative Constraints section: Add Check 10 (cross-stage consistency) to the list of checks that trigger retry. Same as other non-auto-fixable checks — if blocking failures remain after auto-fix, and this module is not yet at attempt 3, retry with cumulative constraints from consistency-check-reference.md Section 7.

5. Add Consistency Check subsection to the trail write section, after the Alignment Check subsection and before the Build Summary update:
   ```
   **Consistency Check:** (write only if consistency check passed — omit if skipped or escalated)
   - **Result:** PASS
   - **Checks run:** Time math, Prerequisite ordering, Outcome coverage
   - **Issues found:** 0
   - **Contradictions:** None
   - **Attempts:** {1, 2, or 3}
   ```

6. Update Build Summary trail write: Add "Increment consistency checks counter by 1 (or note 'skipped' if upstream stages not generated)"

**Marketing (SKILL.md):**

1. Add load directive in the reference loading section:
   `Load .claude/reference/consistency-check-reference.md for cross-stage consistency checks. This must be available before the consistency check step in the draft audit.`

2. Add Check 9 after Check 8 (Source Material Alignment — Marketing Traceability). Title: "Check 9: Cross-Stage Consistency (Marketing Claim Tracing)"

   Content for Check 9:
   - Skip condition: If assessments have not been generated (Stage 3 not complete), skip. Record: "Consistency Check: Skipped — assessments not yet generated."
   - Run the marketing claim tracing check from consistency-check-reference.md Section 4:
     For every marketing claim that links to an outcome ID (Phase 28 alignment enforces the linkage), verify that outcome ID has at least one assessment in `curriculum-registry.json` `assessment_criteria.assessments[].linked_outcomes`.
   - If a claim links to an outcome with no assessment: BLOCKING. Report: "Marketing promises '{claim text}' via outcome {ID}, but no assessment measures that outcome"
   - Report format: Use consistency-check-reference.md Section 6.
   - Not auto-fixable.

3. Update check count: Change "8 checks" to "9 checks".

4. Update retry section: Add Check 9 to the list of checks triggering retry with constraints from consistency-check-reference.md Section 7.

5. Add Consistency Check subsection to trail write, after Alignment Check:
   ```
   **Consistency Check (Claim Tracing):** (write only if consistency check passed — omit if skipped or escalated)
   - **Result:** PASS
   - **Checks run:** Marketing claim-to-assessment chain
   - **Issues found:** 0
   - **Contradictions:** None
   - **Attempts:** {1, 2, or 3}
   ```

6. Update Build Summary trail write: Add consistency checks counter increment.
  </action>
  <verify>
    <automated>grep -c "Check 10.*Consistency\|consistency-check-reference" .claude/commands/curriculum/sessions/SKILL.md | xargs test 2 -le && grep -c "Check 9.*Consistency\|consistency-check-reference" .claude/commands/curriculum/marketing/SKILL.md | xargs test 2 -le</automated>
  </verify>
  <done>Sessions SKILL.md has Check 10 (cross-stage consistency with time math, prerequisite ordering, outcome coverage), load directive, trail write, and retry integration. Marketing SKILL.md has Check 9 (claim-to-assessment chain), load directive, trail write, and retry integration. Both reference consistency-check-reference.md.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor approve gate to use consistency-check-reference.md</name>
  <files>.claude/commands/curriculum/approve/SKILL.md</files>
  <action>
Refactor the approve gate's Cross-Stage Integration Check (currently lines ~123-159) to load and reference consistency-check-reference.md instead of containing inline logic.

1. Add load directive near the top of the Behavior section (alongside the audit trail reference load):
   `Load .claude/reference/consistency-check-reference.md before running the final gate sweep. This defines all cross-stage consistency checks.`

2. Replace the inline Cross-Stage Integration Check section. Keep the section heading "### Cross-Stage Integration Check" but change the body to:

   "**Also for Final Validation gate — run the full cross-stage consistency sweep:**

   Before showing any summary, load `.claude/reference/consistency-check-reference.md` Section 5 (Final Gate Sweep). Run all checks defined there against the project workspace. Read `workspace/{project}/curriculum-registry.json` as the canonical source. Store all findings as `integration_findings`.

   For stages not yet generated, mark each finding as 'Pending — Stage N not yet generated' (not broken). Use the established pattern: if the target stage directory does not exist on disk, the reference is pending, not broken.

   Result classification follows consistency-check-reference.md Section 5:
   - Broken references: BLOCKING
   - Orphaned outcomes: BLOCKING
   - Phantom/unregistered modules: BLOCKING
   - Registry-file drift: WARNING
   - Pending (stage not generated): INFORMATIONAL

   Store findings in `integration_findings` with the same JSON structure as before."

3. Remove the inline Trace 1, Trace 2, Trace 3, and registry-file drift detection blocks (these are now in consistency-check-reference.md Section 5). Keep the `integration_findings` JSON structure and the result classification — those are used by the display logic below.

4. Keep ALL existing display logic (the Curriculum integrity section, plain-language descriptions, gate option logic) — only the check logic source changes, not how results are displayed.

5. Add a Consistency Check results section to the Final Validation display, after the existing "Curriculum integrity" display and before the gate options. Show per-stage consistency results from the trail if they exist:

   ```
   **Stage consistency:** {If consistency check trail entries exist for sessions and/or marketing, summarize: "Sessions: PASS/FAIL, Marketing: PASS/FAIL". If no entries: "Per-stage checks will be included in the sweep above."}
   ```

6. Do NOT change the Post-Assessment gate behavior — it still skips cross-stage checks as before.
  </action>
  <verify>
    <automated>grep -c "consistency-check-reference" .claude/commands/curriculum/approve/SKILL.md | xargs test 1 -le && grep -v "^--" .claude/commands/curriculum/approve/SKILL.md | grep -c "Trace 1.*bidirectional\|Trace 2.*bidirectional\|Trace 3.*bidirectional" | xargs test 0 -eq</automated>
  </verify>
  <done>Approve gate loads consistency-check-reference.md for the final gate sweep instead of containing inline Trace 1/2/3 logic. Display logic preserved. Post-Assessment gate unchanged. Per-stage consistency results surfaced in Final Validation display.</done>
</task>

</tasks>

<verification>
- Sessions SKILL.md has consistency check as the final numbered check in draft audit
- Marketing SKILL.md has consistency check as the final numbered check in draft audit
- Both stage commands load consistency-check-reference.md
- Both stage commands have trail write for Consistency Check subsection
- Both stage commands have consistency in retry constraint section
- Approve gate references consistency-check-reference.md Section 5 instead of inline traces
- Approve gate's inline Trace 1/2/3 logic removed (now in reference doc)
- Approve gate display logic preserved (blocking/warning/pending classification, plain-language descriptions)
- Check counts updated in both stage commands
- No changes to stages 2-4 or 6-7 (per CONTEXT.md decision — no per-stage consistency checks for those)
</verification>

<success_criteria>
- Consistency checks run automatically during sessions and marketing draft audit (XSTAGE-03)
- Session consistency covers time math, prerequisite ordering, and outcome coverage (XSTAGE-01)
- Marketing consistency covers claim-to-assessment chain (XSTAGE-02)
- Contradictions reported with side-by-side values and file references (XSTAGE-04)
- Approve gate uses shared reference document instead of inline logic (consolidation complete)
- All check counts updated, all trail writes added, all retry sections updated
</success_criteria>

<output>
After completion, create `.planning/phases/29-cross-stage-consistency/29-02-SUMMARY.md`
</output>
