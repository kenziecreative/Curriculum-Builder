---
phase: 12-voice-system
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - .claude/plugins/curriculum/commands/approve.md
  - .claude/plugins/curriculum/commands/assessments.md
  - .claude/plugins/curriculum/commands/evaluation-mode.md
  - .claude/plugins/curriculum/commands/init.md
  - .claude/plugins/curriculum/commands/intake.md
  - .claude/plugins/curriculum/commands/marketing.md
  - .claude/plugins/curriculum/commands/metaskills.md
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/outcomes.md
  - .claude/plugins/curriculum/commands/resume.md
  - .claude/plugins/curriculum/commands/sessions.md
  - .claude/plugins/curriculum/commands/transfer.md
  - .claude/plugins/curriculum/commands/validate.md
  - .claude/plugins/curriculum/agents/session-generator.md
autonomous: true
requirements:
  - VOICE-02
must_haves:
  truths:
    - "Every content-generating command file has a voice file reference line in its Persona section"
    - "The four worst-offending commands have inline guardrails — a bolded tight term list inside their Persona section"
    - "sessions.md orchestrator has only a one-line Persona section (voice file reference, no more)"
    - "session-generator.md agent has the inline guardrails, not sessions.md"
    - "The 4 commands that were missing Persona sections now have a minimal one-line Persona section"
    - "No existing Never-say lists or tone descriptions in commands with existing Persona sections have been modified"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/marketing.md"
      provides: "Worst-offender inline guardrail"
      contains: "curriculum-voice.md"
    - path: ".claude/plugins/curriculum/agents/session-generator.md"
      provides: "Sessions-domain inline guardrail"
      contains: "curriculum-voice.md"
    - path: ".claude/plugins/curriculum/commands/assessments.md"
      provides: "Assessment inline guardrail"
      contains: "curriculum-voice.md"
    - path: ".claude/plugins/curriculum/commands/transfer.md"
      provides: "Transfer inline guardrail"
      contains: "curriculum-voice.md"
  key_links:
    - from: "all 13 command files + session-generator.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "reference line inside ## Persona section"
      pattern: "curriculum-voice\\.md"
    - from: "marketing.md, transfer.md, assessments.md, session-generator.md"
      to: "inline term prohibitions"
      via: "bolded Critical inline guardrail line inside Persona"
      pattern: "Critical inline guardrail"
---

<objective>
Wire all 13 command files and the session-generator agent to read `curriculum-voice.md` before generating output. Add inline guardrails to the 4 worst-offending commands.

Purpose: The voice file created in Plan 01 has no effect until commands are instructed to read it. This plan creates that link for every content-generating command and adds high-risk term reminders directly in the Persona sections of the worst offenders.

Output: 13 modified command files + 1 modified agent file, each referencing curriculum-voice.md. Four of those also have inline guardrails.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-voice-system/12-CONTEXT.md
@.planning/phases/12-voice-system/12-RESEARCH.md
@.planning/phases/12-voice-system/12-01-SUMMARY.md

<interfaces>
<!-- Reference line format for commands WITH existing Persona sections (8 commands) -->
<!-- Add at TOP of existing ## Persona section, before tone description paragraph: -->
<!--   Read .claude/reference/curriculum-voice.md before generating any user-facing content. -->

<!-- Minimal Persona section format for commands MISSING Persona sections (4 commands) -->
<!-- These commands: approve.md, validate.md, evaluation-mode.md, sessions.md -->
<!-- Add a new ## Persona section with ONE line only. No tone paragraph. Phase 13 expands these. -->
<!--   ## Persona                                                                       -->
<!--                                                                                    -->
<!--   Read .claude/reference/curriculum-voice.md before generating any user-facing content. -->

<!-- Also applies to: init.md and resume.md — both lack Persona sections per research audit -->
<!-- Both get the same one-line minimal Persona section                                  -->

<!-- Inline guardrail format (4 worst offenders) -->
<!-- Bolded line inside Persona section, AFTER the reference line, BEFORE tone description -->
<!--   **Critical inline guardrail: Never use in [output type]: [term-1], [term-2], [term-3].**  -->

Worst-4 inline guardrail terms (per persona audit research):
  marketing.md:        schema, curriculum_traceability, bloom_level, learning_objective_id, element_type
  transfer.md:         implementation_intention, Kirkpatrick, peer_accountability_structure, schema, enum
  assessments.md:      Bloom's, bloom_level, paired_objective, outcome_id, formative assessment, summative assessment
  session-generator.md (agent): TMA, DCR, WIPPEA, bloom_level, and session template names as visible labels

Special case — sessions.md orchestrator vs session-generator.md agent:
  sessions.md: one-line Persona only (reference line, nothing else) — orchestrator produces routing prose, not content
  session-generator.md: new ## Persona section with reference line + inline guardrail — agent generates all session content

Commands with existing Persona sections (8 — add reference line at top of Persona):
  intake.md, outcomes.md, assessments.md, modules.md, metaskills.md, transfer.md, marketing.md
  Note: marketing.md and transfer.md are also worst-4 offenders — they get reference line + inline guardrail

Commands missing Persona sections — get minimal one-line Persona (6 total):
  sessions.md, approve.md, validate.md, evaluation-mode.md, init.md, resume.md
  Note: sessions.md is worst-4 adjacent but orchestrator gets reference line only (no inline guardrail)

Agent missing Persona section (1 — gets reference line + inline guardrail):
  session-generator.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reference line to all 13 commands</name>
  <files>
    .claude/plugins/curriculum/commands/intake.md,
    .claude/plugins/curriculum/commands/outcomes.md,
    .claude/plugins/curriculum/commands/assessments.md,
    .claude/plugins/curriculum/commands/modules.md,
    .claude/plugins/curriculum/commands/metaskills.md,
    .claude/plugins/curriculum/commands/transfer.md,
    .claude/plugins/curriculum/commands/marketing.md,
    .claude/plugins/curriculum/commands/approve.md,
    .claude/plugins/curriculum/commands/validate.md,
    .claude/plugins/curriculum/commands/evaluation-mode.md,
    .claude/plugins/curriculum/commands/sessions.md,
    .claude/plugins/curriculum/commands/init.md,
    .claude/plugins/curriculum/commands/resume.md
  </files>
  <action>
For each of the 13 command files, add the voice file reference line. Read each file before editing.

**Commands WITH existing ## Persona sections (7 commands):**
intake.md, outcomes.md, assessments.md, modules.md, metaskills.md, transfer.md, marketing.md

Add this line at the very top of the existing `## Persona` section, before the existing tone description paragraph:
```
Read .claude/reference/curriculum-voice.md before generating any user-facing content.
```
Leave a blank line between this reference line and the existing tone paragraph that follows. Do NOT modify the existing tone description, Never-say list, or any other content in the Persona section.

**Commands MISSING ## Persona sections (6 commands):**
approve.md, validate.md, evaluation-mode.md, sessions.md, init.md, resume.md

Insert a new `## Persona` section. For these commands, the complete Persona section is:
```markdown
## Persona

Read .claude/reference/curriculum-voice.md before generating any user-facing content.
```
That is the entire section — one reference line only. No tone paragraph. No prohibition list. This is intentional; Phase 13 expands these.

**Placement for commands missing Persona:** Insert the new `## Persona` section immediately before the `## Generation` section (or equivalent main execution section) in each file.

**Constraint:** Do NOT modify anything other than adding the reference line or new Persona section. No edits to existing Persona content, generation steps, output formats, or any other section.
  </action>
  <verify>
    <automated>grep -l "curriculum-voice.md" .claude/plugins/curriculum/commands/*.md | wc -l</automated>
  </verify>
  <done>
    - All 13 command files contain "curriculum-voice.md" — grep count returns 13
    - Commands with existing Persona sections have reference line at top of Persona, existing content untouched
    - Commands that were missing Persona sections now have a minimal ## Persona section with exactly one line
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inline guardrails to 4 worst-offending files</name>
  <files>
    .claude/plugins/curriculum/commands/marketing.md,
    .claude/plugins/curriculum/commands/transfer.md,
    .claude/plugins/curriculum/commands/assessments.md,
    .claude/plugins/curriculum/agents/session-generator.md
  </files>
  <action>
Add inline guardrail lines to the 4 worst-offending files. Read each file before editing.

**Format for all 4:**
A bolded critical note, placed AFTER the reference line and BEFORE the tone description paragraph inside the Persona section:
```
**Critical inline guardrail: Never use in [output type]: [term-1], [term-2], [term-3].**
```

**marketing.md:**
The file already has a `## Persona` section with the reference line (added in Task 1) and the existing tone description. Insert this line between the reference line and the existing tone paragraph:
```
**Critical inline guardrail: Never use in marketing copy: schema, curriculum_traceability, bloom_level, learning_objective_id, element_type.**
```

**transfer.md:**
Same placement — between the reference line (added in Task 1) and the existing tone description:
```
**Critical inline guardrail: Never use in transfer output: implementation_intention, Kirkpatrick, peer_accountability_structure, schema, enum.**
```

**assessments.md:**
Same placement:
```
**Critical inline guardrail: Never use in assessment output: bloom_level, paired_objective, outcome_id, formative assessment, summative assessment.**
```
Note: "Bloom's" is already in assessments.md Never-say list — omit from inline guardrail to avoid redundancy; use bloom_level instead.

**session-generator.md (agent, not command):**
This file has no existing Persona section. Task 1 does not cover it (it is not in the commands/ directory). Add a new `## Persona` section with reference line AND inline guardrail:
```markdown
## Persona

Read .claude/reference/curriculum-voice.md before generating any session content.

**Critical inline guardrail: TMA, DCR, WIPPEA, bloom_level, and session template names (gagne, merrill, 5e_7e, universal_tma_arc) never appear as visible labels in facilitator guides, participant materials, or slide outlines. These are structural tags only.**
```

**Placement for session-generator.md:** Insert before the main generation instructions section (whichever section begins the generation logic). Read the file first to confirm placement.

**Constraint:** Tight term lists only — 3-5 terms per command (or a short explanatory phrase for session-generator). Do not write a paragraph. Do not modify any other content.
  </action>
  <verify>
    <automated>grep -c "Critical inline guardrail" .claude/plugins/curriculum/commands/marketing.md .claude/plugins/curriculum/commands/transfer.md .claude/plugins/curriculum/commands/assessments.md .claude/plugins/curriculum/agents/session-generator.md</automated>
  </verify>
  <done>
    - Each of the 4 files has exactly 1 "Critical inline guardrail" line
    - session-generator.md has a new ## Persona section with reference line + inline guardrail
    - marketing.md, transfer.md, assessments.md each have the inline guardrail inside their existing Persona section, between the reference line and the tone description
    - Inline guardrails are 3-5 terms (tight), not paragraphs
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# All 13 commands reference voice file
grep -l "curriculum-voice.md" .claude/plugins/curriculum/commands/*.md | wc -l

# Agent references voice file
grep -l "curriculum-voice.md" .claude/plugins/curriculum/agents/session-generator.md

# 4 worst offenders have inline guardrails
grep -c "Critical inline guardrail" \
  .claude/plugins/curriculum/commands/marketing.md \
  .claude/plugins/curriculum/commands/transfer.md \
  .claude/plugins/curriculum/commands/assessments.md \
  .claude/plugins/curriculum/agents/session-generator.md

# sessions.md orchestrator has only one-line Persona (not more)
grep -A3 "## Persona" .claude/plugins/curriculum/commands/sessions.md
```

Expected: 13 commands with voice reference, agent with reference, 4 files with inline guardrail, sessions.md Persona showing only reference line.
</verification>

<success_criteria>
- grep -l "curriculum-voice.md" .claude/plugins/curriculum/commands/*.md returns 13 files
- session-generator.md contains "curriculum-voice.md"
- All 4 worst-offending files contain "Critical inline guardrail"
- sessions.md Persona section contains only the reference line (no tone paragraph, no prohibition list)
- No existing Persona content in any command was modified (only additions)
</success_criteria>

<output>
After completion, create `.planning/phases/12-voice-system/12-02-SUMMARY.md`
</output>
