---
phase: 04-dashboard-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ../knz-curriculum-dashboard/package.json
  - ../knz-curriculum-dashboard/tsconfig.json
  - ../knz-curriculum-dashboard/src/main.tsx
  - ../knz-curriculum-dashboard/src/index.css
  - ../knz-curriculum-dashboard/src/App.tsx
  - ../knz-curriculum-dashboard/vitest.config.ts
  - ../knz-curriculum-dashboard/src/test/fixtures/STATE.md
  - ../knz-curriculum-dashboard/src/lib/state-loader.test.ts
  - ../knz-curriculum-dashboard/src/lib/workspace-loader.test.ts
  - ../knz-curriculum-dashboard/src/lib/use-workspace-poll.test.ts
  - ../knz-curriculum-dashboard/vite-plugins/generate-html.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "A knz-curriculum-dashboard/ project directory exists at the sibling level to knz-builder-src/"
    - "npm install runs without errors and produces node_modules/"
    - "npx vitest run exits with no failures (stubs all pass or are skipped)"
    - "Dev server starts with npm run dev without TypeScript or Tailwind errors"
  artifacts:
    - path: "../knz-curriculum-dashboard/package.json"
      provides: "Exact dependency versions matching Brand Compass: react 19.2, vite 7.2.x, tailwindcss 4.1, typescript 5.9.x, react-markdown 10.1, remark-gfm 4.0.1, lucide-react 0.554.x, marked 15.x"
    - path: "../knz-curriculum-dashboard/vitest.config.ts"
      provides: "Vitest test framework config for the project"
    - path: "../knz-curriculum-dashboard/src/test/fixtures/STATE.md"
      provides: "Fixture file for parser tests — exact copy of workspace/test-program/STATE.md"
    - path: "../knz-curriculum-dashboard/src/lib/state-loader.test.ts"
      provides: "Failing test stubs for DASH-01 parser functions"
    - path: "../knz-curriculum-dashboard/src/lib/workspace-loader.test.ts"
      provides: "Failing test stubs for DASH-02 file discovery"
    - path: "../knz-curriculum-dashboard/src/lib/use-workspace-poll.test.ts"
      provides: "Failing test stubs for DASH-05 polling hook"
    - path: "../knz-curriculum-dashboard/vite-plugins/generate-html.test.ts"
      provides: "Failing test stubs for DASH-04 HTML generation"
  key_links:
    - from: "../knz-curriculum-dashboard/vitest.config.ts"
      to: "src/lib/*.test.ts"
      via: "include glob pattern"
      pattern: "include.*test"
    - from: "../knz-curriculum-dashboard/src/test/fixtures/STATE.md"
      to: "src/lib/state-loader.test.ts"
      via: "readFileSync fixture import"
      pattern: "fixtures/STATE\\.md"
---

<objective>
Scaffold the knz-curriculum-dashboard React project and create Wave 0 test stubs with fixture data.

Purpose: The dashboard is a new sibling project to knz-builder-src/ — it does not exist yet. This plan creates the project directory with the correct dependency versions (matching Brand Compass exactly), wires up Vitest as the test framework, and writes failing test stubs for every parser and hook before any implementation starts. Nyquist rule requires test files to exist before implementation tasks run.

Output: A buildable, testable project directory at ../knz-curriculum-dashboard/ with a working dev server stub and vitest finding (but not yet passing) the behavioral tests.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-mvp/04-CONTEXT.md
@.planning/phases/04-dashboard-mvp/04-RESEARCH.md
@.planning/phases/04-dashboard-mvp/04-VALIDATION.md

<interfaces>
<!-- Key reference: Brand Compass package.json exact versions -->
<!-- Source: /Users/kelseyruger/Projects/a-emporium-working/knz-brand-guidance/package.json -->
Exact dependency versions to replicate:
- react: "^19.2.0"
- react-dom: "^19.2.0"
- react-markdown: "^10.1.0"
- remark-gfm: "^4.0.1"
- lucide-react: "^0.554.0"
- tailwindcss: "^4.1.0"
- @tailwindcss/vite: "^4.1.0"
- @vitejs/plugin-react: "^5.1.0"
- vite: "^7.2.0"
- typescript: "^5.9.0"
New additions (not in Brand Compass):
- marked: "^15.0.0"
- vitest: "^3.0.0" (devDependency)
- @vitest/ui: "^3.0.0" (devDependency)
- @types/node: "^22.0.0" (devDependency)

<!-- KNZ STATE.md format (verified from workspace/test-program/STATE.md) -->
## Stage Progress
| Stage | Name | Status | Completed |
|-------|------|--------|-----------|
| 1 | Intake | complete | 2026-03-19 |
| 2 | Outcome Design | complete | 2026-03-20 |
| 3 | Assessment Design | complete | 2026-03-20 |
| 4 | Module Structure | not-started | - |

## Review Gates
| Gate | After Stage | Status | Approved |
|------|-------------|--------|----------|
| Post-Intake | 1 | approved | 2026-03-19 |
| Post-Assessment | 3 | approved | 2026-03-20 |
| Final Validation | 9 | not-reached | - |

## Session Continuity
- **Next Action:** Run /knz-modules to build module structure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold project directory with correct dependencies</name>
  <files>
    ../knz-curriculum-dashboard/package.json
    ../knz-curriculum-dashboard/tsconfig.json
    ../knz-curriculum-dashboard/vite.config.ts
    ../knz-curriculum-dashboard/src/main.tsx
    ../knz-curriculum-dashboard/src/App.tsx
    ../knz-curriculum-dashboard/src/index.css
  </files>
  <action>
The dashboard project lives at the PARENT of knz-builder-src/ — that is, at /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-curriculum-dashboard/. Confirm the parent path before scaffolding.

Steps:
1. Run: `ls /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/` to confirm the correct sibling location.
2. Create the project directory: `mkdir -p /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-curriculum-dashboard/src/test/fixtures`
3. Create all subdirectories: `src/components/`, `src/lib/`, `src/types/`, `vite-plugins/`
4. Write package.json with exact versions matching Brand Compass plus marked and vitest additions (see interfaces block for versions). Set port to 3002 (Brand Compass uses 3001). Add scripts: `"dev": "vite"`, `"build": "tsc && vite build"`, `"test": "vitest run"`, `"test:ui": "vitest --ui"`.
5. Write tsconfig.json — standard Vite React TypeScript config with `"@/*": ["./src/*"]` path alias and `"moduleResolution": "bundler"`.
6. Write a minimal vite.config.ts — import react and tailwindcss plugins, set `resolve.alias` for `@`, set `server.port` to 3002. Do NOT add serveWorkspace or generateHtml yet (Plan 02 does that). Add `import path from 'path'` and `import fs from 'fs'` to prepare for Plan 02 additions.
7. Write src/main.tsx — standard Vite React 19 entry: `import { StrictMode } from 'react'; import { createRoot } from 'react-dom/client'; import './index.css'; import App from './App'; createRoot(document.getElementById('root')!).render(<StrictMode><App /></StrictMode>)`
8. Write src/App.tsx — minimal stub: `export default function App() { return <div className="p-4 text-gray-900">KNZ Dashboard — Loading...</div> }`
9. Write src/index.css — only `@import "tailwindcss";` (Tailwind 4 syntax — no config file needed).
10. Run `npm install` from the project directory to install all dependencies.
11. Run `npm run dev` briefly to confirm the dev server starts without errors (Ctrl+C after confirming "Local: http://localhost:3002").

CRITICAL: Use `../knz-builder-src/workspace` as the workspace path reference — this is for Plan 02. Document it in a comment in vite.config.ts:
`// WORKSPACE_PATH: ../knz-builder-src/workspace (relative to this project root)`
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-curriculum-dashboard && node -e "const p=require('./package.json'); console.log(p.dependencies.react, p.dependencies.vite, p.devDependencies.vitest)"</automated>
  </verify>
  <done>Project directory exists, npm install succeeds, package.json has correct dependency versions, dev server starts on port 3002 without errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write Vitest config and failing test stubs with fixture data</name>
  <files>
    ../knz-curriculum-dashboard/vitest.config.ts
    ../knz-curriculum-dashboard/src/test/fixtures/STATE.md
    ../knz-curriculum-dashboard/src/lib/state-loader.test.ts
    ../knz-curriculum-dashboard/src/lib/workspace-loader.test.ts
    ../knz-curriculum-dashboard/src/lib/use-workspace-poll.test.ts
    ../knz-curriculum-dashboard/vite-plugins/generate-html.test.ts
  </files>
  <action>
Write the test infrastructure so all test files are found by Vitest and each test stub describes the expected behavior before implementation.

1. Write vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts', 'vite-plugins/**/*.test.ts'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

2. Copy workspace/test-program/STATE.md content to src/test/fixtures/STATE.md (copy the exact file as the parser fixture). The file is at /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/workspace/test-program/STATE.md.

3. Write src/lib/state-loader.test.ts — stubs using `describe`/`it.todo` for each behavior:
```typescript
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'fs'
import { join } from 'path'

// Load fixture — same format as workspace/test-program/STATE.md
const fixture = readFileSync(join(__dirname, '../test/fixtures/STATE.md'), 'utf-8')

describe('parseStageProgress', () => {
  it('returns all 9 stages with correct status from fixture', () => {
    // TODO: import { parseStageProgress } from './state-loader'
    // const stages = parseStageProgress(fixture)
    // expect(stages).toHaveLength(9)
    // expect(stages[0]).toEqual({ number: 1, name: 'Intake', status: 'complete', completed: '2026-03-19' })
    // expect(stages[3]).toEqual({ number: 4, name: 'Module Structure', status: 'not-started', completed: null })
  })
  it('returns status as not-started | in-progress | complete enum values', () => {
    // TODO
  })
})

describe('parseReviewGates', () => {
  it('returns all gates with correct status from fixture', () => {
    // TODO
    // expect gates[0] = { name: 'Post-Intake', afterStage: 1, status: 'approved', approved: '2026-03-19' }
    // expect gates[2] = { name: 'Final Validation', afterStage: 9, status: 'not-reached', approved: null }
  })
})

describe('parseNextAction', () => {
  it('returns the next action string from Session Continuity section', () => {
    // TODO
    // expect(parseNextAction(fixture)).toBe('Run /knz-modules to build module structure')
  })
  it('returns null when Next Action is missing', () => {
    // TODO
  })
})
```
IMPORTANT: Write stubs with commented-out assertions so tests PASS (empty test bodies) but describe the expected behavior. The TODO comments make expected behavior explicit before implementation in Plan 03.

4. Write src/lib/workspace-loader.test.ts — stubs for file discovery:
```typescript
import { describe, it } from 'vitest'

describe('discoverActiveProject', () => {
  it('returns the project with most recent STATE.md mtime', () => {
    // TODO: mock /workspace-index endpoint response
    // sort by stateMtime descending → return first result
  })
  it('returns null when no projects exist', () => {
    // TODO
  })
})

describe('listStageFiles', () => {
  it('returns filenames for a valid stage directory', () => {
    // TODO: mock /workspace-files/{project}/{stage} response
  })
  it('returns empty array for stage with no files', () => {
    // TODO
  })
})
```

5. Write src/lib/use-workspace-poll.test.ts — stubs for polling hook:
```typescript
import { describe, it } from 'vitest'

describe('useWorkspacePoll', () => {
  it('calls fetch on mount and sets state with parsed result', () => {
    // TODO: renderHook with vi.useFakeTimers
  })
  it('re-fetches at 3-second intervals', () => {
    // TODO: advance timers, verify fetch called multiple times
  })
  it('clears interval on unmount — no memory leak', () => {
    // TODO: unmount, advance timers, verify no further fetch calls
  })
})
```

6. Write vite-plugins/generate-html.test.ts — stubs for HTML generation:
```typescript
import { describe, it } from 'vitest'

describe('generateHtmlFromWorkspace', () => {
  it('writes an .html file to delivery/ for each .md file in stage dirs', () => {
    // TODO: create temp workspace with mock project, run generate, verify delivery/ contains html
  })
  it('generated HTML contains heading text from source markdown', () => {
    // TODO: verify <h1> or <h2> from md file appears in generated html
  })
  it('strips YAML frontmatter before HTML conversion', () => {
    // TODO: md file with --- frontmatter → html should not contain ---
  })
})
```

Run `npx vitest run --reporter=dot` — all tests should show as passing (empty stubs) or skipped. Zero failures expected at this stage.
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-curriculum-dashboard && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>npx vitest run exits with 0 failures. All 4 test files discovered by Vitest. src/test/fixtures/STATE.md contains the Stage Progress table with 9 stages.</done>
</task>

</tasks>

<verification>
- knz-curriculum-dashboard/ directory exists at the correct sibling path
- package.json contains all required dependencies at correct versions
- npm install succeeds (node_modules/ present)
- vite.config.ts references correct workspace path in comment
- npx vitest run finds 4 test files and exits with 0 failures
- src/test/fixtures/STATE.md matches the real workspace STATE.md format
</verification>

<success_criteria>
Project directory is bootstrapped with correct dependencies, Vitest finds all 4 test files, and all stubs pass (empty bodies). A fresh npm run dev serves on port 3002 without errors. All Wave 0 gaps from 04-VALIDATION.md are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-mvp/04-01-SUMMARY.md`
</output>
