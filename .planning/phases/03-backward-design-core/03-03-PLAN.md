---
phase: 03-backward-design-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/knz-assessments.md
  - .claude/commands/knz-outcomes.md
  - workspace/test-program/02-assessments/formative-assessments.md
autonomous: true
gap_closure: true
requirements:
  - PIPE-05
  - OUTC-01
  - OUTC-02
  - OUTC-03
  - OUTC-04
  - ASMT-01
  - ASMT-02
  - ASMT-03
  - ASMT-04

must_haves:
  truths:
    - "FA-2 in formative-assessments.md has format: written (not oral) — virtual delivery requires this substitution"
    - "FA-2 instructions no longer describe a partner verbal-exchange activity — they describe a written equivalent appropriate for async/virtual delivery"
    - "The transparency note in formative-assessments.md or assessment-map.md records that FA-2 was substituted from oral to written for virtual delivery"
    - "knz-outcomes.md Constraint Enforcement Step 2 enforces max(duration_scaled_minimum, 4) as the Bloom span floor — a medium program can no longer pass with only 3 levels"
    - "The Schema Compliance Checklist in knz-outcomes.md reflects the 4-level global minimum floor, not duration-scaled values alone"
  artifacts:
    - path: "workspace/test-program/02-assessments/formative-assessments.md"
      provides: "Corrected FA-2 entry — format changed from oral to written"
      contains: "format: written"
    - path: ".claude/commands/knz-assessments.md"
      provides: "Step 6 root cause investigation — substitution logic confirmed or clarified so it fires reliably"
    - path: ".claude/commands/knz-outcomes.md"
      provides: "Updated Constraint Enforcement Step 2 — Bloom span floor is max(duration_scaled, 4)"
  key_links:
    - from: ".claude/commands/knz-outcomes.md"
      to: ".claude/reference/schemas/stage-02-outcomes.md"
      via: "Constraint Enforcement Step 2 — global 4-level floor from schema line 122"
      pattern: "max.*duration.*4|at least 4"
    - from: "workspace/test-program/02-assessments/formative-assessments.md"
      to: ".claude/reference/schemas/stage-03-assessments.md"
      via: "FA-2 format value matches substitution map for virtual/oral → written"
      pattern: "format: written"
---

<objective>
Surgical gap closure for two enforcement correctness issues found during Phase 3 verification: (1) FA-2 in the test workspace has format: oral despite the program being virtual — the output file needs correction and the root cause in the command's Step 6 needs to be hardened; (2) the knz-outcomes Bloom span enforcement allows medium programs to pass with 3 levels, but the schema's global floor is 4.

Purpose: Phase 4 and Phase 5 depend on this pipeline's output being correct. These are enforcement logic gaps that would silently produce invalid curriculum for any medium-duration program with exactly 3 Bloom levels or any virtual program with oral assessments.
Output: Corrected formative-assessments.md, hardened Step 6 in knz-assessments.md, and updated Step 2 in knz-outcomes.md.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backward-design-core/03-VERIFICATION.md

<interfaces>
<!-- Exact current state of the two enforcement sections being fixed. -->

From .claude/commands/knz-assessments.md — Step 6 as written (correct logic, but did not fire in the verification run):
```markdown
**Step 6 — Delivery format compatibility check:**

Read `modality` from Stage 01 intake. If `modality` = `virtual`: check every assessment format against the exclusion list in `stage-03-assessments.md` Delivery Format Constraints. For any assessment using `oral` or `demonstration`: auto-substitute using the substitution map, keeping the same bloom_level and paired_objective. Record the substitution (e.g., "Substituted `oral` → `performance-based` for FA-3 — oral format is not practical for virtual delivery"). If `modality` = `blended`: flag (do not auto-substitute) any `oral` or `demonstration` assessments without a module_placement that can be confirmed as an in-person session — include a note in the transparency section asking the user to confirm placement.

**Step 7 — Record changes:**
```

From .claude/reference/schemas/stage-03-assessments.md — substitution map for virtual:
```
oral → written (for knowledge/comprehension outcomes) or performance-based (for Apply and above)
```
FA-2 is Understand-level → substitution is oral → written.

From .claude/commands/knz-outcomes.md — Step 2 as written (enforces 3-level minimum for medium programs):
```markdown
**Step 2 — Thinking-level distribution check:**

Count the number of unique `bloom_level` enum values used across all objectives. Compare against the duration-scaled minimum for this program's `contact_hours`.

If unique levels < minimum: auto-add objectives at the missing levels...
```

Duration-scaled minimums table (in Generation section, same file):
```
| Short (< 2 hr) | 2 |
| Medium (2–16 hr) | 3 |
| Long (> 16 hr) | 4 |
```

Schema line 122 (global floor): "A complete program must span at least 4 of the 6 Bloom's taxonomy levels."

Reconciliation required: max(duration_scaled_minimum, 4). For short programs (<2 hr), this means 4. For medium, 4. For long, 4. The duration-scaled table values for short and medium are below the schema's global floor.

From .claude/commands/knz-outcomes.md — Schema Compliance Checklist (current, needs updating):
```markdown
- [ ] Thinking-level span meets duration-scaled minimum (short=2, medium=3, long=4) — count unique levels
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Step 6 in knz-assessments.md and correct FA-2 in formative-assessments.md</name>
  <files>.claude/commands/knz-assessments.md, workspace/test-program/02-assessments/formative-assessments.md</files>
  <action>
Two fixes in this task — they touch different files and are independent, but both address the same FA-2 oral/virtual gap.

**Fix A — Harden Step 6 in knz-assessments.md:**

The root cause of Step 6 not firing: the step instructs Claude to read modality "from Stage 01 intake" but the Generation section already reads modality from project-brief.md (line: `Read from workspace/*/00-project-brief/project-brief.md: skill_type, contact_hours, modality`). The Step 6 instruction's phrasing "Read modality from Stage 01 intake" is ambiguous — it does not specify the file path. If the executor reads modality from STATE.md Key Decisions instead of project-brief.md, it may not find the field and silently skip the check.

Make Step 6 unambiguous: change the opening instruction to reference the exact file path already used in the Generation section. Also add an explicit instruction that if modality cannot be confirmed as non-virtual, treat it as virtual (fail-safe direction).

Replace the current Step 6 opening line with:

```
Read `modality` from `workspace/{project-name}/00-project-brief/project-brief.md` (same file read in Generation section).
```

Keep the rest of Step 6 exactly as written — the substitution logic is correct. Do not change Step 7.

Also add one explicit behavioral anchor to the substitution instruction to prevent the executor from treating Step 6 as optional when the field is missing:

After "Record the substitution..." add:

```
If `modality` cannot be read (field missing or file not found): treat as `virtual` and apply the virtual exclusion check. Do not skip this step.
```

**Fix B — Correct FA-2 in workspace/test-program/02-assessments/formative-assessments.md:**

Change FA-2's format field from `oral` to `written`.

Current FA-2 entry (lines 11-18):
```markdown
## FA-2: Profit vs. Cash Oral Explanation
- **paired_objective:** SO-1-1-2
- **bloom_level:** Understand
- **format:** oral
- **module_placement:** Module 1 / Session 1 (mid)
- **instructions_for_learner:** You'll be given a one-paragraph scenario about a fictional business: they had a profitable quarter but their bank account is dangerously low. Explain to your partner — in plain language — why that can happen. You have 3 minutes to explain; your partner has 2 minutes to ask clarifying questions.
- **success_criteria:** Explanation correctly identifies that timing differences between invoicing and collection can create a gap between profit and cash; uses at least one specific mechanism from the scenario (e.g., unpaid invoices, early expenses); partner can paraphrase the explanation back accurately.
```

Replace with:
```markdown
## FA-2: Profit vs. Cash Written Explanation
- **paired_objective:** SO-1-1-2
- **bloom_level:** Understand
- **format:** written
- **module_placement:** Module 1 / Session 1 (mid)
- **instructions_for_learner:** You'll be given a one-paragraph scenario about a fictional business: they had a profitable quarter but their bank account is dangerously low. Write 2–3 sentences in plain language explaining why that can happen. Focus on the mechanism — not just that "cash and profit are different" but why the gap occurs.
- **success_criteria:** Written response correctly identifies that timing differences between invoicing and collection can create a gap between profit and cash; uses at least one specific mechanism from the scenario (e.g., unpaid invoices, early expenses); explanation is legible to someone without accounting vocabulary.
```

Note: The title also changes from "Profit vs. Cash Oral Explanation" to "Profit vs. Cash Written Explanation" to match the corrected format. The success criteria removes the partner paraphrase criterion (which requires synchronous verbal interaction) and replaces it with a written-output equivalent.
  </action>
  <verify>
    <automated>grep -n "format:" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/workspace/test-program/02-assessments/formative-assessments.md | head -5</automated>
  </verify>
  <done>FA-2 in formative-assessments.md has format: written (not oral). The FA-2 title, instructions, and success criteria are updated to written-format equivalents. Step 6 in knz-assessments.md specifies the exact file path for modality and includes the fail-safe instruction for missing modality field.</done>
</task>

<task type="auto">
  <name>Task 2: Update Bloom span floor in knz-outcomes.md to max(duration_scaled, 4)</name>
  <files>.claude/commands/knz-outcomes.md</files>
  <action>
The schema's global rule is: a complete program must span at least 4 Bloom levels. The command currently enforces 3 for medium programs. Fix: apply max(duration_scaled_minimum, 4) as the effective floor.

This requires two changes in knz-outcomes.md:

**Change 1 — Constraint Enforcement Step 2:**

Current text in Step 2:
```
Count the number of unique `bloom_level` enum values used across all objectives. Compare against the duration-scaled minimum for this program's `contact_hours`.
```

Replace with:
```
Count the number of unique `bloom_level` enum values used across all objectives. Determine the effective minimum: take the duration-scaled minimum for this program's `contact_hours` and apply a global floor of 4.

Effective minimums:
| Program Size | Duration-scaled | Global floor | Enforced minimum |
|---|---|---|---|
| Short (< 2 hr) | 2 | 4 | 4 |
| Medium (2–16 hr) | 3 | 4 | 4 |
| Long (> 16 hr) | 4 | 4 | 4 |

Use the Enforced minimum column. The global floor of 4 is the schema requirement (stage-02-outcomes.md) — the duration-scaled values are starting guidance, not the ceiling.
```

Keep the rest of Step 2 exactly as written — the auto-add logic for missing levels is correct.

**Change 2 — Thinking-level span minimums table (in Generation section):**

The Generation section contains this table:
```markdown
| Program Size | Minimum unique thinking levels |
|---|---|
| Short (< 2 hr) | 2 |
| Medium (2–16 hr) | 3 |
| Long (> 16 hr) | 4 |
```

Replace with:
```markdown
| Program Size | Minimum unique thinking levels |
|---|---|
| Short (< 2 hr) | 4 (global floor — schema requirement) |
| Medium (2–16 hr) | 4 (global floor — schema requirement) |
| Long (> 16 hr) | 4 |
```

Add a note below the table:
```
Note: The global minimum of 4 thinking levels applies to all program sizes. Short and medium programs must still span 4 levels even if fewer sessions might suggest otherwise — this requires deliberate distribution of objectives across levels at design time.
```

**Change 3 — Schema Compliance Checklist:**

Current checklist entry:
```
- [ ] Thinking-level span meets duration-scaled minimum (short=2, medium=3, long=4) — count unique levels
```

Replace with:
```
- [ ] Thinking-level span meets the 4-level global minimum (enforced for all program sizes per schema requirement) — count unique bloom_level values across all objectives; must be >= 4
```
  </action>
  <verify>
    <automated>grep -n "Enforced minimum\|global floor\|4-level global" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/commands/knz-outcomes.md</automated>
  </verify>
  <done>knz-outcomes.md Constraint Enforcement Step 2 enforces the 4-level global floor for all program sizes. The Generation section thinking-level span table reflects the enforced minimums. The Schema Compliance Checklist states the 4-level global minimum. A medium program that generates only 3 unique Bloom levels will trigger auto-add behavior, not pass the constraint check.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "format:" workspace/test-program/02-assessments/formative-assessments.md` — FA-2 row shows `format: written`, no `format: oral` anywhere in the file
2. `grep -n "oral" workspace/test-program/02-assessments/formative-assessments.md` — zero results (the word "oral" no longer appears in this file)
3. `grep -n "global floor\|Enforced minimum" .claude/commands/knz-outcomes.md` — both terms appear in Step 2
4. `grep -n "4-level global" .claude/commands/knz-outcomes.md` — appears in Schema Compliance Checklist
5. `grep -n "modality.*00-project-brief\|project-brief.*modality\|not found.*virtual" .claude/commands/knz-assessments.md` — Step 6 updated phrasing is present
</verification>

<success_criteria>
- workspace/test-program/02-assessments/formative-assessments.md: FA-2 format is `written`; title, instructions, and success criteria are written-format equivalents; no `oral` anywhere in file
- .claude/commands/knz-assessments.md: Step 6 specifies exact file path (`workspace/{project-name}/00-project-brief/project-brief.md`) for modality read; includes fail-safe for missing modality field
- .claude/commands/knz-outcomes.md: Step 2 enforces `max(duration_scaled_minimum, 4)` as the floor; Generation section table shows enforced minimums for all sizes; Schema Compliance Checklist references the 4-level global minimum
- A medium program with exactly 3 unique Bloom levels would now fail Step 2 and trigger auto-add behavior
- A virtual program generating an `oral` assessment would now have it substituted by the hardened Step 6
</success_criteria>

<output>
After completion, create `.planning/phases/03-backward-design-core/03-03-SUMMARY.md`
</output>
