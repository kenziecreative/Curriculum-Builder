---
phase: 03-backward-design-core
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .claude/commands/knz-assessments.md
autonomous: false
requirements:
  - ASMT-01
  - ASMT-02
  - ASMT-03
  - ASMT-04
  - PIPE-05

must_haves:
  truths:
    - "Running /knz-assessments against a completed-Stage-2 workspace generates assessments where every objective has a paired assessment"
    - "The assessment Bloom's level matches or exceeds the paired objective's Bloom's level in every row of the alignment map"
    - "At least one formative assessment exists per module in the generated output"
    - "Every assessment has a success_criteria_for_assessment field with observable verbs"
    - "Misaligned assessments (Bloom level below objective) are auto-elevated before the alignment map is shown"
    - "The PIPE-05 gate appears as an AskUserQuestion with approve/concern/start-over options"
    - "Stage 4 is blocked until the Post-Assessment gate is approved — STATE.md Post-Assessment gate shows approved only after explicit user confirmation"
    - "Files are written to 02-assessments/ only on gate approval — never before"
  artifacts:
    - path: ".claude/commands/knz-assessments.md"
      provides: "/knz-assessments command implementing assessment generation and PIPE-05 inline gate"
      contains: "AskUserQuestion"
  key_links:
    - from: ".claude/commands/knz-assessments.md"
      to: ".claude/reference/schemas/stage-03-assessments.md"
      via: "Load instruction in generation section"
      pattern: "Load.*stage-03-assessments"
    - from: ".claude/commands/knz-assessments.md"
      to: "workspace/*/STATE.md"
      via: "Post-Assessment gate write on approval"
      pattern: "Post-Assessment.*approved"
    - from: ".claude/commands/knz-assessments.md"
      to: "workspace/*/01-outcomes/learning-objectives.md"
      via: "Read all objective outcome_ids for paired_objective field"
      pattern: "learning-objectives"
---

<objective>
Build the /knz-assessments command that generates a complete assessment suite from Stage 2 outcomes, enforces Bloom's alignment and formative coverage before displaying results, and ends with the PIPE-05 human review gate that must be approved before Stage 4 (module structure) can begin. Then verify the full Stage 2→3 pipeline against the test workspace.

Purpose: This command is the enforcement point for PIPE-05 — the gate that prevents Stage 4 from starting without explicit human approval. Getting the gate logic and STATE.md write timing exactly right is the most critical correctness requirement.
Output: knz-assessments.md command file + confirmed working Stage 2→3 pipeline verified against test workspace.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-backward-design-core/03-CONTEXT.md
@.planning/phases/03-backward-design-core/03-RESEARCH.md
@.planning/phases/03-backward-design-core/03-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from existing files. -->

From .claude/commands/knz-approve.md — three-option gate structure (established Phase 2 pattern):
```markdown
Use `AskUserQuestion` with three options:
- **"Approve and continue"**
- **"I have concerns"**
- **"Start this stage over"**
```

From templates/project-scaffold/STATE.md — Review Gates table with Post-Assessment row:
```markdown
## Review Gates
| Gate | After Stage | Status | Approved |
|------|-------------|--------|----------|
| Post-Intake | 1 | not-reached | - |
| Post-Assessment | 3 | not-reached | - |
| Final Validation | 9 | not-reached | - |
<!-- Gate status enums: not-reached | pending | approved -->
```

Gate status progression for Post-Assessment:
- not-reached → pending: when the alignment map is first presented to the user
- pending → approved: ONLY on "Approve and continue" selection
- "I have concerns" and "Start this stage over" branches: never advance gate status

From .claude/reference/schemas/stage-03-assessments.md — assessment alignment map format:
```markdown
## Assessment Alignment Map

| Outcome ID | Outcome (abbreviated) | Assessment ID | Assessment Name | Type | Bloom Match |
|------------|----------------------|---------------|-----------------|------|-------------|
| PO-1       | ...                  | SA-1          | ...             | summative | Evaluate >= Evaluate |
| MO-1-1     | ...                  | FA-1          | ...             | formative | Apply >= Apply |
```

From .claude/reference/schemas/stage-03-assessments.md — output files:
```
02-assessments/assessment-map.md
02-assessments/formative-assessments.md
02-assessments/summative-assessments.md
```

From .claude/reference/schemas/stage-03-assessments.md — skill-type constraints:
```
closed: at least one procedural or demonstration assessment per major objective
open: at least one performance-based or simulation assessment per major objective
```

Bloom's level ordering (for auto-elevation comparison):
1=Remember, 2=Understand, 3=Apply, 4=Analyze, 5=Evaluate, 6=Create
Auto-elevation: if assessment_bloom_level < paired_objective_bloom_level, elevate assessment to next level >= objective level

Critical timing rule (from CONTEXT.md and RESEARCH.md):
STATE.md stage progression writes happen ONLY in the "Approve and continue" branch.
"I have concerns" and "Start over" branches never advance stage status.
Gate status moves: not-reached → pending when alignment map is first presented; pending → approved ONLY on final approval.
Files are written ONLY in the approval branch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build /knz-assessments command with PIPE-05 gate</name>
  <files>.claude/commands/knz-assessments.md</files>
  <action>
Create .claude/commands/knz-assessments.md following the established section structure from knz-intake.md and knz-outcomes.md. This command generates assessments from Stage 2 outcomes with silent auto-correction, shows the alignment map as evidence, and ends with the PIPE-05 gate (inline AskUserQuestion) that unlocks Stage 4.

**Frontmatter:**
```yaml
---
description: Generate assessments paired to every learning outcome — Bloom's alignment enforced, formative coverage required, inline PIPE-05 review gate before Stage 4 can begin
---
```

**Section structure:**

### # /knz-assessments
Opening line: what this command does.

### ## Prerequisites

**1. Check workspace exists**
Read workspace/*/STATE.md. If none found: "It looks like you haven't set up a project workspace yet. Run /knz-init first to get started." Stop.

**2. Check Stage 2 prerequisite**
Read Stage 2 row from STATE.md Stage Progress table.
If Stage 2 != `complete`: "Assessment design starts after outcomes are finalized. Run /knz-outcomes first." Stop.

**3. Check Stage 3 status**
Read Stage 3 row from STATE.md Stage Progress table.
- `not-started` — proceed to Generation section
- `in-progress` — re-display alignment map if 02-assessments/ files exist; proceed to PIPE-05 Gate section
- `complete` AND Post-Assessment gate = `approved` — respond: "Assessments are approved. Stage 4 is next — run /knz-modules to build the module structure." Stop.
- `complete` but Post-Assessment gate != `approved` — surface the pending gate: "Your assessments are ready for review." Show alignment map and proceed to PIPE-05 Gate section.

### ## Persona
Expert instructional designer persona — same as knz-outcomes.md. No ID vocabulary. Never say: Bloom's taxonomy, bloom_level, paired_objective, formative assessment, summative assessment, assessment_type, schema, enum. Use plain language: "complexity level," "check-in assessment," "final project," "how well it measures the skill."

### ## Generation

**Load and generate:**
Load `.claude/reference/schemas/stage-03-assessments.md` as generation context before generating. Read all required fields, enum values, skill-type constraints, formative/summative requirements, and duration scaling from the schema.

Read from workspace/*/01-outcomes/learning-objectives.md: all outcome_ids and their bloom_level values.
Read from workspace/*/00-project-brief/project-brief.md: skill_type, contact_hours.

Generate assessments silently — no running commentary. Generate:
- Every objective gets at least one assessment (100% coverage)
- skill_type constraints honored: open skills get at least one performance-based or simulation; closed skills get at least one procedural or demonstration
- At least one formative per module
- At least one summative at program level (unless contact_hours < 2)
- Every assessment has: assessment_id (FA-N or SA-N format), assessment_name (descriptive, not "Assessment 1"), paired_objective (real outcome_id from Stage 2), assessment_type (formative or summative), bloom_level (at or above paired objective), format (exact enum value), instructions_for_learner (specific enough for a facilitator to assign), success_criteria_for_assessment (observable verbs only)

**Duration scaling** (read contact_hours):
- Short (< 2 hours): 2–4 formative only; summative optional
- Medium (2–16 hours): 3–6 formative; 1–3 summative; full assessment-map required
- Long (> 16 hours): full matrix; summative at each major phase; portfolio or cumulative recommended

### ## Constraint Enforcement (runs before any output is shown)

Run this internal check sequence. Corrections are silent. Record what changed.

**Step 1 — Coverage check:**
Cross-reference every outcome_id from Stage 2 learning-objectives.md against generated assessments. For any objective without a paired assessment: auto-generate an assessment for it. Record that it was added.

**Step 2 — Bloom's alignment check:**
For every assessment, compare assessment bloom_level to paired_objective bloom_level using the ordinal scale:
1=Remember, 2=Understand, 3=Apply, 4=Analyze, 5=Evaluate, 6=Create

If assessment level < paired objective level: auto-elevate the assessment to the same level as the objective (or the next level up if that produces a better assessment format). Record the elevation.

**Step 3 — Formative coverage check:**
Verify at least one formative assessment exists per module (using module_placement field). If any module lacks a formative: auto-generate one. Record the addition.

**Step 4 — Success criteria quality check:**
Verify every success_criteria_for_assessment uses observable verbs. If any contain "understands," "knows," or "appreciates": rewrite using observable equivalents (correctly identifies, produces, demonstrates, completes, role-plays using). Record changes.

**Step 5 — Skill-type compliance check:**
If skill_type = open: verify at least one performance-based or simulation assessment among all assessments. If missing, add one. If skill_type = closed: verify at least one procedural or demonstration. If missing, add one.

**Step 6 — Record changes:**
Track: how many assessments were auto-generated, how many were elevated, what was changed in success criteria.

### ## Output Presentation

**Validation badge** (show only if no corrections were needed):
> All objectives paired — complexity levels aligned across all assessments.

**Transparency note** (show only if corrections were made — confident tone):
> I added [N] assessment(s) and elevated [N] assessment(s) to match their complexity levels. Example: "I added an assessment for MO-2-3 and elevated FA-4 to Evaluate to match its objective." Here's the complete picture:

**Then display the Assessment Alignment Map** — this IS the evidence for gate approval. Use the format from stage-03-assessments.md output spec:

```markdown
## Assessment Alignment Map

| Outcome ID | Outcome (abbreviated) | Assessment ID | Assessment Name | Type | Bloom Match |
|------------|----------------------|---------------|-----------------|------|-------------|
```

"Type" values in the table use plain language: "formative check" for formative, "final assessment" for summative. (Do not expose the enum values in the user-facing table.)
"Bloom Match" column shows the relationship (e.g., "Apply >= Apply", "Evaluate > Analyze") — this makes alignment legible without ID vocabulary.

After the alignment map, show a brief summary: "X formative checks throughout the program, Y summative assessments."

### ## PIPE-05 Gate

After displaying the alignment map, silently update STATE.md:
- `Review Gates` → Post-Assessment: `pending`

Then use AskUserQuestion with three options:

- **"Approve and continue"** — write files, update STATE.md, unlock Stage 4
- **"I have concerns"** — ask what's wrong, revise assessments, re-present alignment map, gate again
- **"Start this stage over"** — destructive confirmation gate

**Critical timing: STATE.md stage status writes happen ONLY in the "Approve and continue" branch. "I have concerns" and "Start this stage over" never advance stage or gate status.**

### ## On "Approve and continue"

1. Write three output files to workspace/{project-name}/02-assessments/:

   Load `.claude/reference/schemas/stage-03-assessments.md` as generation context before writing.

   **assessment-map.md**: Full alignment map table linking every Stage 02 objective to its assessment(s). Format per schema output spec.

   **formative-assessments.md**: Full specification for each formative assessment. Format per schema output spec (assessment_id, assessment_name, paired_objective, bloom_level, format, module_placement, instructions_for_learner, success_criteria).

   **summative-assessments.md**: Full specification for each summative assessment. Same format.

2. Silently update workspace/{project-name}/STATE.md:
   - `Stage Progress` → Stage 3 status: `complete`, Completed: {today's date}
   - `Review Gates` → Post-Assessment: `approved`, Approved: {today's date}
   - `Session Continuity` → **Next Action:** Run /knz-modules to build module structure

3. End with brief confirmation:
   > Assessments approved. Your program now has a complete outcome-to-assessment map. Next: `/knz-modules` to build the module structure.

### ## On "I have concerns"

Ask what's wrong:
> What would you like to change?

Take free-text feedback. Revise the FULL assessment set (not individual assessments) using the Stage 2 outcomes plus the user's feedback. Re-run all five constraint enforcement steps on the revised set. Re-display the validation badge or transparency note (if any corrections were needed), then the alignment map. Present the PIPE-05 Gate again. Do not update STATE.md Post-Assessment during this branch — it stays at `pending`.

### ## On "Start this stage over"

Use AskUserQuestion to confirm the destructive action:
> Are you sure? This will clear all the assessment work from this stage and start from the beginning.

Options: **"Yes, start over"** / **"Actually, keep what we have"**

On "Yes, start over":
- Do not write any files
- Reset Stage 3 status in STATE.md to `not-started`
- Reset Post-Assessment gate to `not-reached`
- Regenerate from scratch: full generation + full constraint enforcement + display alignment map + PIPE-05 gate

On "Actually, keep what we have": return to the alignment map and re-present PIPE-05 Gate AskUserQuestion.

### ## State Management Rules

All STATE.md reads and writes are silent. Never announce state updates.

Gate status update timing:
- Post-Assessment moves to `pending` when alignment map is first presented (before AskUserQuestion)
- Post-Assessment moves to `approved` ONLY when user selects "Approve and continue"
- "I have concerns" loop: gate stays at `pending`, stage status stays at current value
- "Start this stage over": both gate and stage status reset

Files written ONLY in the "Approve and continue" branch. Never write 02-assessments/ files before approval.

### ## Schema Compliance Checklist

Before writing any output file, verify internally:

- [ ] All three output files will be written: assessment-map.md, formative-assessments.md, summative-assessments.md
- [ ] Every assessment has: assessment_id (FA-N or SA-N format), assessment_name (descriptive), paired_objective (real outcome_id), assessment_type (formative or summative), bloom_level (>= paired objective), format (exact enum), instructions_for_learner (specific), success_criteria_for_assessment (observable verbs)
- [ ] Every outcome_id from Stage 2 appears in at least one row of assessment-map.md
- [ ] All assessment bloom_level values are >= their paired objective bloom_level (never <)
- [ ] At least one formative per module
- [ ] At least one summative at program level (unless contact_hours < 2)
- [ ] skill_type constraint honored: open skills have performance-based or simulation; closed skills have procedural or demonstration
- [ ] success_criteria_for_assessment uses observable verbs — never "understands" or "appreciates"
- [ ] Post-Assessment gate written to STATE.md as `approved` in the approval branch
  </action>
  <verify>
    <automated>MISSING — manual verification required. Run /knz-assessments against workspace/test-program/ after /knz-outcomes has been approved (Stage 2 complete). Verify: (1) prerequisite check passes (Stage 2 complete), (2) alignment map shows every outcome_id from learning-objectives.md with a paired assessment, (3) every Bloom Match entry shows >= not <, (4) AskUserQuestion PIPE-05 gate appears, (5) on approval: 02-assessments/ contains all three files, STATE.md Stage 3 = complete, Post-Assessment gate = approved.</automated>
  </verify>
  <done>knz-assessments.md exists in .claude/commands/. The command generates a complete assessment suite from Stage 2 outcomes, shows the alignment map with Bloom match column, presents the PIPE-05 gate via AskUserQuestion, and writes three files to 02-assessments/ only after approval. Post-Assessment gate in STATE.md is approved only after explicit user confirmation. Stage 4 is blocked until this gate is approved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full Stage 2-3 pipeline end-to-end</name>
  <what-built>
Complete Stage 2→3 pipeline: test workspace (Stage 1 complete), /knz-outcomes command, /knz-assessments command with PIPE-05 gate. Both commands exist in .claude/commands/.
  </what-built>
  <action>Run the full Stage 2→3 pipeline against workspace/test-program/ to confirm all requirements are met. This is human verification of the automated work in Plan 01 Task 2 and Plan 02 Task 1.</action>
  <how-to-verify>
Run the full Stage 2→3 pipeline against workspace/test-program/ (contact_hours=9, medium program, skill_type=open, Stage 2 - Interested self-direction level):

**Step 1 — Verify /knz-outcomes:**
1. Run `/knz-outcomes`
2. Confirm: command proceeds without being blocked (Stage 1 is complete)
3. Confirm: outcome hierarchy appears — program-level, module-level, and session-level outcomes
4. Confirm: distribution summary table appears using "thinking level" language — not "Bloom's taxonomy" anywhere in user-facing text
5. Confirm: AskUserQuestion gate appears with three options
6. Select "Looks good — write the outcomes"
7. Confirm: workspace/test-program/01-outcomes/ now contains enduring-understandings.md, essential-questions.md, learning-objectives.md
8. Inspect learning-objectives.md: no objective uses "understand," "know," or "appreciate" — all use observable verbs
9. Inspect learning-objectives.md: every objective has a transfer_context field (none blank)
10. Count unique Bloom levels in learning-objectives.md: should be >= 3 for a medium program
11. Confirm: STATE.md Stage 2 row now shows `complete`

**Step 2 — Verify /knz-assessments:**
1. Run `/knz-assessments`
2. Confirm: command proceeds without being blocked (Stage 2 is complete)
3. Confirm: assessment alignment map appears — every outcome_id from learning-objectives.md has a row
4. Confirm: Bloom Match column shows >= for every row (no row shows a value less than the objective)
5. Confirm: AskUserQuestion PIPE-05 gate appears with approve/concern/start-over options
6. Select "Approve and continue"
7. Confirm: workspace/test-program/02-assessments/ contains assessment-map.md, formative-assessments.md, summative-assessments.md
8. Inspect formative-assessments.md: at least one formative per module
9. Inspect summative-assessments.md: at least one summative at program level
10. Inspect both files: every entry has success_criteria field with observable verbs (no "understands")
11. Confirm: STATE.md Stage 3 row = `complete`, Post-Assessment gate = `approved`
12. Confirm: STATE.md Next Action = "Run /knz-modules to build module structure"
  </how-to-verify>
  <verify>Full Stage 2→3 pipeline manually verified — all 12 check points above pass.</verify>
  <done>Human confirms pipeline works end-to-end: outcomes generated and approved, assessments generated and approved, STATE.md Stage 2 and Stage 3 complete, Post-Assessment gate approved in STATE.md.</done>
  <resume-signal>Type "verified" if the full pipeline works end-to-end, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
After Task 1 (command file created) and Task 2 (human verification):
1. Both commands exist: .claude/commands/knz-outcomes.md, .claude/commands/knz-assessments.md
2. Full Stage 2→3 pipeline verified against workspace/test-program/
3. All nine requirements confirmed by manual inspection:
   - OUTC-01: every objective has bloom_level enum
   - OUTC-02: hierarchy has program, module, and session levels
   - OUTC-03: every objective has transfer_context
   - OUTC-04: Bloom span >= 3 for medium program
   - ASMT-01: every objective has paired assessment (alignment map 100% coverage)
   - ASMT-02: every assessment Bloom >= objective Bloom (all >= in Bloom Match column)
   - ASMT-03: at least one formative per module
   - ASMT-04: every assessment has success_criteria with observable verbs
   - PIPE-05: gate appears, Stage 3 advances to complete only after explicit approval
4. Post-Assessment gate = approved in STATE.md — Stage 4 is now unblockable
</verification>

<success_criteria>
- .claude/commands/knz-assessments.md exists with PIPE-05 gate as inline AskUserQuestion
- STATE.md Post-Assessment gate is approved only after explicit user selection of "Approve and continue"
- Files are written to 02-assessments/ only in the approval branch
- Auto-corrections (coverage, Bloom elevation, formative addition) run before display
- The alignment map is legible to a non-ID user (plain language, no schema vocabulary)
- Human verifier confirms the full Stage 2→3 pipeline works against the test workspace
</success_criteria>

<output>
After completion, create `.planning/phases/03-backward-design-core/03-02-SUMMARY.md`
</output>
