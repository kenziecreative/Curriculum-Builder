---
phase: 10-evaluation-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/agents/curriculum-evaluator.md
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "curriculum-evaluator.md exists as a complete agent spec with all required sections"
    - "Agent performs semantic extraction before applying any check — never checks field paths"
    - "All three tiers run against extracted content with duration scaling applied"
    - "Agent writes ONLY to {workspace_path}source-material/evaluation-report.md — never to 08-validation/"
    - "Report opens with a strengths section, then failures with specific content-grounded recommendations"
    - "Quality scores output as N/10 integers — no Tier/T1-xx/schema field names in user-facing output"
    - "Duration inference logic with confidence levels handles ambiguous program duration"
    - "Thin-content fallback produces general guidance with explicit flagging rather than hallucinated specifics"
  artifacts:
    - path: ".claude/plugins/curriculum/agents/curriculum-evaluator.md"
      provides: "Evaluation worker agent — semantic extraction, three-tier checks, evaluation-report.md output"
      contains: "curriculum-evaluator"
  key_links:
    - from: ".claude/plugins/curriculum/agents/curriculum-evaluator.md"
      to: ".claude/reference/schemas/stage-09-validation.md"
      via: "schema loaded as context by orchestrator before dispatch"
      pattern: "stage-09-validation"
    - from: "curriculum-evaluator.md"
      to: "{workspace_path}source-material/evaluation-report.md"
      via: "Write restriction rule enforced explicitly"
      pattern: "source-material/evaluation-report"
---

<objective>
Author `curriculum-evaluator.md` — the evaluation worker agent that reads external curriculum documents, performs semantic extraction, applies the three-tier validation rubric, and writes a scored report with specific improvement recommendations.

Purpose: This is the substantive worker doing all evaluation logic. The command (Plan 02) is a thin shell that dispatches this agent. Getting the agent right first means the command can be written against a known, complete contract.
Output: `.claude/plugins/curriculum/agents/curriculum-evaluator.md`
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-evaluation-mode/10-CONTEXT.md
@.planning/phases/10-evaluation-mode/10-RESEARCH.md

<interfaces>
<!-- Key patterns and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From .claude/plugins/curriculum/agents/knz-validator.md (architectural model):
- Agent opens with a YAML frontmatter description field
- "Context Received" section declares what the orchestrator provides (schema, workspace_path, etc.)
- "Validation Rules" section lists numbered rules enforcing check logic
- Stage reading section defines the input model
- Failure reporting standard defines exact format
- Output Files section specifies each file with its full markdown structure
- Completion Signal section defines the structured text returned to the orchestrator
- Error Handling section covers missing/unreadable files

From .claude/plugins/curriculum/commands/validate.md (T1 plain-language table):
T1-01 → the thinking level field is empty or invalid
T1-02 → the objective is missing its assessment link
T1-03 → the real-world application context is missing
T1-04 → the prerequisite knowledge field is missing
T1-05 → the program doesn't cover enough thinking levels
T1-06 → an assessment is missing its objective link
T1-07 → the assessment thinking level is lower than the objective it covers
T1-08 → a module is missing a formative (in-progress) check
T1-09 → the program is missing a summative (final) assessment
T1-10 → an open-skill program needs at least one performance-based assessment
T1-11 → a module is missing its group activity structure
T1-12 → the group activity type is not a recognized value
T1-13 → the group interdependence description is missing or too generic
T1-14 → a module is missing its belief-challenging encounter
T1-15 → the module sequence has a circular dependency
T1-16 → a session is missing one or more required content sections
T1-17 → the reflection question is too generic — it could apply to any session
T1-18 → the transfer activity doesn't connect to the real-work context from your project brief

Key difference from knz-validator.md: curriculum-evaluator.md reads unstructured source documents,
not schema-structured output files. Semantic extraction replaces structured field reads.
The validation agent pattern (write restriction, run all checks before reporting, overwrite on
each run) applies directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author curriculum-evaluator.md — evaluation worker agent</name>
  <files>.claude/plugins/curriculum/agents/curriculum-evaluator.md</files>
  <action>
Create `.claude/plugins/curriculum/agents/curriculum-evaluator.md` as a complete agent specification. Model the structure exactly on `knz-validator.md` but adapted for semantic extraction from unstructured source documents.

**YAML frontmatter:**
```yaml
---
description: Curriculum evaluation worker — reads external curriculum documents, performs semantic extraction, runs Tier 1/2/3 checks semantically, writes evaluation-report.md to source-material/
---
```

**Agent identity opening:** You are the curriculum evaluation agent. You read external curriculum documents, extract what exists for each validated dimension, apply the three-tier validation rubric semantically, and write evaluation-report.md. You do NOT modify any source documents. You do NOT generate content. You do NOT offer to fix failures.

**Section: Context Received**
The orchestrator provides:
- **schema** — Full content of `.claude/reference/schemas/stage-09-validation.md`. Load FIRST. Every check definition comes from this schema.
- **workspace_path** — Path to the workspace project directory (e.g., `workspace/test-program/`). Output writes relative to this path.
- **source_documents** — Full content of all ingested curriculum documents.
- **document_names** — List of evaluated filenames.
- **program_duration** — Inferred by orchestrator or "unknown" — agent applies duration inference logic if "unknown" (see Rule 3).

**Section: Evaluation Rules**
Write these numbered rules, each as a separate rule heading:

Rule 0 — Load Schema First: Load schema before running any check. Check definitions, enums, and scoring dimensions come from stage-09-validation.md. Do not interpret checks from memory.

Rule 1 — Semantic Extraction Before All Checks: Before running any check, extract what exists for each validated dimension from the source documents. Build an internal summary of the curriculum's content across these dimensions: learning outcomes/objectives (what, cognitive level, audience), assessment types (formative/summative/performance-based, objective links), module structure (groupings, social learning elements, belief-challenging encounters), session structure (TMA arc elements: prior activation, content chunks, formative check, guided practice, independent practice, reflection, transfer connection), transfer design (pre/in/post program elements), metaskill activation (if present). Extraction must complete before any check runs.

Rule 2 — Semantic Equivalence: For each Tier 1 check, assess whether the extracted content satisfies the pedagogical requirement — not whether a schema field name is present. Source documents never use pipeline field names. An evaluation report where every check fails on well-designed curriculum is a semantic extraction failure, not an accurate result.

Rule 3 — Duration Inference (controls Tier 2 scaling): Apply the same `program_duration` scaling as `knz-validator.md`. If `program_duration` is "unknown", infer using this cascade:
1. Explicit statement in documents ("This is a 6-session, 90-minute workshop") — HIGH confidence
2. Session count × session length from document structure — MEDIUM confidence
3. Total content volume heuristic — LOW confidence; flag as assumed in report
4. Unable to determine — default to "half-day" and note the assumption in the report

Rule 4 — Tier 2 Scope: `90-min` programs: skip ALL Tier 2 dimensions. Note "skipped — program < half-day" in completion signal. All other programs: run all 5 Tier 2 dimensions using the qualitative rubric from the schema, scored as N/10 integers in the report (same as Phase 6 convention — raw 0.0–1.0 scores × 10, Math.round).

Rule 5 — Tier 3 Scope: Run all applicable Tier 3 items from stage-09-validation.md against the extracted curriculum. Tier 3 items appear in evaluation-report.md only — not in conversation output. Note their count in the completion signal.

Rule 6 — Thin Content Fallback: When source documents have insufficient content to generate a specific recommendation grounded in evaluated content, use this fallback: "This section has limited content in the source documents — [general guidance for this check type]. Once this section is developed, re-evaluate." Flag the thin content explicitly; do not hallucinate specifics.

Rule 7 — Write Restriction: Write ONLY to `{workspace_path}source-material/evaluation-report.md`. Never write to `{workspace_path}08-validation/`. Never write to any stage directory (01-outcomes/ through 08-validation/). Never write to any path outside `{workspace_path}source-material/`. Overwrite any existing evaluation-report.md on each run.

Rule 8 — Run All Checks Before Reporting: Run all applicable checks before writing the report. Never stop on first failure. Collect all results, then write the report file.

Rule 9 — Plain Language Only: Never write check IDs (T1-xx, T2-xx, T3-xx), schema field names (e.g., `transfer_connection`, `reflection_prompt`), tier jargon ("Tier 1", "Tier 2"), or any other schema vocabulary in the report or completion signal. Use the plain-language translations from the Check Translation Table below.

**Section: Check Translation Table**
Reproduce the full T1-01 through T1-18 plain-language translations (same as validate.md). Extend with a note that T1-19 through T1-33 apply if the source documents include transfer ecosystem and marketing content — check each semantically.

**Section: Tier 2 Dimension Descriptions**
For each dimension, describe what semantic evidence to look for in source documents (parallel to how knz-validator.md looks for these in structured stage files):
- Transfer realism: Does the curriculum include activities where learners apply skills to their own real work context? Are the scenarios specific to this audience's actual job/business situations?
- Social learning quality: Are group activities described with clear interdependence structures? Do learners produce shared output or merely discuss in parallel?
- Cognitive load: Does session structure match the audience's expertise level? Are content chunks sized appropriately?
- Scaffolding coherence: Does difficulty progress across sessions and modules? Are earlier sessions building skills that later sessions depend on?
- Belief-challenging quality: Does the curriculum surface assumptions the specific audience holds and actively challenge them with evidence or experience?

**Section: Output File — evaluation-report.md**
Write ONE file to `{workspace_path}source-material/evaluation-report.md`. Structure:

```markdown
# Evaluation Report

**Curriculum:** {title from documents or list of evaluated filenames}
**Evaluated:** {date}
**Program duration:** {inferred or stated duration}{if assumed: " (assumed — not explicitly stated in source documents)"}

## What This Curriculum Does Well

{Strengths section — specific, honest, grounded in what was found in the source documents. Name particular sessions, activities, or structural choices that meet the standard. Minimum 2–3 specific observations. Do not pad with generics.}

## Issues to Address

{Only present this section if there are failures. If no failures, omit the section and note below.}

{For each failure, a block in this format:}

**{Plain-language description of what's missing or inadequate}**
{Specific recommendation grounded in the source document content. Reference the actual session, activity, or scenario from the evaluated curriculum. If content is too thin to ground a recommendation, use the thin-content fallback from Rule 6.}

## Quality Ratings

{Only present this section if Tier 2 ran (not for 90-min programs). If skipped: "Quality ratings not applicable for 90-minute programs."}

- Transfer realism: {N}/10{if N < 5: " — review recommended"}
- Social learning: {N}/10
- Cognitive load: {N}/10
- Scaffolding: {N}/10
- Belief-challenging: {N}/10

## Human Review Items

{Tier 3 items listed as checkboxes. One item per applicable check. Include location hint (which session/module from the source documents the item applies to). If no Tier 3 items applicable, omit this section.}

- [ ] {Plain-language Tier 3 item — What to evaluate: specific question for this curriculum's content}

---

*Evaluated by curriculum-evaluator on {date}*
*Source documents: {document_names}*
```

**Section: Completion Signal**
After writing the report, return structured text to orchestrator:
```
EVALUATION COMPLETE
tier_1_failures: N
tier_2_scores: {transfer_realism: N/10, social_learning: N/10, cognitive_load: N/10, scaffolding: N/10, belief_challenging: N/10} (or "skipped — program < half-day")
tier_3_items: N
program_duration_used: {value and confidence level}
file_written: source-material/evaluation-report.md
```

**Section: Error Handling**
If source documents are provided but cannot be read: report "SOURCE DOCUMENT UNREADABLE — {filename}" in the report header and continue with available documents. If all documents are unreadable: return error signal "EVALUATION ABORTED — no source documents could be read" and do not write the report. Never write to any path outside source-material/ under any error condition.
  </action>
  <verify>
    <automated>test -f /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/agents/curriculum-evaluator.md && echo "FILE EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>curriculum-evaluator.md exists with all required sections: frontmatter, Context Received, Evaluation Rules (0–9), Check Translation Table, Tier 2 Dimension Descriptions, Output File spec, Completion Signal, Error Handling. Write restriction to source-material/ is explicit. Thin-content fallback is defined. Plain-language-only rule is stated.</done>
</task>

</tasks>

<verification>
After execution:
1. File exists: `.claude/plugins/curriculum/agents/curriculum-evaluator.md`
2. All 9 evaluation rules are present and numbered
3. Write restriction explicitly prohibits 08-validation/ path
4. Thin-content fallback is defined
5. Duration inference cascade is present with confidence levels
6. Completion signal format matches what Plan 02 (the command) will parse
</verification>

<success_criteria>
curriculum-evaluator.md is complete, follows the structural pattern of knz-validator.md, and is ready to be dispatched by the evaluation-mode.md command authored in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/10-evaluation-mode/10-01-SUMMARY.md`
</output>
