---
phase: 10-evaluation-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/commands/evaluation-mode.md
autonomous: false
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "evaluation-mode.md exists as a complete command following the established orchestrator pattern"
    - "/curriculum:evaluate accepts file path arguments; auto-detects source-material/ files when no args given"
    - "Auto-detect explicitly filters evaluation-report.md from candidate file list"
    - "ONE Task dispatched with schema content, all source documents, workspace_path, and program_duration"
    - "Conversation output shows failures with plain-language descriptions; never shows check IDs or tier jargon"
    - "File verification confirms evaluation-report.md in source-material/ before showing any results"
    - "User verification with real-world test candidate confirms end-to-end behavior"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/evaluation-mode.md"
      provides: "Evaluation orchestrator command — workspace detection, arg parsing, agent dispatch, output presentation"
      contains: "curriculum:evaluate"
  key_links:
    - from: ".claude/plugins/curriculum/commands/evaluation-mode.md"
      to: ".claude/plugins/curriculum/agents/curriculum-evaluator.md"
      via: "Task dispatch instructions reference curriculum-evaluator.md by name"
      pattern: "curriculum-evaluator"
    - from: "evaluation-mode.md"
      to: "{workspace_path}source-material/evaluation-report.md"
      via: "File verification check after Task returns"
      pattern: "source-material/evaluation-report"
---

<objective>
Author `evaluation-mode.md` — the `/curriculum:evaluate` orchestrator command — and verify the full evaluation pipeline end-to-end with the real-world test candidate.

Purpose: The command is the user-facing entry point. It handles all routing (argument parsing, workspace detection, auto-detect fallback), dispatches the evaluation agent as a Task, presents plain-language results, and confirms the report file was written. A human verification checkpoint confirms the output meets the quality bar with real curriculum documents.
Output: `.claude/plugins/curriculum/commands/evaluation-mode.md`; end-to-end verified evaluation pipeline
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-evaluation-mode/10-CONTEXT.md
@.planning/phases/10-evaluation-mode/10-RESEARCH.md
@.planning/phases/10-evaluation-mode/10-01-SUMMARY.md

<interfaces>
<!-- Key patterns and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From .claude/plugins/curriculum/commands/validate.md (orchestrator structural model):
- YAML frontmatter with description field
- "Output Formatting" directive: Follow the `curriculum:interactive-design` skill
- Section: Prerequisites (workspace check → halt with message if missing; then stage-specific prereq check)
- Section: Dispatch — spawn ONE Task; provide context; give instructions; "Do not dispatch a second Task"
- Section: File Verification — check files exist after Task returns; halt with retry message if missing
- Section: Conversation Output — show plain-language summary; FAIL-rows only; never show check IDs
- Section: State Update — silent per INFR-10; specific field updates for pass/fail branches

From .claude/plugins/curriculum/commands/intake.md (Audit Mode, document ingestion pattern):
- Per-file progress announcement: "Reading [filename]..." (one line per file, no description during reading)
- Auto-detect with AskUserQuestion confirmation before proceeding
- AskUserQuestion options for the no-args fallback: "Yes, evaluate these files" / "Let me add more files first"

From .claude/plugins/curriculum/agents/curriculum-evaluator.md (completion signal to parse):
- tier_1_failures: N
- tier_2_scores: object or "skipped — program < half-day"
- tier_3_items: N
- program_duration_used: value and confidence
- file_written: source-material/evaluation-report.md

Workspace detection pattern (all commands):
- Read workspace/*/STATE.md via glob
- If none found: "It looks like you haven't set up a project workspace yet. Run `/curriculum:init` first to get started."

Real-world test candidate (for verification task):
- workspace/accessible-development-with-ai/source-material/workshop-outline-integrated.md
- workspace/accessible-development-with-ai/source-material/workshop-slides-outline.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author evaluation-mode.md — /curriculum:evaluate orchestrator command</name>
  <files>.claude/plugins/curriculum/commands/evaluation-mode.md</files>
  <action>
Create `.claude/plugins/curriculum/commands/evaluation-mode.md` as a complete command spec. Model structure exactly on `validate.md` but adapted for external document input. The command does no evaluation work — all checks happen in `curriculum-evaluator.md`.

**YAML frontmatter:**
```yaml
---
description: Run three-tier evaluation on external curriculum documents — accepts file paths or auto-detects from source-material/, dispatches evaluation agent, presents scored report with improvement recommendations
---
```

**Output Formatting directive** (same as all commands):
```
## Output Formatting

Follow the `curriculum:interactive-design` skill for all user-facing output — headings, tables, status indicators, and interaction patterns.
```

**Command identity:** `/curriculum:evaluate — Curriculum Evaluation Orchestrator`
Opening: You are the evaluation orchestrator. You check prerequisites, resolve input documents, dispatch the evaluation agent as a Task, verify the output file was written, and show a plain-language results summary. You do NOT run evaluation checks yourself — all evaluation happens in the evaluation worker.

**Section: Prerequisites**
1. Check workspace exists — read `workspace/*/STATE.md` via glob. If no STATE.md found: "It looks like you haven't set up a project workspace yet. Run `/curriculum:init` first to get started." Stop. Do not proceed.
2. No pipeline stage required — evaluation mode runs independently of pipeline status. Skip the stage gate that other commands use.

**Section: Input Resolution**

Step 1: Parse `$ARGUMENTS`.

**If ARGUMENTS were provided** (e.g., `/curriculum:evaluate path/to/guide.md path/to/workbook.md`):
Read each file at the provided paths. As each file is read, announce exactly one line: `Reading [filename]...`
Do not wait until all files are read. Do not describe what you find during reading.
Store all file contents as source_documents.

**If no ARGUMENTS were provided:**
Check `workspace/{project}/source-material/` for any files.
EXCLUDE `evaluation-report.md` from the candidate list — this file is output from a previous evaluation run, not curriculum to evaluate.
Also exclude any file named `evaluation-report.md` regardless of casing or location in source-material/.

If curriculum files are found (after exclusion), list them and use `AskUserQuestion`:

> I found these files in your source-material folder:
> - [filename 1]
> - [filename 2]
>
> Should I evaluate these?

Options:
- **"Yes, evaluate these files"** — read all listed files; announce `Reading [filename]...` per file
- **"Let me add more files first"** — respond: "Drop your files into `workspace/{project}/source-material/` and run `/curriculum:evaluate` again when ready." Stop.

If source-material/ is empty or contains only `evaluation-report.md`:

> I didn't find any curriculum documents to evaluate. You can either:
> - Drop files into `workspace/{project}/source-material/` and re-run `/curriculum:evaluate`
> - Run `/curriculum:evaluate` with file paths directly: `/curriculum:evaluate path/to/your/guide.md`

Stop. Do not proceed.

**Section: Dispatch**

Spawn ONE Task with the following:

**Description:** Run curriculum evaluation per the curriculum-evaluator.md agent specification

**Context provided to the Task:**
- Full content of `.claude/reference/schemas/stage-09-validation.md`
- Full content of all ingested source documents (collected in Input Resolution)
- `workspace_path`: `workspace/{project}/`
- `document_names`: list of evaluated filenames
- `program_duration`: "unknown" (agent will infer from source documents)

**Instructions:**
> You are the curriculum evaluation agent. Follow curriculum-evaluator.md exactly. All source documents are in your context. Extract what exists for each validated dimension, then apply Tier 1 checks semantically. Run Tier 2 rubric scoring per duration scaling rules — skip all dimensions for 90-min programs. Run all applicable Tier 3 checks. Write evaluation-report.md to {workspace_path}source-material/. Return a completion signal with tier_1_failures count, tier_2_scores (or "skipped"), tier_3_items count, program_duration_used, and file_written.

Do not dispatch a second Task. Do not run evaluation logic in this orchestrator command.

**Section: File Verification**

After the Task returns its completion signal, verify:
- `workspace/{project}/source-material/evaluation-report.md` — always required

If the file is missing:

> The evaluation didn't complete — the report file wasn't written. Run `/curriculum:evaluate` again to retry.

Do not show results. Stop here.

If the file is verified, proceed to Conversation Output.

**Section: Conversation Output**

Parse the completion signal to get `tier_1_failures`, `tier_2_scores`, `tier_3_items`, and `program_duration_used`.

Also read `workspace/{project}/source-material/evaluation-report.md` to access the strengths section and full failure/recommendation details.

Show the user the following. Never show check IDs (T1-xx), tier jargon ("Tier 1", "Tier 2"), or schema field names.

**Header — always show:**
```
Evaluation complete. Report saved to source-material/evaluation-report.md.
```

If `program_duration_used` includes "assumed" (LOW confidence inference):
```
Note: I estimated this as a {duration} program based on document structure. If that's wrong, the quality ratings may not apply — re-run with a document that states the program length explicitly.
```

**Strengths — always show** (pull from report strengths section or summarize):
```
**What this curriculum does well:**
{2–3 bullet summary of strengths found — grounded in the evaluated documents}
```

**Failures section — show only if tier_1_failures > 0:**
```
**{N} issue{s} to address:**
{For each failure: plain-language description + specific recommendation}
```

Use the same T1 plain-language translations as `validate.md`. Never show "T1-16", "T1-07", etc.

**Quality ratings — show only if Tier 2 ran (not for 90-min):**
```
**Quality ratings:**
- Transfer realism: {N}/10{if N < 5: " — review recommended"}
- Social learning: {N}/10
- Cognitive load: {N}/10
- Scaffolding: {N}/10
- Belief-challenging: {N}/10
```

**Tier 3 items — show count only, not items:**
```
{tier_3_items} item{s} require human judgment — see evaluation-report.md for the full checklist.
```
(Omit this line if tier_3_items == 0.)

**If tier_1_failures == 0:**
```
No required elements are missing.
```

**Section: State Management Rules**
All STATE.md reads and writes are silent. Evaluation mode does NOT update pipeline stage status. Never write to STATE.md. Evaluation is not a pipeline stage — evaluation-report.md is its own artifact. Never announce file reads or workspace detection.

Never say: "Updating STATE.md", "Saving progress", "Writing to disk", "I'm recording that", "Logging this"
  </action>
  <verify>
    <automated>test -f /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/evaluation-mode.md && echo "FILE EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>evaluation-mode.md exists with all required sections: frontmatter, Output Formatting directive, Prerequisites, Input Resolution (with auto-detect and evaluation-report.md exclusion), Dispatch, File Verification, Conversation Output (with T1 plain-language translations and tier display rules), State Management Rules.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end evaluation pipeline with real-world test candidate</name>
  <files>workspace/accessible-development-with-ai/source-material/evaluation-report.md</files>
  <action>Run /curriculum:evaluate against the real-world test candidate documents and verify output format, routing logic, and file writes meet all acceptance criteria.</action>
  <verify>
    <automated>MISSING — all verification is manual slash command execution; see how-to-verify below</automated>
  </verify>
  <done>All four tests pass: file-path argument mode produces evaluation-report.md in source-material/; auto-detect excludes evaluation-report.md from candidate list; no check IDs or tier jargon appear in any output; report was not written to 08-validation/.</done>
  <what-built>
    Two new files completing Phase 10:
    - `.claude/plugins/curriculum/commands/evaluation-mode.md` — the /curriculum:evaluate command
    - `.claude/plugins/curriculum/agents/curriculum-evaluator.md` — the evaluation worker agent
  </what-built>
  <how-to-verify>
Run the following verifications in a fresh Claude Code context (/clear first):

**Test 1 — File path argument mode:**
```
/curriculum:evaluate workspace/accessible-development-with-ai/source-material/workshop-outline-integrated.md workspace/accessible-development-with-ai/source-material/workshop-slides-outline.md
```
Expected: Per-file "Reading..." announcements, ONE Task dispatched, evaluation-report.md written to workspace/accessible-development-with-ai/source-material/, conversation output with strengths section + any failures + quality ratings (or skipped for 90-min), Tier 3 item count shown.

**Test 2 — Auto-detect fallback (no arguments):**
After Test 1 has run (so evaluation-report.md exists in source-material/):
```
/curriculum:evaluate
```
Expected: Command detects files in source-material/, lists only the curriculum documents (NOT evaluation-report.md), shows AskUserQuestion with "Yes, evaluate these files" and "Let me add more files first" options.

**Test 3 — Format compliance checks:**
Review the conversation output and evaluation-report.md for:
- [ ] No check IDs (T1-xx, T2-xx, T3-xx) visible anywhere in conversation or report
- [ ] No schema field names (transfer_connection, reflection_prompt, etc.) in user-facing output
- [ ] Quality scores shown as N/10 integers, not decimals
- [ ] Strengths section present and specific (not generic)
- [ ] Any failures include specific recommendations referencing the workshop content
- [ ] Report written to source-material/, NOT to 08-validation/
- [ ] No pipeline stage required — command ran without any prior /curriculum:outcomes, /curriculum:sessions, etc.

**Test 4 — Empty source-material fallback (optional):**
In a workspace where source-material/ has no curriculum files (or contains only evaluation-report.md), run /curriculum:evaluate with no args. Expected: informative message about where to add files, no crash.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
Phase 10 complete when:
1. Both new files exist: commands/evaluation-mode.md and agents/curriculum-evaluator.md
2. Human verification passed: evaluation produces a complete, specific report against real workshop materials
3. evaluation-report.md written to source-material/, never to 08-validation/
4. No tier jargon or check IDs in any user-facing output
5. Auto-detect correctly excludes evaluation-report.md from candidate list
</verification>

<success_criteria>
A user can run /curriculum:evaluate against external curriculum documents and receive a scored report with specific improvement recommendations — without touching the pipeline. The report format is honest, specific, and grounded in the evaluated documents' actual content.
</success_criteria>

<output>
After completion, create `.planning/phases/10-evaluation-mode/10-02-SUMMARY.md`
</output>
