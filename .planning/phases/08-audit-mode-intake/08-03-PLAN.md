---
phase: 08-audit-mode-intake
plan: 03
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - .claude/commands/knz-intake.md
autonomous: false
requirements:
  - INTK-11

must_haves:
  truths:
    - "Audit mode ends with the same AskUserQuestion gate as clean intake (Looks good / Edit something / Start over)"
    - "Both project-brief.md and curriculum-gap-report.md are written simultaneously on gate approval"
    - "curriculum-gap-report.md is organized by pipeline stage (Stages 2-8)"
    - "Shallow is defined by schema-field-completeness per stage — not quality judgment"
    - "STATE.md Stage 1 completion state is identical to clean intake — no new fields required"
    - "Gap report summary table shows all 7 stages with status and calls out what's missing"
  artifacts:
    - path: ".claude/commands/knz-intake.md"
      provides: "Audit Mode Steps 5-6 (confirmation gate + output file writing + gap report logic)"
      contains: "curriculum-gap-report.md"
    - path: "workspace/{project}/00-project-brief/curriculum-gap-report.md"
      provides: "Human-facing gap analysis organized by pipeline stage"
      contains: "Stage 2: Learning Outcomes"
  key_links:
    - from: "Audit Mode Step 5 (confirmation gate)"
      to: "Step 6 (file writing)"
      via: "AskUserQuestion 'Looks good' branch — both files written simultaneously"
    - from: "Step 6 gap report"
      to: "stage schemas (stage-02 through stage-08)"
      via: "Explicit reference: read schemas to assess Shallow vs. Missing per stage"
---

<objective>
Author Audit Mode Steps 5-6 in knz-intake.md: confirmation gate, simultaneous file writing, and the full curriculum gap report structure with schema-field-completeness thresholds per pipeline stage.

Purpose: INTK-11 requires a curriculum gap report alongside project-brief.md, organized by pipeline stage, with Shallow defined by schema-field-completeness (not subjective depth judgment). The gate pattern must match clean intake UX exactly — same AskUserQuestion options, same STATE.md completion state.

Output: knz-intake.md Audit Mode section fully complete with Steps 5-6 authored. The gap report logic references all seven pipeline stage schemas for Shallow/Missing thresholds, uses plain-language stage names, and includes a summary table. After this plan, the Audit Mode section has no remaining placeholders.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-audit-mode-intake/08-CONTEXT.md
@.planning/phases/08-audit-mode-intake/08-RESEARCH.md
@.planning/phases/08-audit-mode-intake/08-01-SUMMARY.md
@.planning/phases/08-audit-mode-intake/08-02-SUMMARY.md

<interfaces>
<!-- Gap report schema thresholds per pipeline stage. From 08-RESEARCH.md. -->

Stage 2 (Outcomes) — Shallow if:
- Outcomes exist but don't span minimum Bloom's levels (4 for long programs, 3 medium, 2 short)
- Outcomes use prohibited verbs ("understand", "know", "appreciate")
- Module-level or session-level outcomes missing when program length requires them
- transfer_context missing per outcome
- No enduring understandings or essential questions

Stage 3 (Assessments) — Shallow if:
- Assessments exist but lack paired_objective linkage
- Formative assessments missing (existing materials often have summative only)
- Assessment does not match skill_type

Stage 4 (Modules) — Shallow if:
- Module structure exists but lacks DCR trigger consideration (required when skill_type=open and bloom>=Analyze)
- Module groupings not named with module_name format
- Group processing prompt absent or generic

Stage 5 (Sessions) — Shallow if:
- Session content exists but not organized by session_template
- Pre-work exists but not tagged to specific sessions
- No reflection prompts per session
- Theory content without application activities (flipped classroom violation)

Stage 6 (Metaskills) — Shallow if:
- Thinking routines mentioned but not named specifically (generic "discussion" or "reflection")
- Metaskills referenced but not mapped to specific sessions
- Developability hierarchy sequence not respected

Stage 7 (Transfer Ecosystem) — Shallow if:
- Pre-program readiness assessment absent or lacks minimum 3 questions
- Implementation intentions absent or not tied to specific modules
- Post-program follow-through plan absent
- Manager briefing absent when contact_hours > 4

Stage 8 (Marketing) — Almost always Missing
- Treat as Missing unless source documents contain explicit audience-facing promotional copy

Clean intake confirmation gate (same pattern audit mode must use):
AskUserQuestion options: "Looks good — let's keep going" / "I want to edit something" / "Start over from scratch"
On approval: write project-brief.md, update STATE.md Stage 1 complete, forward-looking message
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Audit Mode Steps 5-6 (gate + file writing + gap report)</name>
  <files>.claude/commands/knz-intake.md</files>
  <action>
Read .claude/commands/knz-intake.md. Find the Step 5 and Step 6 placeholders in the Audit Mode section. Replace both with the full authored prose below.

**Replace Step 5 placeholder with:**

```markdown
### Step 5: Confirmation Gate

Present a clean summary in the same "Your Program at a Glance" format as clean intake — using the user's own words, no schema field names:

---

**Your Program at a Glance**

- **Topic:** {program_topic}
- **Learners:** {target_audience.description}
- **Current skill level:** {prior_knowledge in plain language — "can do X, cannot yet do Y"}
- **Program format:** {session_count} sessions × {session_length_minutes} minutes — {modality}, {delivery_mode in plain language}
- **Where they'll use it:** {transfer_context}
- **Success looks like:** {success_criteria}

---

Then add a brief gap summary:

> **Your curriculum gap report is ready.** {X of 7} pipeline stages have content to build from; {Y} will be built from scratch.

Use `AskUserQuestion` with these three options:

- **"Looks good — let's keep going"**
- **"I want to edit something"**
- **"Start over from scratch"**

---

**On "I want to edit something":**
Ask what they want to change. Take their answer, update the relevant captured field, re-present the summary, and use `AskUserQuestion` again with the same three options.

**On "Start over from scratch":**
Use `AskUserQuestion` to confirm:
> Are you sure? This will clear everything we just extracted and start from the beginning.

Options: **"Yes, start over"** / **"Actually, keep what we have"**

On "Yes, start over": reset STATE.md Stage 1 status to `not-started`, reset Key Decisions to `—`, restart from the Opening section.
On "Actually, keep what we have": return to the summary and re-present `AskUserQuestion`.
```

**Replace Step 6 placeholder with:**

```markdown
### Step 6: Write Output Files

On "Looks good — let's keep going":

**1. Derive `context_of_use`** from `transfer_context` + `target_audience.description` without asking. Synthesize where these specific learners will apply this specific skill in their specific work context.

**2. Load `.claude/reference/schemas/stage-01-intake.md`** as generation context.

**3. Write `project-brief.md`** to `workspace/{project}/00-project-brief/project-brief.md`.

Use the exact same structure as clean intake (see Schema Compliance Checklist at the bottom of this command). All required fields with exact enum values per the schema.

**4. Write `curriculum-gap-report.md`** to `workspace/{project}/00-project-brief/curriculum-gap-report.md`.

Before writing, assess each pipeline stage against its schema. Load `.claude/reference/schemas/stage-02-outcomes.md` through `stage-08-marketing.md` to confirm threshold criteria per stage. Apply this assessment structure:

- **Exists** = source materials contain content for this stage that meets schema-field-completeness requirements
- **Shallow** = content exists but fails specific schema requirements (name the specific missing field — do not use quality language like "limited" or "underdeveloped")
- **Missing** = no relevant content found in any source document

Use this file structure:

```markdown
# Curriculum Gap Report

**Project:** {project-name}
**Generated:** {date}
**Source documents reviewed:** {list of filenames}

---

## How to Read This Report

This report compares your existing materials against what the full curriculum pipeline needs. For each stage:
- **Exists** = your materials have this; the pipeline will build from it
- **Shallow** = something is here, but it doesn't yet meet the structural requirements; the pipeline will fill it out
- **Missing** = nothing found; the pipeline will build this from scratch

---

## Stage 2: Learning Outcomes

### Exists
{What outcome-level content was found, if any}

### Shallow
{Specific schema requirements not met — e.g., "Outcomes present but don't span 4 Bloom's levels; session-level outcomes absent; transfer_context missing per outcome." If nothing is shallow, write: None.}

### Missing
{Stage 2 schema fields not represented at all. If nothing is missing, write: None.}

---

## Stage 3: Assessment Design

### Exists
### Shallow
### Missing

---

## Stage 4: Module Structure

### Exists
### Shallow
### Missing

---

## Stage 5: Session Content

### Exists
### Shallow
### Missing

---

## Stage 6: Metaskill Mapping

### Exists
### Shallow
### Missing

---

## Stage 7: Transfer Ecosystem

### Exists
### Shallow
### Missing

---

## Stage 8: Marketing

### Exists
### Shallow
### Missing

---

## Summary

| Stage | Status |
|-------|--------|
| 2: Learning Outcomes | {Exists / Shallow / Missing — with one-line description if Shallow} |
| 3: Assessment Design | {Exists / Shallow / Missing} |
| 4: Module Structure | {Exists / Shallow / Missing} |
| 5: Session Content | {Exists / Shallow / Missing} |
| 6: Metaskill Mapping | {Exists / Shallow / Missing} |
| 7: Transfer Ecosystem | {Exists / Shallow / Missing} |
| 8: Marketing | {Exists / Shallow / Missing} |

**Next step:** Run `/knz-outcomes` to continue — the pipeline will use your existing materials where they meet requirements and generate what's missing.
```

**Shallow assessment rules per stage:**

- **Stage 2 (Outcomes):** Shallow if outcomes exist but don't span minimum Bloom's levels required by program length (long: 4, medium: 3, short: 2) OR use prohibited verbs ("understand", "know", "appreciate") OR session-level outcomes absent OR transfer_context missing per outcome
- **Stage 3 (Assessments):** Shallow if assessments exist but lack paired_objective linkage OR formative assessments absent (summative-only) OR assessment type doesn't match skill_type
- **Stage 4 (Modules):** Shallow if module groupings exist but aren't named OR group processing prompt absent or generic OR DCR trigger not addressed when skill_type=open and bloom>=Analyze
- **Stage 5 (Sessions):** Shallow if session content exists but not organized by session_template OR pre-work not tagged to sessions OR reflection prompts absent OR theory exists without application activities
- **Stage 6 (Metaskills):** Shallow if thinking routines mentioned but not named specifically (generic "discussion" or "reflection" is insufficient) OR metaskills referenced without session mapping
- **Stage 7 (Transfer Ecosystem):** Shallow if pre-program readiness assessment absent OR lacks minimum 3 questions OR implementation intentions absent OR manager briefing absent when contact_hours > 4
- **Stage 8 (Marketing):** Treat as Missing unless source documents contain explicit audience-facing promotional copy with documented rationale

Write both files simultaneously before announcing anything to the user.

**5. Update STATE.md silently:**
- `Stage Progress` → Stage 1 status: `complete`, Completed: {today's date}
- `Review Gates` → Post-Intake: `approved`, Approved: {today's date}
- `Session Continuity` → **Next Action:** Run /knz-outcomes to begin outcome design

**6. End with a forward-looking message:**

> Your program brief and gap report are ready in `workspace/{project}/00-project-brief/`. The gap report shows what the pipeline will build from your existing materials and what it'll generate from scratch. Run `/knz-outcomes` when you're ready to continue.
```
  </action>
  <verify>
    <automated>grep -n "curriculum-gap-report\|Shallow assessment\|Stage 2.*Outcomes\|Stage 8.*Marketing\|Looks good" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/commands/knz-intake.md</automated>
  </verify>
  <done>Steps 5 and 6 in Audit Mode section contain full prose; Step 5 uses identical AskUserQuestion gate as clean intake; Step 6 writes both files simultaneously, gap report structure covers Stages 2-8, Shallow thresholds are schema-field-completeness rules (not quality judgments), Stage 8 defaults to Missing per research recommendation; no placeholder comments remain in Audit Mode section</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete Audit Mode section</name>
  <what-built>Complete knz-intake.md Audit Mode section (Steps 1-6) authored across Plans 01-03. /knz-init.md updated with source-material/ scaffold. Full audit mode branch in /knz-intake from routing question through gap report output.</what-built>
  <how-to-verify>
Manual review of the complete knz-intake.md file:

1. Open `.claude/commands/knz-intake.md` and verify the Opening section routes with AskUserQuestion before any framing paragraph
2. Verify clean intake path (Thematic Groups 1-3, Confirmation Gate, State Management, Schema Checklist) is entirely unchanged
3. Verify Audit Mode section is appended at the end with Steps 1-6 all authored (no placeholder comments remaining)
4. Read Step 1 and confirm: arguments-first path + auto-detect fallback + "Do not ask any questions" instruction after reading
5. Read Step 2 and confirm: extraction table with plain-language field labels (no schema field names)
6. Read Step 3 and confirm: four-level rubric with concrete examples + format-conversion rule + vocabulary quarantine list
7. Read Step 4 and confirm: per-confidence-level rules + conflict definition (substantive, not format) + source-attribution display
8. Read Step 5 and confirm: identical AskUserQuestion gate as clean intake (same three options)
9. Read Step 6 and confirm: both files written simultaneously + Shallow thresholds name specific schema fields + Stage 8 defaults to Missing
10. Open `.claude/commands/knz-init.md` and confirm source-material/ appears in scaffold directory list

Optional live test (if a test workspace is available):
- Run `/knz-intake` in a fresh project — confirm routing question appears first
- Select "I'm starting from scratch" — confirm clean intake begins with Thematic Group 1
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe which steps have issues</resume-signal>
  <action>Human reviews knz-intake.md and knz-init.md per the checklist above. No automated action — this is a verification gate.</action>
  <verify>
    <automated>MISSING — manual verification only; see how-to-verify checklist</automated>
  </verify>
  <done>All 10 checklist items confirmed; user types "approved"</done>
</task>

</tasks>

<verification>
- grep confirms curriculum-gap-report.md referenced in Step 6
- grep confirms Shallow assessment rules present for all 7 stages
- grep confirms Step 5 uses "Looks good — let's keep going" gate (matching clean intake exactly)
- Read full Audit Mode section: no placeholder comments remaining in Steps 5-6
- Human verification checkpoint passed (see checkpoint task above)
</verification>

<success_criteria>
- INTK-11: curriculum-gap-report.md structure covers Stages 2-8; Shallow defined by schema-field-completeness (not quality); Missing defined as absent from all source documents; written to 00-project-brief/ simultaneously with project-brief.md
- Gate pattern identical to clean intake — no audit-specific divergence in UX
- STATE.md Stage 1 completion identical to clean intake path — no new fields required
- Full Audit Mode section (Steps 1-6) complete with no placeholder comments remaining
</success_criteria>

<output>
After completion, create `.planning/phases/08-audit-mode-intake/08-03-SUMMARY.md`
</output>
