---
phase: 13-command-retrofit
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .claude/plugins/curriculum/commands/marketing.md
  - .claude/schemas/stage-08-marketing.md
autonomous: true
requirements:
  - QUAL-03
  - QUAL-09

must_haves:
  truths:
    - "marketing.md write instruction produces a markdown prose file, not YAML"
    - "The written marketing-package.md file has copy-paste-ready prose paragraphs organized as program description → learning promises → audience positioning"
    - "Traceability data appears only in a compact audit table at the bottom of the file, never wrapped around or interrupting prose"
    - "The audit table has exactly three columns: Claim | Source | Strength"
    - "No YAML syntax appears in marketing-package.md write instructions"
    - "marketing.md contains PAS/DOS and VOC generation instructions that produce benefits-first language"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/marketing.md"
      provides: "prose write instruction + PAS/DOS generation guidance"
      contains: "markdown prose"
    - path: ".claude/schemas/stage-08-marketing.md"
      provides: "schema write note updated to match prose format"
      contains: "prose"
  key_links:
    - from: "marketing.md write instruction"
      to: "marketing-package.md on disk"
      via: "Write tool call in command"
      pattern: "marketing prose|program description"
    - from: "stage-08-marketing.md"
      to: "marketing.md write instruction"
      via: "schema write note"
      pattern: "prose|markdown"
---

<objective>
Upgrade marketing.md from YAML output to PAS/DOS prose output. The write instruction currently produces a schema-compliant YAML file. After this plan, it produces a markdown prose file that an SME can copy-paste directly into any platform — program description, learning promises, and audience positioning as clean paragraphs, with traceability data in a compact table at the bottom only. The schema file (stage-08-marketing.md) gets its write note updated in the same commit.

Purpose: Marketing output is the most externally visible content the tool produces. YAML structure forces anyone reading it to translate internal fields into copy, which no Hello Alice SME will do. Prose output removes that friction.

Output: Updated marketing.md and stage-08-marketing.md. Schema field definitions stay intact — only the write instruction and generation format change.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/reference/curriculum-voice.md

<interfaces>
<!-- Current write instruction location and required replacement format. Extracted from research. -->

Current write instruction in marketing.md (line ~117):
> Write `workspace/{project}/07-marketing/marketing-package.md` with complete schema-compliant YAML content.

Required write instruction format:
Write `workspace/{project}/07-marketing/marketing-package.md` as markdown prose:

```
# [Program Name] — Marketing Package

## Program Description
[2-3 paragraph prose. Benefits-first. PAS or DOS structure. VOC language.]

## Learning Promises
[Bulleted list of outcomes written as learner-facing benefit statements. Not internal outcome text.]

## Audience Fit
[1-2 paragraph prose. Who this is for, what they'll be able to do, why it matters to them.]

---

## Source Traceability

| Claim | Source | Strength |
|-------|--------|----------|
| [marketing claim] | [stage/field] | [High/Medium/Low] |
```

PAS/DOS generation instructions to add to marketing.md:
- PAS: Problem → Agitation → Solution — use when addressing a pain point
- DOS: Dream → Obstacle → Solution — use when positioning toward an aspiration
- VOC (Voice of Customer): use language that reflects how learners describe their own challenges, not how instructional designers describe competencies
- Benefits-first: lead with what the learner gains, not what the program covers

curriculum-voice.md marketing register is already in the command's Persona — generation instructions add the structural framework (PAS/DOS), not additional vocabulary rules.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update marketing.md write instruction and add PAS/DOS generation guidance</name>
  <files>.claude/plugins/curriculum/commands/marketing.md</files>
  <action>
Read marketing.md fully before editing.

**Change 1 — Write instruction:**
Find the line that says "Write `workspace/{project}/07-marketing/marketing-package.md` with complete schema-compliant YAML content." Replace it with a markdown prose write instruction. The written file must have:
- H1 title: "[Program Name] — Marketing Package"
- Section "Program Description": 2-3 prose paragraphs, benefits-first, PAS or DOS structure, VOC language
- Section "Learning Promises": bullet list of outcomes rewritten as learner-facing benefit statements (full text, not outcome IDs)
- Section "Audience Fit": 1-2 paragraphs on who this program is for and what they gain
- Horizontal rule separator
- Section "Source Traceability": compact table with three columns: Claim | Source | Strength
- The traceability table must appear after the separator — never inline with the prose sections

**Change 2 — Generation instructions:**
Add PAS/DOS and VOC generation instructions to the generation section (not the Persona). These are structural frameworks for writing, not vocabulary rules:
- "Use PAS (Problem → Agitation → Solution) when the audience has a pain point to address."
- "Use DOS (Dream → Obstacle → Solution) when positioning toward an aspiration."
- "Write in VOC (Voice of Customer) — use language that reflects how learners describe their own challenges."
- "Lead with benefits, not coverage. 'You'll be able to...' not 'This program covers...'"

**Do not change:**
- Persona section (already has strong inline guardrails from Phase 12)
- Prerequisites section
- Generation logic for sourcing claims from earlier stages
- The schema reference (field definitions stay in stage-08-marketing.md)

After editing, run:
`grep -n "schema-compliant YAML\|YAML content\|yaml_content\|schema:" .claude/plugins/curriculum/commands/marketing.md`

Zero matches confirms YAML write instruction is gone.
  </action>
  <verify>
    <automated>grep -n "schema-compliant YAML\|YAML content\|yaml_content" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/marketing.md; echo "Exit: $?"</automated>
  </verify>
  <done>marketing.md write instruction produces a markdown prose file with program description, learning promises, audience fit, and a traceability table at the bottom. PAS/DOS and VOC generation instructions are present. No YAML write instruction remains.</done>
</task>

<task type="auto">
  <name>Task 2: Update stage-08-marketing.md write note to match prose format</name>
  <files>.claude/schemas/stage-08-marketing.md</files>
  <action>
Read stage-08-marketing.md fully before editing.

Find any write instruction note or output format note in the schema file that references YAML output. Update it to indicate that the written file is markdown prose (not YAML). This is a schema annotation change only — the field definitions and field names in the schema are NOT changed.

If the schema has a section like "Output Format" or "Write Instruction" that says "schema-compliant YAML", change it to: "Written as markdown prose. See marketing.md for file structure."

If no such note exists, add a brief write note at the bottom of the schema:
```
## Write Format
marketing-package.md is written as markdown prose, not YAML. Field definitions in this schema inform generation; they do not appear as YAML keys in the output file.
```

After editing, run:
`grep -n "schema-compliant YAML\|YAML content" .claude/schemas/stage-08-marketing.md`

Zero matches (or no output) confirms the write note is updated.
  </action>
  <verify>
    <automated>grep -n "schema-compliant YAML\|YAML content" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/schemas/stage-08-marketing.md; echo "Exit: $?"</automated>
  </verify>
  <done>stage-08-marketing.md write note reflects prose output format. Schema field definitions are unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete, confirm:
1. `grep "schema-compliant YAML\|yaml_content" .claude/plugins/curriculum/commands/marketing.md` — zero matches
2. `grep "schema-compliant YAML" .claude/schemas/stage-08-marketing.md` — zero matches
3. Read the write instruction section of marketing.md — confirm it describes the four-section prose structure with traceability table at bottom
4. Read the generation section of marketing.md — confirm PAS, DOS, and VOC terms are present as framework instructions
</verification>

<success_criteria>
- marketing.md write instruction produces a markdown prose file (not YAML)
- File structure: program description → learning promises → audience fit → separator → traceability table
- PAS/DOS and VOC generation frameworks are explicitly instructed
- Traceability table is last, separated by horizontal rule — never inline with copy
- stage-08-marketing.md write note updated to match — same commit
</success_criteria>

<output>
After completion, create `.planning/phases/13-command-retrofit/13-02-SUMMARY.md`
</output>
