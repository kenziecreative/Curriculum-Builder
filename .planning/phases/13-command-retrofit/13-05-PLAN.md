---
phase: 13-command-retrofit
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/commands/marketing.md
  - .claude/plugins/curriculum/commands/validate.md
  - .claude/plugins/curriculum/commands/assessments.md
autonomous: true
requirements: [PRES-02, PRES-07]
gap_closure: true

must_haves:
  truths:
    - "marketing.md Traceability Display close includes the context-clear nudge after the /curriculum:approve line"
    - "validate.md all-pass path includes the context-clear nudge after the /curriculum:approve line"
    - "assessments.md routing instructions use 'assessment summary' not 'alignment map' in lines 39-51 and line 175"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/marketing.md"
      provides: "Context-clear nudge at end of auto-chain"
      contains: "Your work is saved — clear context before running the next command."
    - path: ".claude/plugins/curriculum/commands/validate.md"
      provides: "Context-clear nudge on all-pass path"
      contains: "Your work is saved — clear context before running the next command."
    - path: ".claude/plugins/curriculum/commands/assessments.md"
      provides: "Plain-language routing labels — no insider vocabulary"
      contains: "assessment summary"
  key_links:
    - from: ".claude/plugins/curriculum/commands/marketing.md"
      to: "PRES-07 context-clear nudge"
      via: "Traceability Display section close, after Run /curriculum:approve line"
      pattern: "Your work is saved — clear context"
    - from: ".claude/plugins/curriculum/commands/validate.md"
      to: "PRES-07 context-clear nudge"
      via: "all-pass path close, after Run /curriculum:approve line (line ~128)"
      pattern: "Your work is saved — clear context"
    - from: ".claude/plugins/curriculum/commands/assessments.md"
      to: "PRES-02 plain language"
      via: "Prerequisites routing block and PIPE-05 Gate preamble"
      pattern: "assessment summary"
---

<objective>
Close the two PRES-07 gaps (missing context-clear nudge in marketing.md and validate.md) and the PRES-02 vocabulary warning (assessments.md routing instructions still use "alignment map").

Purpose: Phase 13 verification scored 15/17. These three targeted edits close the remaining gaps and bring the phase to full goal achievement.
Output: Three modified command files; no new files created.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-command-retrofit/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add context-clear nudge to marketing.md and validate.md</name>
  <files>.claude/plugins/curriculum/commands/marketing.md, .claude/plugins/curriculum/commands/validate.md</files>
  <action>
    **marketing.md** — Traceability Display section, around line 183.

    Current text (the closing display block):
    ```
    Your marketing materials are ready. Run /curriculum:approve to review your complete curriculum package and mark it delivery-ready.
    ```

    Replace with:
    ```
    Your marketing materials are ready. Run /curriculum:approve to review your complete curriculum package and mark it delivery-ready.

    Your work is saved — clear context before running the next command.
    ```

    The nudge goes inside the same fenced display block, on a new line after the existing sentence. Do not add a blank line between the two sentences — they form one closing block.

    **validate.md** — all-pass path, around line 128.

    Current text (the next-step display block on the all-pass path):
    ```
    Run `/curriculum:approve` to review the human checklist and mark your curriculum delivery-ready.
    ```

    Replace with:
    ```
    Run `/curriculum:approve` to review the human checklist and mark your curriculum delivery-ready.

    Your work is saved — clear context before running the next command.
    ```

    The nudge goes inside the same fenced display block, after the existing sentence. Do not add the nudge to the failure path (the path that reads "Found {N} issue{s} that need fixing" — that path ends with a NEEDS: list, not an approve prompt).
  </action>
  <verify>
    <automated>grep -n "Your work is saved" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/marketing.md /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/validate.md</automated>
  </verify>
  <done>Both files contain exactly one instance of "Your work is saved — clear context before running the next command." in the correct position (after the /curriculum:approve line on the all-pass / auto-chain-end path).</done>
</task>

<task type="auto">
  <name>Task 2: Replace "alignment map" with "assessment summary" in assessments.md</name>
  <files>.claude/plugins/curriculum/commands/assessments.md</files>
  <action>
    Four occurrences of "alignment map" appear in assessments.md routing instructions. Replace each with "assessment summary".

    **Line 39-40** (pre-populated path):
    - Current: `Display the corrected assessment plan and alignment map. Proceed directly to the PIPE-05 Gate section.`
    - Replace with: `Display the corrected assessment plan. Proceed directly to the PIPE-05 Gate section.`
    (Drop "and alignment map" — "assessment plan" already names what is shown; the phrase is redundant.)

    **Line 45** (in-progress path):
    - Current: `re-display alignment map if `02-assessments/` files exist from a previous partial run; if yes, proceed directly to the PIPE-05 Gate section`
    - Replace with: `re-display the assessment summary if `02-assessments/` files exist from a previous partial run; if yes, proceed directly to the PIPE-05 Gate section`

    **Line 51** (complete/gate-pending path):
    - Current: `Re-display alignment map and proceed to PIPE-05 Gate section.`
    - Replace with: `Re-display the assessment summary and proceed to PIPE-05 Gate section.`

    **Line 175** (PIPE-05 Gate preamble):
    - Current: `After displaying the alignment map, silently update `workspace/{project-name}/STATE.md``
    - Replace with: `After displaying the assessment summary, silently update `workspace/{project-name}/STATE.md``

    Do not change line 181 — "re-present alignment map" in the "I have concerns" branch. That is an internal instruction about regenerating and re-presenting, not a user-facing label. Actually on review: change it to "re-present the assessment summary" for consistency.

    **Line 181** (I have concerns branch inside PIPE-05 Gate):
    - Current: `- **"I have concerns"** — ask what's wrong, revise assessments, re-present alignment map, gate again`
    - Replace with: `- **"I have concerns"** — ask what's wrong, revise assessments, re-present the assessment summary, gate again`
  </action>
  <verify>
    <automated>grep -n "alignment map" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/assessments.md</automated>
  </verify>
  <done>grep returns zero matches for "alignment map" in assessments.md. The file uses "assessment summary" in all five locations that previously used "alignment map".</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -n "Your work is saved" .claude/plugins/curriculum/commands/marketing.md` — returns exactly 1 match after the /curriculum:approve line
2. `grep -n "Your work is saved" .claude/plugins/curriculum/commands/validate.md` — returns exactly 1 match on the all-pass path
3. `grep -n "alignment map" .claude/plugins/curriculum/commands/assessments.md` — returns zero matches
4. `grep -n "assessment summary" .claude/plugins/curriculum/commands/assessments.md` — returns matches at the five corrected locations
</verification>

<success_criteria>
- PRES-07: marketing.md and validate.md both include the context-clear nudge in the correct position
- PRES-02: assessments.md routing instructions contain no "alignment map" references — only "assessment summary"
- Phase 13 verification score moves from 15/17 to 17/17 (both failed/partial truths resolved)
- No other text in the three files changed
</success_criteria>

<output>
After completion, create `.planning/phases/13-command-retrofit/13-05-SUMMARY.md`
</output>
