---
phase: 13-command-retrofit
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .claude/plugins/curriculum/commands/transfer.md
  - .claude/schemas/stage-07-transfer.md
autonomous: true
requirements:
  - QUAL-04
  - QUAL-09

must_haves:
  truths:
    - "transfer.md write instruction produces a markdown narrative file, not YAML"
    - "The written transfer-ecosystem.md file has plain section headings and reads as a document, not a data structure"
    - "No YAML keys, colons-as-labels, or field names appear in transfer-ecosystem.md as written to disk"
    - "Section headings use plain language (e.g., 'Before the Program', 'During the Program', 'After the Program') not schema field names"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/transfer.md"
      provides: "narrative write instruction replacing YAML output"
      contains: "narrative"
    - path: ".claude/schemas/stage-07-transfer.md"
      provides: "schema write note updated to match narrative format"
      contains: "narrative"
  key_links:
    - from: "transfer.md write instruction"
      to: "transfer-ecosystem.md on disk"
      via: "Write tool call in command"
      pattern: "narrative|plain headings"
    - from: "stage-07-transfer.md"
      to: "transfer.md write instruction"
      via: "schema write note"
      pattern: "narrative|markdown"
---

<objective>
Upgrade transfer.md from YAML output to narrative prose output. The write instruction currently produces a schema-compliant YAML file. After this plan, it produces a readable markdown document with plain section headings and prose descriptions — a transfer ecosystem document that an SME can share with managers, participants, and facilitators without translation. The schema file (stage-07-transfer.md) gets its write note updated in the same commit.

Purpose: Transfer ecosystem content is designed to be shared across the program environment — managers receive pre-program priming, facilitators get in-session transfer moments, participants get post-program application support. A YAML file cannot serve any of these audiences. Narrative prose can.

Output: Updated transfer.md and stage-07-transfer.md. Schema field definitions stay intact — only the write instruction and output structure change.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/reference/curriculum-voice.md
@.claude/reference/doctrine-how-i-teach-how-i-learn.md

<interfaces>
<!-- Current write instruction location and required replacement format. Extracted from research. -->

Current write instruction in transfer.md (line ~179):
> Write `workspace/{project}/06-transfer/transfer-ecosystem.md` with complete schema-compliant YAML content.

Required write instruction format — transfer-ecosystem.md must be a narrative document:

```
# Transfer Ecosystem: [Program Name]

## Before the Program
[Prose paragraph describing what managers and participants should do before attending.
Written to the facilitator or program coordinator who will brief participants.
2-4 sentences.]

## During the Program
[Prose paragraph describing the in-session transfer moments built into the program.
What learners will practice, what connections they'll make to their real work.
2-4 sentences.]

## After the Program
[Prose paragraph describing the 30/60/90-day application support structure.
What participants will do, what managers can reinforce, what markers indicate transfer occurred.
3-5 sentences.]

## Making It Stick
[Optional section: key conditions for transfer success.
Plain-language principles a non-ID manager can actually act on.
Bullet list of 3-5 actionable items.]
```

Plain heading principles:
- Use audience-facing language: "Before the Program", "During the Program", "After the Program"
- Never use schema field names as headings (no: `pre_program_priming:`, `in_session_transfer:`, `post_program_application:`)
- Never use TMA arc labels as headings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update transfer.md write instruction to narrative prose format</name>
  <files>.claude/plugins/curriculum/commands/transfer.md</files>
  <action>
Read transfer.md fully before editing.

**Change 1 — Write instruction:**
Find the line that says "Write `workspace/{project}/06-transfer/transfer-ecosystem.md` with complete schema-compliant YAML content." Replace it with a narrative write instruction. The written file must have:
- H1 title: "Transfer Ecosystem: [Program Name]"
- Plain-language section headings (Before the Program / During the Program / After the Program as the model; exact labels are Claude's discretion as long as no schema field names appear)
- Prose paragraphs under each heading — not bullet lists of YAML values, not field:value pairs
- Optional final section for transfer conditions or key success factors (plain language, bulleted)
- No YAML syntax anywhere in the written file

**Change 2 — Vocabulary in generation section:**
Scan the generation section for any schema field names used as display labels (e.g., `pre_program_priming:`, `in_session_transfer:`, `post_program_application:`, `activation_trigger:`). If any appear in content that will be written to the file or shown to users, replace with plain language equivalents.

**Do not change:**
- Persona section (already has inline guardrails from Phase 12)
- Prerequisites section
- Generation logic that sources transfer content from earlier stages
- The schema reference (field definitions stay in stage-07-transfer.md)

After editing, run:
`grep -n "schema-compliant YAML\|YAML content\|yaml_content\|pre_program_priming:\|in_session_transfer:\|post_program_application:" .claude/plugins/curriculum/commands/transfer.md`

Zero matches confirms YAML write instruction and field-name leaks are gone.
  </action>
  <verify>
    <automated>grep -n "schema-compliant YAML\|YAML content\|yaml_content" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/transfer.md; echo "Exit: $?"</automated>
  </verify>
  <done>transfer.md write instruction produces a narrative markdown file with plain section headings and prose content. No YAML write instruction remains. Schema field names do not appear as display labels in the written file format.</done>
</task>

<task type="auto">
  <name>Task 2: Update stage-07-transfer.md write note to match narrative format</name>
  <files>.claude/schemas/stage-07-transfer.md</files>
  <action>
Read stage-07-transfer.md fully before editing.

Find any write instruction note or output format note in the schema file that references YAML output. Update it to indicate that the written file is a narrative markdown document (not YAML). Field definitions in the schema are NOT changed — only the write note.

If the schema has a section like "Output Format" or "Write Instruction" that says "schema-compliant YAML", change it to: "Written as narrative markdown. See transfer.md for file structure."

If no such note exists, add a brief write note at the bottom:
```
## Write Format
transfer-ecosystem.md is written as narrative markdown prose, not YAML. Field definitions in this schema inform generation; they do not appear as YAML keys in the output file.
```

After editing, run:
`grep -n "schema-compliant YAML\|YAML content" .claude/schemas/stage-07-transfer.md`

Zero matches (or no output) confirms the write note is updated.
  </action>
  <verify>
    <automated>grep -n "schema-compliant YAML\|YAML content" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/schemas/stage-07-transfer.md; echo "Exit: $?"</automated>
  </verify>
  <done>stage-07-transfer.md write note reflects narrative output format. Schema field definitions are unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete, confirm:
1. `grep "schema-compliant YAML\|yaml_content" .claude/plugins/curriculum/commands/transfer.md` — zero matches
2. `grep "schema-compliant YAML" .claude/schemas/stage-07-transfer.md` — zero matches
3. Read the write instruction section of transfer.md — confirm it describes the narrative structure with plain section headings and prose paragraphs
4. `grep "pre_program_priming:\|in_session_transfer:\|post_program_application:" .claude/plugins/curriculum/commands/transfer.md` — zero matches in user-facing sections
</verification>

<success_criteria>
- transfer.md write instruction produces a narrative markdown file (not YAML)
- Written file uses plain section headings, not schema field names
- Prose paragraphs under each heading — no YAML key:value structure in the written output
- stage-07-transfer.md write note updated to match — same commit
</success_criteria>

<output>
After completion, create `.planning/phases/13-command-retrofit/13-03-SUMMARY.md`
</output>
