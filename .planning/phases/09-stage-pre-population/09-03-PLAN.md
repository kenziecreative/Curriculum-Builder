---
phase: 09-stage-pre-population
plan: 03
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - .claude/plugins/curriculum/commands/outcomes.md
  - .claude/plugins/curriculum/commands/assessments.md
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/sessions.md
autonomous: false
requirements:
  - INTK-12
must_haves:
  truths:
    - "Running /curriculum:outcomes after pre-population displays the corrected draft (not blank generation) and presents the three-option gate"
    - "Running /curriculum:assessments after pre-population follows the same enforcement-then-gate pattern"
    - "Running /curriculum:modules after pre-population follows the same enforcement-then-gate pattern"
    - "Running /curriculum:sessions after pre-population displays the session manifest and enters enforcement mode rather than dispatching generation subagents"
    - "Choosing 'Start over' at any enforcement gate wipes stage files and resets STATE.md status to not-started"
    - "NEEDS: markers are absent from stage files after the downstream command writes its corrected output"
    - "STATE.md status-check order: pre-populated branch checked before in-progress branch in all four commands"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/outcomes.md"
      provides: "`pre-populated` branch in Stage 2 status check — reads draft, runs enforcement, strips NEEDS: markers, displays corrected version, presents gate"
      contains: "pre-populated"
    - path: ".claude/plugins/curriculum/commands/assessments.md"
      provides: "`pre-populated` branch in Stage 3 status check"
      contains: "pre-populated"
    - path: ".claude/plugins/curriculum/commands/modules.md"
      provides: "`pre-populated` branch in Stage 4 status check"
      contains: "pre-populated"
    - path: ".claude/plugins/curriculum/commands/sessions.md"
      provides: "`pre-populated` branch in Stage 5 status check — reads session manifest, runs structure enforcement, presents gate"
      contains: "pre-populated"
  key_links:
    - from: "outcomes.md pre-populated branch"
      to: "workspace/{project}/01-outcomes/"
      via: "reads pre-populated files, runs five constraint enforcement steps silently, strips # NEEDS: lines, displays corrected version"
      pattern: "pre-populated"
    - from: "sessions.md pre-populated branch"
      to: "workspace/{project}/04-sessions/session-manifest.md"
      via: "reads manifest, enforces structure constraints, presents gate before any subagent dispatch"
      pattern: "pre-populated"
---

<objective>
Add the `pre-populated` prerequisite branch to all four downstream commands so they enter enforcement-and-review mode when they detect a pre-populated draft, rather than generating from scratch.

Purpose: Without these branches, a user who runs `/curriculum:outcomes` after audit mode intake would land in the `not-started` generation flow and overwrite the pre-populated draft. These branches are the mechanism that makes the pre-populated draft visible and reviewable.

Output: All four downstream command files updated with a `pre-populated` branch in their stage status check sections. The branch pattern is identical across all four: read draft, run enforcement silently, strip NEEDS: markers, display corrected version, go to review gate.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-stage-pre-population/09-02-SUMMARY.md
@.planning/phases/09-stage-pre-population/09-CONTEXT.md
@.planning/phases/09-stage-pre-population/09-RESEARCH.md

<interfaces>
<!-- Current Prerequisites stage status check pattern in outcomes.md — add pre-populated branch here -->

From .claude/plugins/curriculum/commands/outcomes.md:
```markdown
### 2. Check Stage 2 status

Read the Stage 2 row from STATE.md `Stage Progress` table.

- **`not-started`** — proceed to Generation section below
- **`in-progress`** — check whether `workspace/*/01-outcomes/` files exist from a previous
  partial run; if yes, re-display them and proceed directly to the Review Gate section;
  if no files exist, regenerate from scratch starting from the Generation section
- **`complete`** — respond:
> Outcomes are already designed for this program. Run `/curriculum:assessments` to continue.
Stop here.
```

<!-- assessments.md stage check pattern — same structure, Stage 3 -->

From .claude/plugins/curriculum/commands/assessments.md:
```markdown
### 3. Check Stage 3 status
- **`not-started`** — proceed to Generation section
- **`in-progress`** — re-display alignment map if `02-assessments/` files exist...
- **`complete`** AND Post-Assessment gate = `approved` — ...
```

<!-- modules.md stage check pattern — same structure, Stage 4 -->

From .claude/plugins/curriculum/commands/modules.md:
```markdown
### 3. Check Stage 4 status
- **`not-started`** — proceed to Generation section
- **`in-progress`** — if `03-modules/` files exist...
- **`complete`** AND Module-Structure gate = `approved` — ...
```

<!-- sessions.md stage check pattern — Stage 5, orchestrator model -->

From .claude/plugins/curriculum/commands/sessions.md:
```markdown
### 3. Check Stage 5 status
- `not-started` → Proceed to Module Reading section
- `in-progress` → Check which session directories already exist...
- `complete` → Respond: "Sessions are already generated..."
```

<!-- pre-populated branch template from RESEARCH.md Pattern 3 -->

New branch to add (outcomes.md example):
```markdown
- **`pre-populated`** — Read all files from `workspace/*/01-outcomes/`. Run all five
  constraint enforcement steps silently against the existing content (same steps as post-generation).
  Remove any `# NEEDS:` marker lines from the corrected output. Display the corrected outcome set
  and thinking-level distribution summary. Proceed directly to the Review Gate section.
  Do not regenerate — the content source is the extracted draft, not the project brief.
```

<!-- Start over behavior for pre-populated branch (CONTEXT.md locked decision) -->

On "Start over" from pre-populated branch:
- Wipe all files in the stage directory
- Set STATE.md Stage N status to `not-started`
- Restart from the Generation section (generate fresh from project brief)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-populated branch to outcomes.md and assessments.md</name>
  <files>
    .claude/plugins/curriculum/commands/outcomes.md
    .claude/plugins/curriculum/commands/assessments.md
  </files>
  <action>
**In outcomes.md:**

Find the `### 2. Check Stage 2 status` section. Insert a `pre-populated` branch between `not-started` and `in-progress`. The branch must appear BEFORE `in-progress` — this is critical. (Pitfall 2 from RESEARCH.md: if `in-progress` branch runs first, it will find pre-populated files in the directory and misroute. STATE.md status check must precede file-existence check.)

Add this branch:
```markdown
- **`pre-populated`** — Read all files from `workspace/*/01-outcomes/`. Run all five constraint
  enforcement steps silently against the existing content (same steps as post-generation: Bloom
  verb check, level distribution, transfer context completeness, hierarchy integrity, prohibited
  verb replacement). Remove any `# NEEDS:` marker lines from the corrected output before
  displaying. Display the corrected outcome set and thinking-level distribution summary.
  Proceed directly to the Review Gate section.
  Do not regenerate — the content source is the extracted draft, not the project brief.
  On "Start over" at the Review Gate: wipe all files in `workspace/*/01-outcomes/`, set Stage 2
  status to `not-started` in STATE.md, restart from the Generation section.
```

**In assessments.md:**

Find the `### 3. Check Stage 3 status` section. Insert a `pre-populated` branch before `in-progress`:

```markdown
- **`pre-populated`** — Read all files from `workspace/*/02-assessments/`. Run all constraint
  enforcement steps silently against the existing content (coverage check, Bloom auto-elevation,
  formative coverage, observable verbs, skill-type compliance). Remove any `# NEEDS:` marker
  lines from the corrected output before displaying. Display the corrected assessment plan and
  alignment map. Proceed directly to the PIPE-05 Gate section.
  Do not regenerate — the content source is the extracted draft, not the project brief.
  On "Start over" at the PIPE-05 Gate: wipe all files in `workspace/*/02-assessments/`, set
  Stage 3 status to `not-started` in STATE.md, restart from the Generation section.
```
  </action>
  <verify>
    <automated>grep -c 'pre-populated' /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/outcomes.md && grep -c 'pre-populated' /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/assessments.md</automated>
  </verify>
  <done>Both files contain `pre-populated` branch text. `grep -c 'pre-populated'` returns ≥ 2 for each file (branch text plus the "Start over" reset instruction both reference the status).</done>
</task>

<task type="auto">
  <name>Task 2: Add pre-populated branch to modules.md and sessions.md</name>
  <files>
    .claude/plugins/curriculum/commands/modules.md
    .claude/plugins/curriculum/commands/sessions.md
  </files>
  <action>
**In modules.md:**

Find the `### 3. Check Stage 4 status` section. Insert a `pre-populated` branch before `in-progress`:

```markdown
- **`pre-populated`** — Read all files from `workspace/*/03-modules/`. Run all module structure
  enforcement checks silently against the existing content (named group processing prompts, DCR
  trigger check when skill_type=open and bloom>=Analyze, sequence coherence). Remove any `# NEEDS:`
  marker lines from the corrected output before displaying. Display the corrected module structure
  summary and gate table. Proceed directly to the Module Structure Gate section.
  Do not regenerate — the content source is the extracted draft, not the project brief.
  On "Start over" at the Module Structure Gate: wipe all files in `workspace/*/03-modules/`, set
  Stage 4 status to `not-started` in STATE.md, restart from the Generation section.
```

**In sessions.md:**

The sessions command is an orchestrator that dispatches subagent Tasks. The `pre-populated` branch here is different — it reads the session manifest (structure-only file from intake pre-population) rather than dispatching full generation subagents.

Find the `### 3. Check Stage 5 status` section. Insert a `pre-populated` branch before `in-progress`:

```markdown
- `pre-populated` — Read `workspace/*/04-sessions/session-manifest.md`. Run structure enforcement
  silently: verify each session has named outcomes mapped, flag sessions with NEEDS: template
  marker. Remove any `# NEEDS:` lines from the display (markers stay in raw file until full
  generation replaces them). Display the session manifest summary: session names, mapped outcomes,
  template TBD indicators. Present the review gate (three options below). Do not dispatch
  generation subagents — full session content generation happens only after the user approves or
  flags an issue.
  On "Looks good": proceed to Module Reading section and dispatch subagent Tasks as normal
  (session manifest provides context for each Task). Set Stage 5 status to `in-progress`.
  On "Flag an issue": take feedback, regenerate the manifest from project brief plus feedback,
  re-display. Do not dispatch full generation.
  On "Start over": wipe `workspace/*/04-sessions/`, set Stage 5 status to `not-started`,
  restart from Module Reading section as if `not-started`.
```

Note on sessions: The three-option gate for sessions pre-populated mode reuses the existing review gate pattern. "Looks good" advances to full generation (the orchestrator then dispatches Tasks per normal sessions flow, using the manifest as additional context). This is the correct behavior per CONTEXT.md: sessions pre-population is structure only; full generation is still done by `/curriculum:sessions`.
  </action>
  <verify>
    <automated>grep -c 'pre-populated' /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/modules.md && grep -c 'pre-populated' /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/sessions.md</automated>
  </verify>
  <done>Both files contain `pre-populated` branch text. `grep -c 'pre-populated'` returns ≥ 2 for each file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full audit mode flow end-to-end</name>
  <what-built>
All four downstream commands now have a `pre-populated` branch in their stage status check. The full Phase 9 implementation is complete: intake.md pre-populates stage files, the hook blocks advancement, and downstream commands enter enforcement mode when they detect the draft.
  </what-built>
  <how-to-verify>
Run the full audit mode flow using the test workspace:

1. Confirm test source materials exist in `workspace/test-program/source-material/` (or add a simple test facilitator guide document there)

2. Run `/curriculum:intake` — select "I have existing materials to bring in"

3. Verify the post-intake summary table appears with Stage | What was written | Issues found columns

4. Verify `workspace/test-program/STATE.md` shows `pre-populated` for stages that had content

5. Run `/curriculum:outcomes` — verify it displays the corrected draft (not blank generation) and offers three options: "Looks good / Flag an issue / Start over"

6. Attempt to run `/curriculum:assessments` WITHOUT approving outcomes first — verify the hook blocks it with the forward-looking message: "Stage 2 has a draft ready — run `/curriculum:outcomes` to review and approve it first"

7. From the outcomes review gate, choose "Start over" — verify `01-outcomes/` files are wiped and STATE.md Stage 2 resets to `not-started`

8. Spot-check: `grep 'NEEDS:' workspace/test-program/01-outcomes/*.md` after approving outcomes — verify no NEEDS: markers remain in approved output files
  </how-to-verify>
  <action>Run the verification steps in how-to-verify. No code to write — this task confirms the prior implementation is correct end-to-end.</action>
  <verify>
    <automated>grep -c 'pre-populated' .claude/plugins/curriculum/commands/outcomes.md .claude/plugins/curriculum/commands/assessments.md .claude/plugins/curriculum/commands/modules.md .claude/plugins/curriculum/commands/sessions.md .claude/plugins/curriculum/commands/intake.md .claude/hooks/pre-tool-use.sh</automated>
  </verify>
  <done>User confirms full audit mode flow: pre-populated files written, hook blocks advancement with forward-looking message, downstream command enters enforcement mode, Start over resets status.</done>
  <resume-signal>Type "approved" if the full flow works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep -c 'pre-populated' .claude/plugins/curriculum/commands/outcomes.md` ≥ 2
- `grep -c 'pre-populated' .claude/plugins/curriculum/commands/assessments.md` ≥ 2
- `grep -c 'pre-populated' .claude/plugins/curriculum/commands/modules.md` ≥ 2
- `grep -c 'pre-populated' .claude/plugins/curriculum/commands/sessions.md` ≥ 2
- Manual: full audit mode flow demonstrates enforcement mode on first downstream command run
- Manual: hook blocks advancement when prior stage is `pre-populated` with forward-looking message
- Manual: "Start over" wipes files and resets status to `not-started`
</verification>

<success_criteria>
All four downstream commands detect `pre-populated` status and enter enforcement-and-review mode. The hook unit test passes. The full audit mode flow from intake through first downstream command review is verified end-to-end by the user.
</success_criteria>

<output>
After completion, create `.planning/phases/09-stage-pre-population/09-03-SUMMARY.md`
</output>
