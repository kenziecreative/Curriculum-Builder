---
phase: 07-full-pipeline-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/hooks/pre-tool-use.sh
  - .claude/settings.json
  - .claude/commands/knz-validate.md
  - .claude/commands/knz-approve.md
autonomous: true
requirements:
  - PIPE-06
  - PIPE-07
  - INFR-07

must_haves:
  truths:
    - "The PreToolUse hook blocks writes to any stage directory when the preceding stage is not complete in STATE.md, with a specific actionable error message naming the missing stage and the command to run"
    - "Validation (Stage 9, 08-validation/) is exempt from the hook — writes are allowed anytime after Stage 5"
    - "The hook handles uninitialized workspaces gracefully — if no STATE.md is found, writes are allowed (hook is a safety net, not primary gate)"
    - "The auto-chain fires from /knz-validate ONLY on the Tier 1 clean path (tier_1_failures == 0) — chain does not fire when failures exist"
    - "The /knz-approve final gate shows a complete pipeline summary (all eight data points: program overview, outcomes, assessments, modules/sessions, metaskill activations, transfer layer, marketing elements, validation status) before the AskUserQuestion gate"
    - "The final gate reads each stage directory only if that stage status is complete in STATE.md — missing stages show 'not yet generated' without breaking the command"
    - "The settings.json PreToolUse array has two entries after update: the existing Bash entry (secrets check) and the new Write|Edit entry (stage sequencing) — the Bash entry is preserved"
  artifacts:
    - path: ".claude/hooks/pre-tool-use.sh"
      provides: "PreToolUse stage sequencing enforcer"
      contains: "directory-to-stage mapping, STATE.md status parse, deny JSON output"
    - path: ".claude/settings.json"
      provides: "Hook registration"
      contains: "Write|Edit PreToolUse entry added to existing array"
    - path: ".claude/commands/knz-validate.md"
      provides: "Auto-chain trigger on Tier 1 clean path"
      contains: "Auto-Trigger Metaskills section after state update"
    - path: ".claude/commands/knz-approve.md"
      provides: "Final delivery gate with complete pipeline summary"
      contains: "Full pipeline summary section for Final Validation gate"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/pre-tool-use.sh"
      via: "PreToolUse hook registration with Write|Edit matcher"
      pattern: "Write|Edit"
    - from: ".claude/commands/knz-validate.md"
      to: ".claude/commands/knz-metaskills.md"
      via: "Skill invocation on tier_1_failures == 0 path"
      pattern: "Auto-Trigger Metaskills|Invoke.*knz-metaskills"
    - from: ".claude/hooks/pre-tool-use.sh"
      to: "workspace/*/STATE.md"
      via: "grep parse of Stage Progress table"
      pattern: "STATE_FILE.*STATE.md|grep.*PREREQ_NUM"
---

<objective>
Wire the autonomous pipeline chain into /knz-validate, implement the PreToolUse stage-sequencing hook, and extend /knz-approve with the full final delivery gate. These three changes complete the enforcement and flow layer that makes the pipeline autonomous and tamper-proof.

Purpose: Plan 01 creates the generation commands. This plan makes them run automatically after clean validation, prevents out-of-order writes at the filesystem level, and gives the user a complete "here is what you built" view before approving delivery-readiness.

Output: pre-tool-use.sh (new), settings.json (updated), knz-validate.md (updated), knz-approve.md (updated).
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Files being modified
@.claude/commands/knz-validate.md
@.claude/commands/knz-approve.md
@.claude/settings.json

# Research reference — hook pattern, settings.json merge rule, final gate format
@.planning/phases/07-full-pipeline-completion/07-RESEARCH.md

<interfaces>
<!-- Critical context for executor — read before modifying any file. -->

From .claude/settings.json (current structure):
```json
{
  "hooks": {
    "PreCompact": [ { "matcher": "", "hooks": [{ "type": "command", "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/pre-compact-check.sh\"", "timeout": 10 }] } ],
    "Stop": [ { "matcher": "", "hooks": [{ "type": "command", "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/milestone-check.sh\"", "timeout": 10 }] } ],
    "PreToolUse": [
      { "matcher": "Bash", "hooks": [{ "type": "command", "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/pre-commit-secrets.sh\"", "timeout": 10 }] }
    ]
  }
}
```
CRITICAL: The `PreToolUse` array already has ONE entry (Bash matcher). The task is to ADD a second entry (Write|Edit matcher) to this array — NOT replace the existing entry. The final array must have BOTH entries.

From .claude/commands/knz-validate.md (current end of file — Conversation Output section structure):
- tier_1_failures == 0 path: shows pass message + quality ratings + "Run /knz-approve..." text
- State Update section follows Conversation Output (updates Stage 9 status and Session Continuity)
- The Auto-Trigger Metaskills section must be added AFTER the State Update section (same as how knz-sessions.md adds Auto-Trigger Validation after its state update)

Directory-to-stage mapping for pre-tool-use.sh (EXACT — do not infer):
```
00-project-brief/  → Stage 1 (prereq: none — always allow)
01-outcomes/       → Stage 2 (prereq: Stage 1 complete)
02-assessments/    → Stage 3 (prereq: Stage 2 complete)
03-modules/        → Stage 4 (prereq: Stage 3 complete)
04-sessions/       → Stage 5 (prereq: Stage 4 complete)
05-metaskills/     → Stage 6 (prereq: Stage 5 complete)
06-transfer/       → Stage 7 (prereq: Stage 6 complete)
07-marketing/      → Stage 8 (prereq: Stage 7 complete)
08-validation/     → Stage 9 (always allow — validation runs anytime after Stage 5)
```
Warning: directory prefix 05 corresponds to pipeline Stage 6, not Stage 5. The mapping must be explicit — do not derive stage numbers from directory numbers.

Block message format (exact):
"Stage N ([Stage Name]) requires Stage N-1 ([Prereq Stage Name]) to be complete. Stage N-1 is currently [status]. Run /knz-[command] to finish it first."

STATE.md stage status parse pattern:
The Stage Progress table in STATE.md uses rows like `| 5 | Session Content | complete |`.
Parse using: `grep -A1 "| $PREREQ_NUM " "$STATE_FILE" | grep -oE 'not-started|in-progress|complete' | head -1`
This extracts the status value from the row matching the stage number.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PreToolUse hook and update settings.json</name>
  <files>.claude/hooks/pre-tool-use.sh, .claude/settings.json</files>
  <action>
**Part A: Create `.claude/hooks/pre-tool-use.sh`**

Create the bash script using the full pattern from 07-RESEARCH.md "PreToolUse Hook — Full Pattern" code example exactly. The script:

1. Reads stdin as JSON (the tool call input from Claude Code)
2. Extracts `tool_name` and `tool_input.file_path` via python3 JSON parsing
3. Matches the file path against `workspace/([^/]+)/(0[0-9]-[^/]+)/` regex
4. If no match: `exit 0` (allow — not a workspace stage directory write)
5. Extracts `PROJECT_NAME` and `STAGE_DIR` from the regex match
6. Resolves `STATE_FILE` as `$PROJECT_DIR/workspace/$PROJECT_NAME/STATE.md` where `PROJECT_DIR="${CLAUDE_PROJECT_DIR:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}"`
7. Uses the EXACT directory-to-stage mapping from the interfaces block above (case statement on `$STAGE_DIR`):
   - `00-*` → PREREQ_NUM=0 (always allow via `[ "$PREREQ_NUM" -eq 0 ] && exit 0`)
   - `01-*` → PREREQ_NUM=1, PREREQ_NAME="Intake", STAGE_NUM=2, STAGE_NAME="Outcome Design", PREREQ_CMD="/knz-intake"
   - `02-*` → PREREQ_NUM=2, PREREQ_NAME="Outcome Design", STAGE_NUM=3, STAGE_NAME="Assessment Design", PREREQ_CMD="/knz-outcomes"
   - `03-*` → PREREQ_NUM=3, PREREQ_NAME="Assessment Design", STAGE_NUM=4, STAGE_NAME="Module Structure", PREREQ_CMD="/knz-assessments"
   - `04-*` → PREREQ_NUM=4, PREREQ_NAME="Module Structure", STAGE_NUM=5, STAGE_NAME="Session Content", PREREQ_CMD="/knz-modules"
   - `05-*` → PREREQ_NUM=5, PREREQ_NAME="Session Content", STAGE_NUM=6, STAGE_NAME="Metaskill Mapping", PREREQ_CMD="/knz-sessions"
   - `06-*` → PREREQ_NUM=6, PREREQ_NAME="Metaskill Mapping", STAGE_NUM=7, STAGE_NAME="Transfer Ecosystem", PREREQ_CMD="/knz-metaskills"
   - `07-*` → PREREQ_NUM=7, PREREQ_NAME="Transfer Ecosystem", STAGE_NUM=8, STAGE_NAME="Marketing Derivation", PREREQ_CMD="/knz-transfer"
   - `08-*` → `exit 0` (validation always allowed)
   - `*` → `exit 0` (unknown directory always allowed)
8. If STATE.md not found: `exit 0` (graceful absence — workspace not initialized)
9. Parses prereq stage status from STATE.md: `grep -A1 "| $PREREQ_NUM " "$STATE_FILE" | grep -oE 'not-started|in-progress|complete' | head -1`
10. If status = "complete": `exit 0` (allow)
11. If not complete: output deny JSON with the exact block message format and exit 0

Deny JSON output via python3:
```python
python3 -c "
import json, sys
print(json.dumps({
  'hookSpecificOutput': {
    'hookEventName': 'PreToolUse',
    'permissionDecision': 'deny',
    'permissionDecisionReason': sys.argv[1]
  }
}))" "$REASON"
```

Make the file executable: `chmod +x .claude/hooks/pre-tool-use.sh`

**Part B: Update `.claude/settings.json`**

Read the current settings.json (shown in interfaces block above). The `hooks.PreToolUse` array currently has ONE entry for `"matcher": "Bash"`. Add a SECOND entry to the array for `"matcher": "Write|Edit"`:

```json
{
  "matcher": "Write|Edit",
  "hooks": [{
    "type": "command",
    "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/pre-tool-use.sh\"",
    "timeout": 10
  }]
}
```

The final `hooks.PreToolUse` array must contain BOTH entries:
1. The existing Bash entry (secrets check — unchanged)
2. The new Write|Edit entry (stage sequencing)

Write the complete updated settings.json. Do not remove or modify any other hook entries (PreCompact, Stop).
  </action>
  <verify>
    <automated>ls /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/hooks/pre-tool-use.sh && python3 -c "import json; d=json.load(open('/Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/settings.json')); hooks=d['hooks']['PreToolUse']; assert len(hooks)==2, f'Expected 2 PreToolUse entries, got {len(hooks)}'; assert any(h['matcher']=='Write|Edit' for h in hooks), 'Write|Edit matcher missing'; assert any(h['matcher']=='Bash' for h in hooks), 'Bash matcher was removed'; print('OK')"</automated>
  </verify>
  <done>pre-tool-use.sh exists and is executable. settings.json has exactly two PreToolUse entries: the original Bash entry and the new Write|Edit entry. The python3 validation command prints "OK".</done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-chain in /knz-validate and extend /knz-approve final gate</name>
  <files>.claude/commands/knz-validate.md, .claude/commands/knz-approve.md</files>
  <action>
**Part A: Add auto-chain section to `.claude/commands/knz-validate.md`**

Read the current knz-validate.md. The file ends with a "State Management Rules" section listing prohibited phrases. Find the "State Update" section (which contains the `tier_1_failures == 0` and `tier_1_failures > 0` conditional state update logic).

Add a NEW section called "## Auto-Trigger Metaskills" immediately AFTER the State Update section and BEFORE the State Management Rules section.

This section fires ONLY when `tier_1_failures == 0`. Pattern mirrors the Auto-Trigger Validation section in knz-sessions.md.

New section content:
```markdown
## Auto-Trigger Metaskills

This section runs ONLY when `tier_1_failures == 0` (same condition as the all-pass state update path above).

Do NOT trigger when `tier_1_failures > 0`. The chain stops at the failure message. User must fix and rerun.

After the state update completes (Stage 9 set to complete, Session Continuity updated), show:

> All required checks passed — mapping thinking skills now.

Then invoke `/knz-metaskills` as a Skill.

Do not wait for user input before invoking. This is the same auto-advance pattern used between `/knz-sessions` and `/knz-validate`.
```

Do not modify any other part of knz-validate.md. The State Management Rules section must remain as the final section.

**Part B: Extend `.claude/commands/knz-approve.md` Final Validation gate**

Read the current knz-approve.md. The file has a "Present stage output summary" section (Step 3) that handles both the Post-Assessment gate and Final Validation gate. The Final Validation gate currently reads from `08-validation/` and produces a summary under 10 lines.

Replace the Final Validation gate behavior in Step 3 with the full pipeline summary format. Find the `**For Final Validation gate, include:**` block and replace its content with the following:

```
**For Final Validation gate, show the complete pipeline summary:**

Read each stage directory ONLY if that stage's status is `complete` in STATE.md. For any stage not yet complete, show "Not yet generated" for that section.

Display in this format:

---
## Your Complete Curriculum Package

**[Program Name]** — [Duration] program for [audience description from Stage 1 in plain language]

**What participants learn:**
[Outcome count from 01-outcomes/] learning objectives spanning [Bloom span in plain language, e.g., "recall through create"] thinking levels

**How you'll know they learned it:**
[Assessment count from 02-assessments/] assessments — [formative count] during-program checks, [summative count] end-of-program assessments

**Program structure:**
[Module count from 03-modules/] modules, [session count from 04-sessions/] sessions

**Thinking skills activated:**
[If Stage 6 complete: list each metaskill with its named activation activity, e.g., "Exploring: See-Think-Wonder"]
[If Stage 6 not complete: Not yet generated]

**Transfer support:**
[If Stage 7 complete:]
Before: [readiness check format from 06-transfer/]
During: [real-work application count] real-work applications
After: Spaced follow-up at [intervals], [accountability structure type]
Measuring: [plain-language Kirkpatrick level] — [what's being measured]
[If Stage 7 not complete: Not yet generated]

**Marketing materials:**
[If Stage 8 complete: count of marketing elements — list element types, e.g., "program description, 3 learning promises, audience positioning"]
[If Stage 8 not complete: Not yet generated]

**Validation:**
[If Stage 9 complete and tier_1_failures == 0: All required checks passed]
[If Stage 9 complete and tier_1_failures > 0: N issues pending — list plain-language descriptions]
[If Stage 9 not complete: Not yet run]

---
This is your complete curriculum package. Is it ready to deliver?
```

Also update the AskUserQuestion text for the Final Validation gate (Step 4). Replace the existing "For Final Validation gate" AskUserQuestion with:

```
**For Final Validation gate:**
> Review the summary above — this is everything that will be in your delivered curriculum package.
>
> **Option 1: Approve — mark as delivery-ready** — Everything looks right. Mark this curriculum package as delivery-ready.
> **Option 2: I have concerns** — Something needs fixing before this ships. Describe the issue and I'll revise.
> **Option 3: Start this stage over** — The validation found serious issues. Restart Stage 9 from scratch.
```

Do not modify the Post-Assessment gate behavior, Step 1, Step 2, Step 5 (handle decision), or any other part of knz-approve.md.
  </action>
  <verify>
    <automated>grep -c "Auto-Trigger Metaskills" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/commands/knz-validate.md && grep -c "Complete Curriculum Package" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/commands/knz-approve.md</automated>
  </verify>
  <done>knz-validate.md contains "Auto-Trigger Metaskills" section (grep returns 1). knz-approve.md contains "Complete Curriculum Package" summary format (grep returns 1). The auto-trigger section in knz-validate.md appears after the State Update section and before State Management Rules. The final gate in knz-approve.md reads each stage directory conditionally (only if complete in STATE.md).</done>
</task>

</tasks>

<verification>
After both tasks:
- `bash -n .claude/hooks/pre-tool-use.sh` — bash syntax check exits 0
- `python3 -c "import json; json.load(open('.claude/settings.json'))"` — settings.json is valid JSON
- settings.json PreToolUse array has exactly 2 entries (Bash + Write|Edit)
- knz-validate.md Auto-Trigger Metaskills section is present and fires only on tier_1_failures == 0
- knz-approve.md Final Validation gate shows the 8-section pipeline summary (program overview, outcomes, assessments, modules/sessions, metaskill activations, transfer layer, marketing elements, validation)
- pre-tool-use.sh maps 05-* to Stage 6 (not Stage 5) — the directory-stage mapping is explicit
- pre-tool-use.sh allows writes to 08-validation/ unconditionally (validation is always allowed)
- pre-tool-use.sh exits 0 when STATE.md is not found (graceful absence)
</verification>

<success_criteria>
- PreToolUse hook exists, is executable, uses explicit directory-to-stage mapping, and outputs deny JSON with actionable message when prereq is not complete
- settings.json has both the original Bash entry and the new Write|Edit entry in the PreToolUse array
- /knz-validate auto-triggers /knz-metaskills after successful validation (tier_1_failures == 0 path only)
- /knz-approve Final Validation gate presents complete 8-section pipeline summary before AskUserQuestion gate
- All enforcement is at file-write level (hook) not just conversational level (command prerequisite checks)
</success_criteria>

<output>
After completion, create `.planning/phases/07-full-pipeline-completion/07-02-SUMMARY.md`
</output>
