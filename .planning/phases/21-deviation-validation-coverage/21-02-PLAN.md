---
phase: 21-deviation-validation-coverage
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/plugins/curriculum/commands/transfer.md
  - .claude/plugins/curriculum/commands/marketing.md
autonomous: true
requirements: [DEVL-02]

must_haves:
  truths:
    - "Transfer (stage 7) writes to _drafts/ first and runs audit checks before promoting to deliverable directory"
    - "Marketing (stage 8) writes to _drafts/ first and runs audit checks before promoting to deliverable directory"
    - "Transfer draft audit includes the 4 standard checks plus stage-specific checks for transfer layers, implementation intentions, spaced retrieval, and community continuation"
    - "Marketing draft audit includes the 4 standard checks plus stage-specific checks for source traceability and word count ratio"
    - "Both stages use the same 3-attempt retry pattern as stages 4-6"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/transfer.md"
      provides: "Draft-then-audit pipeline with stage-specific checks and retry"
      contains: "_drafts/"
    - path: ".claude/plugins/curriculum/commands/marketing.md"
      provides: "Draft-then-audit pipeline with stage-specific checks and retry"
      contains: "_drafts/"
  key_links:
    - from: ".claude/plugins/curriculum/commands/transfer.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "vocabulary check in draft audit"
      pattern: "curriculum-voice.md"
    - from: ".claude/plugins/curriculum/commands/marketing.md"
      to: ".claude/reference/curriculum-voice.md"
      via: "vocabulary check in draft audit"
      pattern: "curriculum-voice.md"
    - from: ".claude/plugins/curriculum/commands/transfer.md"
      to: "curriculum-registry.json"
      via: "registry consistency check in draft audit"
      pattern: "curriculum-registry"
---

<objective>
Add the full draft-then-audit pipeline to transfer.md (stage 7) and marketing.md (stage 8), including stage-specific content checks, 3-attempt retry with cumulative constraints, and escalation.

Purpose: Currently stages 7-8 write directly to deliverable directories with no audit gate. This creates a gap where the last two content stages skip the quality checks that stages 4-6 enforce. After this plan, every content-generating stage has a promotion gate.

Output: Updated transfer.md and marketing.md with _drafts/ write targets, audit checks, retry logic, and escalation.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deviation-validation-coverage/21-CONTEXT.md
@.planning/phases/19-pipeline-infrastructure/19-02-SUMMARY.md
@.planning/phases/20-integrity-verification/20-02-SUMMARY.md

<interfaces>
<!-- Draft-then-audit pattern from modules.md (the template to follow) -->

Pattern: Generate to `{stage-dir}/_drafts/`, run checks, auto-fix vocab, retry content failures up to 3 times, promote if all pass, escalate if not.

Four standard checks (present in all draft-audit stages):
1. File Completeness — verify files exist in _drafts/ with non-zero content
2. Registry Consistency — verify outcome wording, module refs, etc. match registry
3. Vocabulary Scan — check against curriculum-voice.md never-say table
4. Schema Compliance — verify required fields per stage schema

From transfer.md current "On Approve" section (line ~227):
- Writes directly to `workspace/{project}/06-transfer/transfer-ecosystem.md`
- No _drafts/ step, no audit checks

From marketing.md current "File Write" section (line ~151):
- Writes directly to `workspace/{project}/07-marketing/marketing-package.md`
- No _drafts/ step, no audit checks

Stage-specific checks from CONTEXT.md and schema:
Transfer (T1-25 to T1-30): three transfer layers present, implementation intentions per module, error management for open skills, spaced retrieval count matches schedule, evaluation meets minimum, community not placeholder
Marketing (T1-31 to T1-33): source citation on every element, supporting element exists in stage output, word count under 25% of curriculum
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add draft-then-audit pipeline to transfer.md</name>
  <files>.claude/plugins/curriculum/commands/transfer.md</files>
  <action>
Modify transfer.md to use the draft-then-audit pattern. Three changes:

**Change 1: Write target to _drafts/**

In the "On Approve and continue" section (line ~229), change the write target from:
`workspace/{project}/06-transfer/transfer-ecosystem.md`
to:
`workspace/{project}/06-transfer/_drafts/transfer-ecosystem.md`

Add directory creation: "Create `06-transfer/_drafts/` if it does not exist."

**Change 2: Add Draft Audit section**

After the file write step and before the STATE.md update, add:

```
### Draft Audit

Run these 10 checks against the file in `workspace/{project}/06-transfer/_drafts/`. All 10 must pass before promotion.

**Check 1: File Completeness**
Verify `transfer-ecosystem.md` exists in `_drafts/` with non-zero content containing all required sections: Before the Program, During the Program, After the Program, How We'll Know It Worked.

**Check 2: Registry Consistency**
Read `curriculum-registry.json`. Verify:
- Module references in follow-through plans match module IDs in registry `time_allocations`
- Program outcomes referenced match registry `outcome_wording.program_outcomes`
- Contact hours assumptions match registry `learner_profile.data.contact_hours`

This is a blocking failure — inconsistent references mean the transfer design is based on stale data.

**Check 3: Vocabulary Scan**
Read `.claude/reference/curriculum-voice.md`. Scan the draft for any term in the never-say table. This is auto-fixable — substitute with the plain-language replacement.

**Check 4: Schema Compliance**
Load `.claude/reference/schemas/stage-07-transfer.md`. Verify all required fields for the program's duration tier are covered in the narrative (the file is prose, but it must address every required schema element).

**Check 5: Transfer Layer Coverage (T1-25)**
Verify all three transfer layers are present in the narrative: pre-program (Before the Program section), in-program (During the Program section), post-program (After the Program section). Each section must contain substantive content, not placeholder text.

This is a blocking failure — requires regeneration.

**Check 6: Implementation Intentions (T1-26)**
Verify that the During the Program section references specific follow-through commitments for each module (at minimum one per module from the module list). Generic references like "each module includes a follow-through plan" without naming the modules are not acceptable.

This is a blocking failure — requires regeneration.

**Check 7: Error Management (T1-27)**
If `skill_type` from the registry is "open": verify the During section includes error correction practice with specific error types and correction processes. If `skill_type` is "closed": this check passes automatically.

This is a blocking failure — requires regeneration.

**Check 8: Spaced Retrieval Match (T1-28)**
Verify the After the Program section describes spaced follow-up at the intervals matching the duration scaling rules (1 week only for < 2 hours; 1 week + 1 month for 2-16 hours; all three intervals for > 16 hours). The number of described follow-up touchpoints must match the number of intervals.

This is a blocking failure — requires regeneration.

**Check 9: Evaluation Level (T1-29)**
Verify the How We'll Know It Worked section describes a measurement approach at or above the minimum level for this program type (Level 2 for short closed-skill; Level 3 for short open-skill or longer programs; Level 4 when business outcomes are specified). If below minimum, check whether `minimum_level_justification` rationale is present in the narrative.

This is a blocking failure — requires regeneration.

**Check 10: Community Continuation (T1-30)**
Verify the After the Program section includes community continuation content that is not empty, not "TBD", and not placeholder text. Must describe a specific continuation mechanism (platform, format, or activity).

This is a blocking failure — requires regeneration.
```

**Change 3: Add Audit Result with retry and escalation**

After the 10 checks, add the full deviation handling pattern:

- Auto-fix pass (vocabulary substitution, registry defaults, outcome drift only)
- 3-attempt retry for content failures (checks 5-10) with cumulative constraint injection
- Structural failures (checks 1, 2, 4) stop immediately — no retry
- Escalation after 3 attempts with problem + location + suggestion format
- On all checks pass: promote from `_drafts/` to `workspace/{project}/06-transfer/transfer-ecosystem.md` (move, not copy), delete `_drafts/`, proceed to existing STATE.md update and completion message

Follow the exact same pattern structure from modules.md (established in Plan 01 / Phase 19-02), including the Verification Integrity section from Phase 20-01 (do not downgrade severity, do not bypass checks, prohibited qualifiers list). Copy the Verification Integrity block from modules.md's Draft Audit section.
  </action>
  <verify>
    <automated>grep -c "_drafts/" .claude/plugins/curriculum/commands/transfer.md | grep -q -v "^0$" && grep -c "Check 10" .claude/plugins/curriculum/commands/transfer.md | grep -q -v "^0$" && grep -c "Escalation" .claude/plugins/curriculum/commands/transfer.md | grep -q -v "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>transfer.md writes to _drafts/, runs 10 audit checks (4 standard + 6 stage-specific for T1-25 through T1-30), has 3-attempt retry with cumulative constraints, and escalates with plain-language problem descriptions after 3 failures</done>
</task>

<task type="auto">
  <name>Task 2: Add draft-then-audit pipeline to marketing.md</name>
  <files>.claude/plugins/curriculum/commands/marketing.md</files>
  <action>
Modify marketing.md to use the draft-then-audit pattern. Three changes:

**Change 1: Write target to _drafts/**

In the "File Write" section (line ~152), change the write target from:
`workspace/{project}/07-marketing/marketing-package.md`
to:
`workspace/{project}/07-marketing/_drafts/marketing-package.md`

Add directory creation: "Create `07-marketing/_drafts/` if it does not exist."

**Change 2: Add Draft Audit section**

After the file write and before the Conversation Display section, add:

```
### Draft Audit

Run these 7 checks against the file in `workspace/{project}/07-marketing/_drafts/`. All 7 must pass before promotion.

**Check 1: File Completeness**
Verify `marketing-package.md` exists in `_drafts/` with non-zero content containing required sections for this program's duration tier (at minimum: The Promise, Program Description, Source Traceability).

**Check 2: Registry Consistency**
Read `curriculum-registry.json`. Verify:
- Learning promises reference outcomes that exist in registry `outcome_wording.program_outcomes`
- Audience description aligns with registry `learner_profile.data.target_audience`
- Program duration references match registry `learner_profile.data.contact_hours`

This is a blocking failure.

**Check 3: Vocabulary Scan**
Read `.claude/reference/curriculum-voice.md`. Scan the draft for any term in the never-say table, plus the marketing-specific prohibited terms: schema, curriculum_traceability, bloom_level, learning_objective_id, element_type, claim_type. Auto-fixable for never-say substitutions. Marketing-specific terms should not appear in prose at all (they are file metadata only).

**Check 4: Schema Compliance**
Load `.claude/reference/schemas/stage-08-marketing.md`. Verify all required elements for this duration tier are present. Verify source_citation and source_link fields are populated for every claim.

**Check 5: Source Citation Completeness (T1-31)**
Verify every marketing claim in the prose sections has a corresponding entry in the Source Traceability table with a populated source reference. Claims without sources are not acceptable — every promise must trace to a curriculum element.

This is a blocking failure — requires regeneration.

**Check 6: Source Element Existence (T1-32)**
For each entry in the Source Traceability table, verify the referenced stage output actually exists. A source_citation of "stage-02 OBJ-03" must correspond to an actual objective in 01-outcomes/learning-objectives.md. A reference to "stage-07 real_work_application session-01" must correspond to content in 06-transfer/transfer-ecosystem.md.

This is a blocking failure — requires regeneration.

**Check 7: Marketing Ratio (T1-33)**
Count the total word count of the marketing package prose (excluding the traceability table). Count the total word count of all curriculum output files in stages 01-07. Verify marketing word count is less than 25% of curriculum word count.

This is a blocking failure — trim learning promises first (keeping most traceable), then audience positioning. Keep program description and direct-strength traceability claims.
```

**Change 3: Add Audit Result with retry and escalation**

After the 7 checks, add the full deviation handling pattern:

- Auto-fix pass (vocabulary substitution only — marketing has fewer auto-fixable categories since it doesn't have outcome drift in the same sense)
- 3-attempt retry for content failures (checks 5-7) with cumulative constraint injection
- Marketing ratio (Check 7) auto-fix on retry: when ratio exceeds 25%, the retry instruction should specify trimming targets: "Trim learning promises to the N most traceable. Remove audience positioning section if ratio still exceeds threshold."
- Structural failures (checks 1, 2, 4) stop immediately
- Escalation after 3 attempts
- On all checks pass: promote from `_drafts/` to `workspace/{project}/07-marketing/marketing-package.md` (move, not copy), delete `_drafts/`

After promotion, the existing Conversation Display section runs unchanged. Move the STATE.md update to after promotion (currently it's at the end — keep it there but make it conditional on successful promotion).

Add Verification Integrity section (same block as other draft-audit commands — copy from modules.md).
  </action>
  <verify>
    <automated>grep -c "_drafts/" .claude/plugins/curriculum/commands/marketing.md | grep -q -v "^0$" && grep -c "Check 7" .claude/plugins/curriculum/commands/marketing.md | grep -q -v "^0$" && grep -c "Escalation" .claude/plugins/curriculum/commands/marketing.md | grep -q -v "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>marketing.md writes to _drafts/, runs 7 audit checks (4 standard + 3 stage-specific for T1-31 through T1-33), has 3-attempt retry with cumulative constraints, and escalates with plain-language problem descriptions after 3 failures</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. transfer.md writes to `06-transfer/_drafts/` and has 10 audit checks covering T1-25 through T1-30
2. marketing.md writes to `07-marketing/_drafts/` and has 7 audit checks covering T1-31 through T1-33
3. Both files have 3-attempt retry with cumulative constraint injection
4. Both files have Verification Integrity sections (anti-softening enforcement)
5. Both files have escalation presentation in plain language
6. Promotion logic moves files from _drafts/ to deliverable directory only after all checks pass
7. STATE.md updates only happen after successful promotion
</verification>

<success_criteria>
- Stages 7 and 8 have full draft-then-audit pipelines matching the pattern from stages 4-6
- Stage-specific checks cover all T1-25 through T1-33 requirements
- 3-attempt retry with escalation is consistent across all draft-audit stages
- No content reaches deliverable directories without passing audit
</success_criteria>

<output>
After completion, create `.planning/phases/21-deviation-validation-coverage/21-02-SUMMARY.md`
</output>
