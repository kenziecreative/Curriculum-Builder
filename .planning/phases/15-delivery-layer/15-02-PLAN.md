---
phase: 15-delivery-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - .claude/plugins/curriculum/commands/assemble.md
autonomous: true
requirements:
  - DLVR-01

must_haves:
  truths:
    - "Running /curriculum:assemble produces a delivery/ directory organized by session with the correct files in each subfolder"
    - "Marketing materials and facilitator guide HTML versions are generated alongside their markdown during assembly"
    - "When a stage is not yet complete, the assembler notes what is missing and which command to run — it does not silently produce an incomplete package"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/assemble.md"
      provides: "Standalone assemble command — copies delivery-scoped files, calls generate-html.js, shows plain-language file list"
      min_lines: 60
  key_links:
    - from: ".claude/plugins/curriculum/commands/assemble.md"
      to: ".claude/plugins/curriculum/scripts/generate-html.js"
      via: "Bash tool: node {script_path} {workspace_path} {project_name}"
      pattern: "generate-html.js"
    - from: "assemble.md (delivery copy logic)"
      to: "workspace/*/04-sessions/M-N-S-N/facilitator-guide.md"
      via: "explicit named file copy — not session.md, not slide-outline.md"
      pattern: "facilitator-guide.md"
---

<objective>
Write the /curriculum:assemble command that compiles delivery-scoped files into a session-organized delivery/ directory and generates HTML for facilitator guides and marketing materials.

Purpose: The assembled delivery/ directory is the handoff artifact — what the program sponsor receives. It must contain exactly the right files, organized for delivery, not raw pipeline output.
Output: assemble.md command file that produces a correct delivery/ structure on every run.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-delivery-layer/15-CONTEXT.md
@.planning/phases/15-delivery-layer/15-RESEARCH.md
@.planning/phases/15-delivery-layer/15-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From .claude/plugins/curriculum/commands/validate.md (the task-agent dispatch pattern to follow):
- Command checks prerequisites, dispatches ONE Task, verifies output files, shows plain-language summary, silently updates STATE.md
- assemble.md is simpler: no task agent needed — the command itself handles file copy and calls generate-html.js via Bash

Delivery directory structure (locked decision from CONTEXT.md):
```
workspace/{project}/delivery/
├── session-1/
│   ├── facilitator-guide.md     ← copied from 04-sessions/M-N-S-N/
│   ├── facilitator-guide.html   ← generated by generate-html.js
│   └── participant-materials.md ← copied from 04-sessions/M-N-S-N/
├── session-2/
│   ├── facilitator-guide.md
│   ├── facilitator-guide.html
│   └── participant-materials.md
├── transfer-plan.md             ← copied from 06-transfer/ (markdown only)
├── {marketing-filename}.md      ← copied from 07-marketing/
└── {marketing-filename}.html    ← generated by generate-html.js
```

Source file mapping (exact file names confirmed from sessions.md command):
- 04-sessions/M-N-S-N/facilitator-guide.md → delivery/session-N/facilitator-guide.md
- 04-sessions/M-N-S-N/participant-materials.md → delivery/session-N/participant-materials.md
- 06-transfer/transfer-plan.md → delivery/transfer-plan.md (or first .md file if named differently)
- 07-marketing/*.md → delivery/{same filename}.md

Excluded (do NOT copy): session.md, slide-outline.md, anything in 01-outcomes/, 02-assessments/, 03-modules/, 08-validation/

Session subdirectory naming: sessions.md writes directories as M-N-S-N (e.g., M-1-S-1, M-1-S-2).
In delivery/, these become session-1, session-2... in sequential order (sorted).

From .claude/plugins/curriculum/commands/approve.md (integration point):
- approve.md currently has no assemble call — Plan 03 will add it
- assemble.md must work as a standalone command AND be callable from approve.md

generate-html.js script location: .claude/plugins/curriculum/scripts/generate-html.js
Call pattern: node {absolute path to script} {absolute workspace path} {project name}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write assemble.md command</name>
  <files>.claude/plugins/curriculum/commands/assemble.md</files>
  <action>
    Create .claude/plugins/curriculum/commands/assemble.md as a new orchestrator command.

    Structure (follow validate.md pattern):

    **Frontmatter:**
    ```yaml
    ---
    description: Compile approved pipeline output into a delivery-ready package
    ---
    ```

    **Persona section:**
    Read .claude/reference/curriculum-voice.md before any user-facing output. Tone: confident colleague confirming a package is ready to hand off. Lead with what's in the package, not the process.

    **Step 1 — Find active project:**
    Same pattern as other commands: glob workspace/*/STATE.md. Handle no-project and multi-project cases (same pattern as approve.md).

    **Step 2 — Read session list from STATE.md:**
    Read workspace/{project}/STATE.md to get the list of sessions (Stage 5 status and session count). If Stage 5 is not complete, show:
    > Sessions haven't been generated yet. Run `/curriculum:sessions` first.
    Stop.

    **Step 3 — Assemble delivery files:**
    Create workspace/{project}/delivery/ directory (overwrite if it exists — re-running is safe).

    For each session subdirectory in workspace/{project}/04-sessions/ (sorted alphabetically):
    - Assign a sequential delivery name: session-1, session-2, etc. (sort order determines numbering)
    - Create delivery/session-N/ directory
    - Copy facilitator-guide.md if it exists → delivery/session-N/facilitator-guide.md
    - Copy participant-materials.md if it exists → delivery/session-N/participant-materials.md
    - If either file is missing, note it (do not stop assembly — continue with other sessions)

    For transfer:
    - Read workspace/{project}/06-transfer/ — copy the first .md file found to delivery/transfer-plan.md
    - If no file exists, note it

    For marketing:
    - Read workspace/{project}/07-marketing/ — copy each .md file to delivery/{same filename}.md
    - If directory is empty or missing, note it

    **Step 4 — Generate HTML:**
    After file copy is complete, call generate-html.js via Bash:
    ```
    node {absolute path to .claude/plugins/curriculum/scripts/generate-html.js} {absolute workspace path} {project name}
    ```
    Use the Read tool to find the script's absolute path, or construct it from the known plugin directory. If the Bash call fails, note it but do not stop — the markdown files are still assembled.

    **Step 5 — Show plain-language file list:**
    Do NOT show a file count or a full directory tree. Show a readable list, for example:

    ```
    Your delivery package is ready.

    What's included:

    Session 1: facilitator guide + participant materials
    Session 2: facilitator guide + participant materials
    Transfer plan
    Program description (+ web version)
    Marketing email (+ web version)

    Files are in workspace/{project}/delivery/.
    ```

    For HTML files: add "(+ web version)" after any file that received an HTML counterpart.
    For missing files: list them under "Not yet generated:" with the command to run.

    **Warm handoff close:**
    > Your delivery package is assembled. Run `/curriculum:approve` to mark it delivery-ready.

    No context-clear nudge here — assemble is called from approve, and approve handles the close.

    **State update (silent):**
    No STATE.md update needed for assemble — it is a packaging step, not a pipeline stage.

    **Key constraints from locked decisions:**
    - Do NOT copy: session.md, slide-outline.md, 01-outcomes/, 02-assessments/, 03-modules/, 08-validation/
    - Partial assembly is acceptable — copy what's available, note what's missing (never silently drop content)
    - Re-running assemble always overwrites delivery/ — idempotent
    - HTML only for: facilitator guides + marketing files (NOT participant materials or transfer plan)
  </action>
  <verify>
    <automated>ls /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/assemble.md</automated>
  </verify>
  <done>assemble.md exists. Reading it confirms: it reads voice file, handles project lookup, copies facilitator-guide.md and participant-materials.md into session-N/ subdirectories, copies transfer and marketing files, calls generate-html.js via Bash, shows a plain-language file list (not a full tree), and notes any missing files with the fix command.</done>
</task>

</tasks>

<verification>
- .claude/plugins/curriculum/commands/assemble.md exists
- File contains references to: curriculum-voice.md, facilitator-guide.md, participant-materials.md, generate-html.js, delivery/session-
- File does NOT contain references to: session.md being copied, slide-outline.md being copied
- Partial assembly handling is present (note what's missing + which command to run)
</verification>

<success_criteria>
1. assemble.md exists as a complete command file
2. Delivery structure matches the locked decision: session-N/ subfolders with facilitator guide + participant materials, transfer at root, marketing at root
3. HTML generation call to generate-html.js is present
4. Missing-stage handling is explicit — partial assembly produces a noted, not silent, incomplete package
</success_criteria>

<output>
After completion, create `.planning/phases/15-delivery-layer/15-02-SUMMARY.md`
</output>
