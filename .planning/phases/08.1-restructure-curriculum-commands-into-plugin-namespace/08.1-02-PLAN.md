---
phase: 08.1-restructure-curriculum-commands-into-plugin-namespace
plan: 02
type: execute
wave: 2
depends_on:
  - 08.1-01
files_modified:
  - .claude/plugins/curriculum/commands/intake.md
  - .claude/plugins/curriculum/commands/validate.md
  - .claude/plugins/curriculum/commands/transfer.md
  - .claude/plugins/curriculum/commands/init.md
  - .claude/plugins/curriculum/commands/outcomes.md
  - .claude/plugins/curriculum/commands/approve.md
  - .claude/plugins/curriculum/commands/resume.md
  - .claude/plugins/curriculum/commands/assessments.md
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/metaskills.md
  - .claude/plugins/curriculum/commands/marketing.md
  - .claude/plugins/curriculum/commands/sessions.md
  - .claude/hooks/pre-tool-use.sh
  - .claude/hooks/session-start.md
autonomous: true
requirements:
  - PLG-01

must_haves:
  truths:
    - "Zero /knz-* references remain anywhere in .claude/"
    - "All command H1 headings reference /curriculum:* not /knz-*"
    - "pre-tool-use.sh PREREQ_CMD values all use /curriculum:* format"
    - "session-start.md user-facing instructions reference /curriculum:* commands"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/intake.md"
      provides: "intake command with updated /curriculum:* references"
      contains: "/curriculum:init"
    - path: ".claude/hooks/pre-tool-use.sh"
      provides: "hook with updated PREREQ_CMD values"
      contains: "/curriculum:intake"
  key_links:
    - from: ".claude/hooks/pre-tool-use.sh"
      to: "PREREQ_CMD values"
      via: "case statement"
      pattern: "/curriculum:"
    - from: ".claude/plugins/curriculum/commands/intake.md"
      to: "/curriculum:init"
      via: "user-facing instruction text"
      pattern: "/curriculum:init"
---

<objective>
Update every `/knz-*` invocation reference inside the moved command files, `pre-tool-use.sh`, and `session-start.md` so no stale `/knz-*` references remain anywhere in `.claude/`.

Purpose: Completes the namespace migration. Without this step, the plugin loads but user-facing messages and hook error text still reference nonexistent `/knz-*` commands.
Output: All 12 command files, 2 hook files updated. `grep -rn "/knz-" .claude/` returns zero matches.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-restructure-curriculum-commands-into-plugin-namespace/08.1-RESEARCH.md
@.planning/phases/08.1-restructure-curriculum-commands-into-plugin-namespace/08.1-01-SUMMARY.md
</context>

<interfaces>
<!-- Complete reference map from RESEARCH.md — what changes where -->

**Command files: internal /knz-* references per file**

intake.md:
- `/knz-init` → `/curriculum:init` (3 occurrences)
- `/knz-intake` → `/curriculum:intake` (5 occurrences — self-references)
- `/knz-outcomes` → `/curriculum:outcomes` (4 occurrences)

validate.md:
- `/knz-init` → `/curriculum:init`
- `/knz-sessions` → `/curriculum:sessions`
- `/knz-approve` → `/curriculum:approve`
- `/knz-validate` → `/curriculum:validate` (self-reference)
- `/knz-metaskills` → `/curriculum:metaskills`

transfer.md:
- `/knz-init` → `/curriculum:init`
- `/knz-metaskills` → `/curriculum:metaskills`
- `/knz-marketing` → `/curriculum:marketing`

init.md:
- `/knz-resume` → `/curriculum:resume`
- `/knz-intake` → `/curriculum:intake`

outcomes.md:
- `/knz-init` → `/curriculum:init`
- `/knz-assessments` → `/curriculum:assessments`
- `/knz-intake` → `/curriculum:intake`

approve.md:
- `/knz-init` → `/curriculum:init`
- `/knz-resume` → `/curriculum:resume`
- `/knz-intake` → `/curriculum:intake`

resume.md:
- `/knz-init` → `/curriculum:init`
- `/knz-intake` → `/curriculum:intake`
- `/knz-approve` → `/curriculum:approve`

assessments.md:
- `/knz-init` → `/curriculum:init`
- `/knz-outcomes` → `/curriculum:outcomes`
- `/knz-modules` → `/curriculum:modules`

modules.md:
- `/knz-init` → `/curriculum:init`
- `/knz-assessments` → `/curriculum:assessments`
- `/knz-sessions` → `/curriculum:sessions`

metaskills.md:
- `/knz-init` → `/curriculum:init`
- `/knz-sessions` → `/curriculum:sessions`
- `/knz-transfer` → `/curriculum:transfer`

marketing.md:
- `/knz-init` → `/curriculum:init`
- `/knz-transfer` → `/curriculum:transfer`
- `/knz-approve` → `/curriculum:approve`

sessions.md:
- `/knz-init` → `/curriculum:init`
- `/knz-modules` → `/curriculum:modules`
- `/knz-validate` → `/curriculum:validate`

**pre-tool-use.sh: 7 PREREQ_CMD values**
- `/knz-intake` → `/curriculum:intake`
- `/knz-outcomes` → `/curriculum:outcomes`
- `/knz-assessments` → `/curriculum:assessments`
- `/knz-modules` → `/curriculum:modules`
- `/knz-sessions` → `/curriculum:sessions`
- `/knz-metaskills` → `/curriculum:metaskills`
- `/knz-transfer` → `/curriculum:transfer`

**session-start.md: 3 occurrences**
- Line ~34: `Run /knz-intake to begin` → `Run /curriculum:intake to begin`
- Line ~64: `Use /knz-approve to continue` → `Use /curriculum:approve to continue`
- Line ~100: `Run /knz-modules to continue` → `Run /curriculum:modules to continue`

**CLAUDE.md: NO changes needed**
- Only `/knz-` reference in CLAUDE.md is `knz-builder-research/` (a directory name, not a command) — leave it alone
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update /knz-* references in all 12 command files and update H1 headings</name>
  <files>
    .claude/plugins/curriculum/commands/intake.md
    .claude/plugins/curriculum/commands/validate.md
    .claude/plugins/curriculum/commands/transfer.md
    .claude/plugins/curriculum/commands/init.md
    .claude/plugins/curriculum/commands/outcomes.md
    .claude/plugins/curriculum/commands/approve.md
    .claude/plugins/curriculum/commands/resume.md
    .claude/plugins/curriculum/commands/assessments.md
    .claude/plugins/curriculum/commands/modules.md
    .claude/plugins/curriculum/commands/metaskills.md
    .claude/plugins/curriculum/commands/marketing.md
    .claude/plugins/curriculum/commands/sessions.md
  </files>
  <action>
    In each of the 12 command files at `.claude/plugins/curriculum/commands/`, make two types of changes:

    **Change 1 — H1 heading update:** Change the opening `# /knz-commandname` heading to `# /curriculum:commandname`. For example:
    - `# /knz-intake` → `# /curriculum:intake`
    - `# /knz-sessions — Session Generator Orchestrator` → `# /curriculum:sessions — Session Generator Orchestrator`
    - `# /knz-validate — ...` → `# /curriculum:validate — ...`

    **Change 2 — Internal reference updates:** Replace every `/knz-*` invocation reference (any place a user would be told to run a command, or a chaining reference invokes another command) with its `/curriculum:*` equivalent. Use the complete reference map in the `<interfaces>` block above — it lists every file and every occurrence.

    Use the Edit tool for each file. Read the file first, then apply targeted replacements. For intake.md (675+ lines), be thorough — it has the most references (12 total across 3 command names).

    After editing all 12 files, run the intermediate grep to confirm command files are clean:
    ```bash
    grep -rn "/knz-" .claude/plugins/curriculum/commands/
    ```
    Expected: zero matches.
  </action>
  <verify>
    <automated>grep -rn "/knz-" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/</automated>
  </verify>
  <done>Zero /knz-* matches in .claude/plugins/curriculum/commands/. All 12 H1 headings use /curriculum:* format.</done>
</task>

<task type="auto">
  <name>Task 2: Update hook files and run final zero-reference verification</name>
  <files>
    .claude/hooks/pre-tool-use.sh
    .claude/hooks/session-start.md
  </files>
  <action>
    **pre-tool-use.sh:**
    Read `.claude/hooks/pre-tool-use.sh`. Find the case statement with PREREQ_CMD assignments. Update all 7 values:
    ```bash
    # Before:
    PREREQ_CMD="/knz-intake"
    PREREQ_CMD="/knz-outcomes"
    PREREQ_CMD="/knz-assessments"
    PREREQ_CMD="/knz-modules"
    PREREQ_CMD="/knz-sessions"
    PREREQ_CMD="/knz-metaskills"
    PREREQ_CMD="/knz-transfer"

    # After:
    PREREQ_CMD="/curriculum:intake"
    PREREQ_CMD="/curriculum:outcomes"
    PREREQ_CMD="/curriculum:assessments"
    PREREQ_CMD="/curriculum:modules"
    PREREQ_CMD="/curriculum:sessions"
    PREREQ_CMD="/curriculum:metaskills"
    PREREQ_CMD="/curriculum:transfer"
    ```

    **session-start.md:**
    Read `.claude/hooks/session-start.md`. Find the 3 occurrences of `/knz-*` command references (approximately lines 34, 64, 100) and update each:
    - `Run /knz-intake to begin` → `Run /curriculum:intake to begin`
    - `Use /knz-approve to continue` → `Use /curriculum:approve to continue`
    - `Run /knz-modules to continue` → `Run /curriculum:modules to continue`

    **Final zero-reference check:**
    After both hook files are updated, run the full verification grep across the entire `.claude/` directory:
    ```bash
    grep -rn "/knz-" .claude/
    ```
    This must return zero matches. If any matches remain, fix them before marking this task done.

    Note: `knz-builder-research/` in CLAUDE.md is a directory path reference, NOT a command invocation — it is intentionally excluded from this pattern by not starting with `/knz-`. It will NOT appear in the grep results.
  </action>
  <verify>
    <automated>grep -rn "/knz-" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/</automated>
  </verify>
  <done>Zero /knz-* matches anywhere in .claude/. pre-tool-use.sh PREREQ_CMD values all use /curriculum:* format. session-start.md user-facing instructions reference /curriculum:* commands.</done>
</task>

</tasks>

<verification>
```bash
# Final zero-reference check — must be empty output
grep -rn "/knz-" .claude/

# Spot-check key files updated
grep "curriculum:intake" .claude/plugins/curriculum/commands/intake.md
grep "curriculum:intake" .claude/hooks/pre-tool-use.sh
grep "curriculum:intake" .claude/hooks/session-start.md
```
</verification>

<success_criteria>
- `grep -rn "/knz-" .claude/` returns zero matches
- All 12 command file H1 headings use `/curriculum:*` format
- `pre-tool-use.sh` has 7 PREREQ_CMD values using `/curriculum:*`
- `session-start.md` references `/curriculum:intake`, `/curriculum:approve`, `/curriculum:modules`
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-restructure-curriculum-commands-into-plugin-namespace/08.1-02-SUMMARY.md`
</output>
