---
phase: 08.1
slug: restructure-curriculum-commands-into-plugin-namespace
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-22
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual verification — file migration with no automated test suite |
| **Config file** | none |
| **Quick run command** | `grep -rn "/knz-" .claude/` |
| **Full suite command** | `grep -rn "/knz-" .claude/ && ls .claude/plugins/curriculum/ && ls .claude/plugins/curriculum/.claude-plugin/` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `grep -rn "/knz-" .claude/`
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green (zero grep matches, correct dirs present)
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 8.1-xx-01 | 01 | 1 | Plugin structure | structural | `ls .claude/plugins/curriculum/.claude-plugin/plugin.json` | ✅ W1 | ⬜ pending |
| 8.1-xx-02 | 01 | 1 | Commands moved | grep | `grep -rn "/knz-" .claude/commands/` | ✅ W1 | ⬜ pending |
| 8.1-xx-03 | 01 | 1 | Refs updated | grep | `grep -rn "/knz-" .claude/` | ✅ W1 | ⬜ pending |
| 8.1-xx-04 | 01 | 1 | Agents moved | structural | `ls .claude/plugins/curriculum/agents/` | ✅ W1 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*None — existing infrastructure covers all phase requirements. This is a file migration; no new test files needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `/curriculum:intake` resolves in Claude Code | Plugin activation | Requires live Claude Code session | Open Claude Code, type `/curriculum:intake`, verify command appears |
| `/curriculum:init` resolves | Plugin activation | Requires live Claude Code session | Open Claude Code, type `/curriculum:init`, verify command appears |
| Old `/knz-*` commands are gone | No stale commands | Requires live Claude Code session | Type `/knz-intake` → expect "unknown command" |
| Hook PREREQ_CMD values updated | Internal ref audit | Spot-check | Read `pre-tool-use.sh`, verify PREREQ_CMD values use `/curriculum:*` |
| CLAUDE.md references updated | Docs integrity | Manual review | Grep CLAUDE.md for `/knz-` → zero matches |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
