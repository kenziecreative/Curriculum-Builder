---
phase: 14-audit-mode-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .claude/plugins/curriculum/commands/intake.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-03

must_haves:
  truths:
    - "intake.md spawns curriculum-auditor.md as a Task subagent and reads the returned audit-results.md file"
    - "The audit mode logic in intake.md Steps 1–6 is replaced by a single dispatch call + file read"
    - "After the extraction table is shown, a mode confirmation table appears with plain-language 'What will happen' descriptions before any follow-up questions"
    - "Mode names gap-fill / enrich / hands-off never appear in any user-facing output — user sees 'Build from scratch' / 'Fill in what's missing' / 'Keep what you have and validate it'"
    - "Per-stage mode overrides are accepted at the confirmation gate and written to STATE.md Mode Assignment table"
    - "intake.md parses audit-results.md using content_quality column for mode assignment — not extraction_confidence"
    - "MODE Assignment per stage is written to STATE.md so downstream commands can read a simple key-value, not re-parse the full table"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/intake.md"
      provides: "Dispatcher that spawns auditor, reads results, shows mode confirmation"
      contains: "curriculum-auditor.md"
  key_links:
    - from: "intake.md audit mode section"
      to: "curriculum-auditor.md"
      via: "Task subagent dispatch"
      pattern: "curriculum-auditor"
    - from: "intake.md"
      to: "workspace/{project}/STATE.md"
      via: "Mode Assignment table write"
      pattern: "Mode Assignment"
---

<objective>
Refactor intake.md's Audit Mode section: replace the monolithic Steps 1–6 extraction block (~350 lines) with a Task subagent dispatch to curriculum-auditor.md. Add mode confirmation UX between the extraction table and follow-up questions. Write per-stage mode assignments to STATE.md.

Purpose: intake.md becomes a dispatcher and orchestrator, not an analysis engine. The auditor does the extraction; intake reads the results and manages the user interaction.

Output: Updated intake.md with Task dispatch, mode confirmation stage table, per-stage override gate, and STATE.md Mode Assignment write.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-mode-enhancement/14-CONTEXT.md
@.planning/phases/14-audit-mode-enhancement/14-RESEARCH.md
@.planning/phases/14-audit-mode-enhancement/14-01-SUMMARY.md

<interfaces>
<!-- Key patterns for the refactored intake.md audit section. -->

Task dispatch pattern (from sessions.md):
```
Spawn a Task subagent:

Description of work:
> Analyze source materials and return a per-stage assessment per the curriculum-auditor agent
> specification at .claude/plugins/curriculum/agents/curriculum-auditor.md

Context provided to the Task:
- Full content of all files in workspace/{project}/source-material/
- Full content of workspace/{project}/00-project-brief/project-brief.md (if present)
- Stage schemas from .claude/reference/schemas/ (stages 02–08)
- Output path: workspace/{project}/00-project-brief/audit-results.md
```

Mode confirmation table format (plain language only — no internal mode names):
```
| Stage | What was found | What will happen |
|-------|---------------|-----------------|
| Learning Outcomes | [extraction summary] | Build from scratch |
| Assessment Design | [extraction summary] | Fill in what's missing |
| Module Structure | [extraction summary] | Keep what you have and validate it |
| Session Content | [extraction summary] | Build from scratch |
| Metaskill Mapping | [extraction summary] | Build from scratch |
| Transfer Ecosystem | [extraction summary] | Build from scratch |
| Marketing | [extraction summary] | Build from scratch |
```

Mode confirmation gate (three options, consistent with existing gate pattern):
- "Looks good" — proceed to follow-up questions
- "Change what happens to a stage" — ask which stage, present three plain-language options, update table, re-present gate
- "Start over" — destructive confirmation, reset to Opening

STATE.md Mode Assignment table format (written after gate approval):
```
## Mode Assignment

| Stage | Mode | Source |
|-------|------|--------|
| 2: Learning Outcomes | gap-fill | auditor |
| 3: Assessment Design | enrich | auditor |
| 4: Module Structure | hands-off | auditor |
| 5: Session Content | gap-fill | user-override |
| 6: Metaskill Mapping | gap-fill | auditor |
| 7: Transfer Ecosystem | gap-fill | auditor |
| 8: Marketing | gap-fill | auditor |
```

content_quality → mode mapping rule:
- absent → gap-fill
- partial → enrich
- strong → hands-off

Pitfall guard: mode confirmation gate MUST appear after extraction table (Step 2 equivalent) and BEFORE follow-up questions (Step 4 equivalent). The per-stage override happens at this gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace audit extraction block with Task dispatch and results reader</name>
  <files>.claude/plugins/curriculum/commands/intake.md</files>
  <action>
Read intake.md in full. Locate the Audit Mode section (triggered from Opening's "I have existing materials to bring in" path).

**Replace Steps 1–6** (the inline extraction logic, ~350 lines) with the following structure:

**Step 1 — Collect source materials** (keep the existing source material collection step — the user uploads files, intake saves them to source-material/)

**Step 2 — Spawn auditor agent**

Spawn a Task subagent:

> Description of work: Analyze source materials and return a per-stage assessment per the curriculum-auditor agent specification at `.claude/plugins/curriculum/agents/curriculum-auditor.md`
>
> Context provided to the Task:
> - Full content of all files in `workspace/{project}/source-material/`
> - Full content of `workspace/{project}/00-project-brief/project-brief.md` (if it exists)
> - Stage schemas from `.claude/reference/schemas/`: stage-02-outcomes.md, stage-03-assessments.md, stage-04-modules.md, stage-05-sessions.md, stage-06-metaskills.md, stage-07-transfer.md, stage-08-marketing.md
> - Output path: `workspace/{project}/00-project-brief/audit-results.md`

After the Task returns: Read `workspace/{project}/00-project-brief/audit-results.md`. Parse the four-column stage table. For each stage, extract: stage name, extraction_confidence, content_quality.

**Step 3 — Show extraction table and mode confirmation**

Display the extraction table to the user (plain language — "What was found" column from the summary column, no schema terms).

Immediately below the extraction table, display the mode confirmation table:

```
| Stage | What was found | What will happen |
|-------|---------------|-----------------|
| [stage plain name] | [summary from audit-results.md] | [plain-language mode] |
```

Plain-language mode mapping:
- content_quality = absent → "Build from scratch"
- content_quality = partial → "Fill in what's missing"
- content_quality = strong → "Keep what you have and validate it"

Note: gap-fill / enrich / hands-off are internal routing values. Never display them to the user.

**Step 4 — Mode confirmation gate**

Use AskUserQuestion with three options:
- "Looks good" — proceed to follow-up questions (Step 5)
- "Change what happens to a stage" — ask which stage (AskUserQuestion with stage list), then ask what should happen (AskUserQuestion: "Build from scratch" / "Fill in what's missing" / "Keep what you have and validate it"). Map selection back to internal mode (gap-fill / enrich / hands-off). Update the stage table. Write override to pending overrides list. Re-present the updated table and this gate.
- "Start over" — confirm destructively, reset to Opening

**Step 5 — Write Mode Assignment to STATE.md**

After gate approval, add a `## Mode Assignment` table to STATE.md with columns: Stage | Mode | Source. Mode values are the internal names (gap-fill / enrich / hands-off). Source is "auditor" or "user-override". This table is what modules.md and sessions.md read — they do not re-parse audit-results.md.

**Step 6 — Follow-up questions** (keep existing follow-up question logic, reordered after the mode gate)

Existing follow-up question decisions are now driven by extraction_confidence (not content_quality):
- High extraction_confidence → few or no follow-up questions for that stage
- Medium/Low → targeted clarifying questions
- None → no questions (gap-fill will generate from scratch)

Keep this logic, just move it after the mode confirmation gate.

Do not change the clean intake path (Opening → "I'm starting from scratch" → Thematic Group 1). That path is unchanged.
  </action>
  <verify>
    <automated>grep -n "curriculum-auditor\|Mode Assignment\|audit-results\|Build from scratch\|Fill in what" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/intake.md | head -20</automated>
  </verify>
  <done>intake.md Audit Mode section contains: Task dispatch to curriculum-auditor.md, read of audit-results.md, mode confirmation table with plain-language labels, three-option gate, STATE.md Mode Assignment write, follow-up questions after the gate. grep finds at minimum: "curriculum-auditor", "audit-results", "Build from scratch", "Fill in what", "Mode Assignment"</done>
</task>

</tasks>

<verification>
- intake.md no longer contains the inline Steps 1–6 extraction logic block (the ~350-line extraction engine is gone)
- intake.md contains a Task dispatch to curriculum-auditor.md
- intake.md reads audit-results.md after the Task returns
- Mode confirmation table appears using plain-language labels only (no gap-fill / enrich / hands-off shown to user)
- STATE.md Mode Assignment table is written after gate approval
- Clean intake path ("I'm starting from scratch") is completely unchanged
</verification>

<success_criteria>
Running `/curriculum:intake` with existing materials: spawns the auditor, reads results, shows mode confirmation table in plain language, allows per-stage overrides, writes Mode Assignment to STATE.md, then proceeds to follow-up questions. The user never sees internal mode names.
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-mode-enhancement/14-02-SUMMARY.md`
</output>
