---
phase: 14-audit-mode-enhancement
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - .claude/plugins/curriculum/commands/modules.md
  - .claude/plugins/curriculum/commands/sessions.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "modules.md pre-populated branch reads the Mode Assignment table from STATE.md and routes to hands-off, enrich, or gap-fill sub-behavior"
    - "When mode is hands-off or enrich, a side-by-side diff table appears before any files are written to 03-modules/"
    - "The diff table uses plain language — no schema field names, no underscores, no ID references in any cell"
    - "sessions.md pre-populated branch reads Mode Assignment for Stage 5 and applies the same three-mode routing"
    - "When audit-results.md is absent (user manually dropped in files), both commands fall back to gap-fill — existing generation behavior — with no error"
    - "Clean intake users (no audit mode) are completely unaffected — the pre-populated branch behaves identically to today when Mode Assignment is absent"
  artifacts:
    - path: ".claude/plugins/curriculum/commands/modules.md"
      provides: "Mode-aware pre-populated branch with diff gate"
      contains: "Mode Assignment"
    - path: ".claude/plugins/curriculum/commands/sessions.md"
      provides: "Mode-aware pre-populated branch for sessions"
      contains: "Mode Assignment"
  key_links:
    - from: "modules.md pre-populated branch"
      to: "workspace/{project}/STATE.md Mode Assignment table"
      via: "direct table read at Stage 4 row"
      pattern: "Mode Assignment"
    - from: "modules.md enrich/hands-off path"
      to: "03-modules/ file writes"
      via: "diff gate approval (gate must pass before any writes)"
      pattern: "From your materials.*What will be added"
---

<objective>
Extend the pre-populated branches in modules.md and sessions.md with three-mode routing. When MODE Assignment is present in STATE.md, both commands check Stage 4 (modules) or Stage 5 (sessions) and route to: hands-off (enforcement only, no regeneration), enrich (partial regeneration), or gap-fill (existing behavior). Add side-by-side diff display before any file writes for enrich and hands-off modes.

Purpose: Downstream commands respect source content quality — strong content is validated, partial content is enriched, absent content is generated fresh. The diff before write ensures users approve changes to their materials.

Output: Updated modules.md and sessions.md with mode-aware pre-populated branches and diff gate.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-mode-enhancement/14-CONTEXT.md
@.planning/phases/14-audit-mode-enhancement/14-RESEARCH.md
@.planning/phases/14-audit-mode-enhancement/14-02-SUMMARY.md

<interfaces>
<!-- Key patterns and existing code these tasks build on. -->

Existing pre-populated branch in modules.md (lines 36–44) — this block gains mode sub-routing:
```markdown
- **`pre-populated`** — Read all files from `workspace/*/03-modules/`. Run all module structure
  enforcement checks silently against the existing content (named group processing prompts, DCR
  trigger check when skill_type=open and bloom>=Analyze, sequence coherence). Remove any `# NEEDS:`
  marker lines from the corrected output before displaying. Display the corrected module structure
  summary and gate table. Proceed directly to the Module Structure Gate section.
  Do not regenerate — the content source is the extracted draft, not the project brief.
  On "Start over" at the Module Structure Gate: wipe all files in `workspace/*/03-modules/`, set
  Stage 4 status to `not-started` in STATE.md (clearing the `pre-populated` status), restart from
  the Generation section.
```

STATE.md Mode Assignment table (written by intake.md Plan 02):
```
## Mode Assignment
| Stage | Mode | Source |
|-------|------|--------|
| 4: Module Structure | hands-off | auditor |
```

Mode routing logic for pre-populated branch (modules.md Stage 4):
```
pre-populated branch:
  → read STATE.md Mode Assignment table
  → look up Stage 4 row
  → if Mode Assignment absent or Stage 4 row not found → gap-fill path (existing behavior, no changes)
  → if mode = hands-off → load files, run enforcement checks, show diff (violations only), gate
  → if mode = enrich → load files, generate only missing/failing fields, show diff, gate
  → if mode = gap-fill → existing generation behavior (unchanged)
```

Side-by-side diff table format (two-column, plain language only):
```
| Module | From your materials | What will be added/changed |
|--------|---------------------|---------------------------|
| [module plain name] | [what exists] | [what changes + one-line reason, plain language] |
```

Plain-language field name substitutions (from curriculum-voice.md — apply to all diff content):
- group_processing_prompt → group reflection question
- transfer_context → where they'll use it
- bloom_level → thinking level
- belief_challenging_encounter → challenging scenario
- skill_type → type of skill

Diff gate (consistent with all other gates):
- "Looks good" → proceed to write files
- "Flag an issue" → allow specific concern before deciding
- "Start over" → wipe 03-modules/, reset Stage 4 to not-started

Graceful degradation rule: If audit-results.md is absent AND Mode Assignment table is absent from STATE.md, fall through to gap-fill. Do NOT error. Do NOT show a diff. Behave as before Phase 14.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode-aware routing and diff gate to modules.md pre-populated branch</name>
  <files>.claude/plugins/curriculum/commands/modules.md</files>
  <action>
Read modules.md in full. Locate the `pre-populated` branch in the Stage 4 status check section (Check Stage 4 status → pre-populated case, currently around lines 36–44).

Replace the single `pre-populated` behavior with a mode-routing structure:

**Pre-populated branch — new structure:**

Step 1: Read `workspace/*/STATE.md`. Look for `## Mode Assignment` table. Find the row where Stage column contains "4:" or "Module Structure".

Step 2: Determine path:
- If Mode Assignment table is absent OR Stage 4 row not found → **gap-fill path**: proceed to Generation section (existing behavior, unchanged). Note: This ensures users who entered pre-populated state via clean intake (not audit mode) see no change.
- If mode = `hands-off` → **hands-off path** (below)
- If mode = `enrich` → **enrich path** (below)
- If mode = `gap-fill` → **gap-fill path**: proceed to Generation section (existing behavior)

**Hands-off path:**
1. Read all files from `workspace/*/03-modules/`
2. Run all enforcement checks silently (named group processing prompts, DCR trigger check, sequence coherence)
3. Remove any `# NEEDS:` marker lines from working content
4. Show side-by-side diff table — rows = modules that have violations or missing required content only. For modules with no violations, do not show a row (strong content, nothing to flag). If zero violations found: skip diff, go directly to Module Structure Gate with a note: "Your module structure meets all requirements — no changes needed."
5. Diff format: `Module | From your materials | What will be added/changed` — plain language in every cell, no schema field names, no underscores. Each change row includes a one-line reason.
6. Show diff gate (three options: "Looks good" / "Flag an issue" / "Start over")
7. On "Looks good": write corrected files (enforcement fixes applied, NEEDS: removed), proceed to Module Structure Gate

**Enrich path:**
1. Read all files from `workspace/*/03-modules/`
2. Run enforcement checks silently; identify fields that are present+valid vs. missing/failing
3. For missing/failing fields: generate content from project brief (targeted generation, not full regeneration)
4. Show side-by-side diff table for ALL modules — rows show what existed and what was added/changed. Mark added content NEW, changed content UPDATED in the "What will be added/changed" column (in the summary display only — written files are clean with no markers)
5. Diff uses plain language only — apply field name substitutions from curriculum-voice.md (group_processing_prompt → "group reflection question", etc.)
6. Show diff gate (three options: "Looks good" / "Flag an issue" / "Start over")
7. On "Looks good": write files (complete content, no NEW/UPDATED markers in the written files), proceed to Module Structure Gate

**Start over behavior (all modes):** Wipe all files in `workspace/*/03-modules/`, set Stage 4 status to `not-started` in STATE.md (clearing both the `pre-populated` status and the Mode Assignment row for Stage 4), restart from the Generation section.

Do not modify the Generation section, Module Structure Gate section, or any other part of modules.md.
  </action>
  <verify>
    <automated>grep -n "Mode Assignment\|hands-off\|enrich\|gap-fill\|From your materials\|What will be added" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/modules.md | head -20</automated>
  </verify>
  <done>modules.md pre-populated branch contains: Mode Assignment read, three-path routing (hands-off / enrich / gap-fill), side-by-side diff table instruction for hands-off and enrich paths, diff gate before file writes, graceful degradation when Mode Assignment is absent. grep finds "Mode Assignment", "From your materials", "What will be added"</done>
</task>

<task type="auto">
  <name>Task 2: Add mode-aware routing to sessions.md pre-populated branch</name>
  <files>.claude/plugins/curriculum/commands/sessions.md</files>
  <action>
Read sessions.md in full. Locate the `pre-populated` branch in the Stage 5 status check section.

Apply the same mode-routing pattern used in modules.md — adapted for Stage 5 (Session Content):

**Pre-populated branch — new structure:**

Step 1: Read `workspace/*/STATE.md`. Look for `## Mode Assignment` table. Find the row where Stage column contains "5:" or "Session Content".

Step 2: Determine path:
- If Mode Assignment table is absent OR Stage 5 row not found → **gap-fill path**: proceed to existing session generation behavior (unchanged). Graceful degradation — no error, no diff shown.
- If mode = `hands-off` → **hands-off path**: load existing session files, run enforcement checks (TMA arc completeness, NEEDS: marker check, HTML comment strip), show diff of violations only, gate before writing. If zero violations: skip diff, proceed directly to session display.
- If mode = `enrich` → **enrich path**: load existing session files, identify which sessions have missing required elements (pre-work absent, transfer activities missing, group reflection questions absent), generate only missing elements per session, show diff before writing, gate.
- If mode = `gap-fill` → proceed to existing session generation behavior (unchanged).

For sessions, the diff table operates at session level rather than module level:
```
| Session | From your materials | What will be added/changed |
|---------|---------------------|---------------------------|
| [session plain name] | [what exists] | [what changes + one-line reason] |
```

Same plain-language rules apply: no schema field names, no underscores, no internal labels in any diff cell.

Diff gate same pattern: "Looks good" / "Flag an issue" / "Start over"

Start over behavior: wipe `workspace/*/04-sessions/`, set Stage 5 to `not-started` in STATE.md (clearing pre-populated and Mode Assignment row for Stage 5), restart from generation.

Do not modify the session generation logic, the Task dispatch section, or the Completion Signal handling — those are unchanged.
  </action>
  <verify>
    <automated>grep -n "Mode Assignment\|hands-off\|enrich\|gap-fill\|From your materials\|What will be added" /Users/kelseyruger/Projects/a-emporium-working/knz-learner-builder/knz-builder-src/.claude/plugins/curriculum/commands/sessions.md | head -20</automated>
  </verify>
  <done>sessions.md pre-populated branch contains: Mode Assignment read for Stage 5, three-path routing, session-level diff table instruction for hands-off and enrich paths, diff gate before file writes, graceful degradation when Mode Assignment absent. grep finds "Mode Assignment", "From your materials", "What will be added"</done>
</task>

</tasks>

<verification>
- modules.md: pre-populated branch replaced with mode-routing structure; diff gate present for hands-off and enrich paths; gap-fill falls through to existing behavior; Mode Assignment absent → gap-fill (no error)
- sessions.md: same mode-routing pattern applied at Stage 5; session-level diff table; same graceful degradation
- No schema field names, internal mode names (gap-fill/enrich/hands-off), or ID references in any user-facing instruction text in the diff section
- Clean intake path in both commands: unaffected — when Mode Assignment is absent, behavior is identical to pre-Phase-14
</verification>

<success_criteria>
Users working with existing materials see: extraction table → mode confirmation (plain language) → optional per-stage overrides → side-by-side diff (when content exists) → gate approval → file writes. Users working from scratch see no change to the existing flow.
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-mode-enhancement/14-03-SUMMARY.md`
</output>
